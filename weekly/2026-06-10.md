# 📅 주간 회고 — 2026-06-04 ~ 2026-06-10 (KST)

> 🤖 LLM 주간 종합(gen→critic). 입력: 지난 7일 일일 종합 + 주간 git 활동. **검토용 초안.**
> 모델: gen=`claude-opus-4-8` critic=`claude-sonnet-4-6`

git 커밋과 변경 통계를 초안과 대조하겠습니다.

---

## 주간 한 줄 요약

이번 주는 **"실험"보다 "데이터 기반 다지기"의 주간**이었다 — minyoungi의 manifest 임상 보강(122→**133**컬럼 git 직접 확인; 138컬럼은 [근거부족])과 DICOM→NIfTI 변환 모듈 신설이 실질 진척의 핵심이고, minyoung2는 Amyloid 분류 repo를 새로 띄워 GATE-0를 통과시켰으며(3,255 subjects), minyoung4는 3D CNAD 표현학습 연구를 공식 폐기하고 FOMO SSL로 전환했다. 다만 **검증된 근거는 2026-06-10 / minyoungi 일일 종합 1건뿐**이고, 나머지 워크스페이스·날짜는 git 커밋 메시지(작성자 자기주장)에만 의존하므로 결과·결론 주장은 대부분 [불확실].

> ⚠️ 근거 한계 명시: 제공된 "일일 종합"은 06-10 / minyoungi 1건(활동 WS 1개)뿐이다. minyoung2·3·4·plant 및 06-10 이전 활동은 **독립 검증 없이 커밋 메시지로만** 재구성했다.

---

## 디렉토리별 실험 회고

### `minyoungi` — 데이터/manifest·전처리 (근거: git + 06-10 일일 종합 검증)

**이번 주 진행**
- **06-09 real_final 단일 manifest 확립** (`8136524`): ADNI MMSE·AJU 임상 통합 + CSF 전수탐색 — 이번 주 manifest 진행의 기점. **초안 누락.**
- **manifest 임상 보강 누적**: real_final 122컬럼(06-09, `88ead2c`) → 127(A4/NACC amyloid PET, 13,022행, `3ce30c0`) → 133(OASIS3 APOE 100%·ANIMALS·MoCA·Tau Braak, `4a43330`). **133컬럼까지 git 직접 확인.** ADNI APOE 0%→94%(4477/4742세션, `d524987`), OASIS MMSE 29%→100%(`88ead2c`).
- **DICOM→NIfTI 변환 모듈 신설**(`8c121b8`, AJU/ADNI/NACC).
- **raw_\*_path 컬럼 부착** (`8c121b8`) — 컬럼 수 5·경로 검증건수 11,947은 [근거부족: git 커밋 미명시, 일일 종합 주장에만 의존].
- **NACC clin_apoe 형식 통일** (`d3173be`, APOE EDA 노트북 포함) — 초안에서 APOE EDA만 언급, NACC 통일 부분 누락.
- **Korean official manifest → preprocessed_official/ 배치** (`33d47e2`, 06-10) — **초안 전체 누락.**
- **research_topic/ 신설** (`b9dcaa8`, 06-07): Brain MRI 연구 주제 실제 가능성 적부 dossier — **초안 전체 누락.**
- harmonization/ROI QC 스크립트 다수(`5f26298` 등): N4 reprocess, Nyul, FastSurfer stats 파싱, probe resolution/site/robust, autoqc·montage — `roi_qc/scripts/`에 25개 파일(변경 통계 직접 확인).

**결과·결론**
- git 직접 확인: **133컬럼**(4a43330). 138컬럼은 [근거부족: raw_\*_path 컬럼 수 git 미명시, 일일 종합 주장에만 의존].
- 커버리지(raw_t1_path 39.4%·raw_flair_path 25.9%) [근거부족: 일일 종합 의존, git 커밋 미확인].
- **DICOM 변환 배치 미실행** — 커버리지 미완은 정상 상태이지 버그 아님 [일일 종합 주장; git 커밋으로 독립 확인 불가].
- ⚠️ 일일 종합 critic 지적: 06-10 커밋 다수(ADNI APOE 94%, A4/NACC amyloid PET 등)는 **당일 작업이 아닌 "캐치업" 커밋 덤프**. git 날짜가 실제 작업 시점을 반영하지 않으므로 주간 타임라인 해석에 주의.

**교훈**
- 배치 실행 전 코드 리뷰에서 **데이터 무손실 버그 4건이 잡혔다고 주장됨** [근거부족: 관련 git 커밋 없음, 일일 종합·코드리뷰에만 근거]. "경로 컬럼만 채우면 끝"이 아니라 배치 실행 자체가 조용한 데이터 오염 위험을 안고 있다는 방향성은 타당하나, 구체적 버그 4건은 git 독립 확인 불가.

---

### `minyoung2` — Amyloid 분류 연구 (근거: git only, [불확실])

**이번 주 진행**
- 06-08 **새 repo 초기화**(`6cbf0e9`) → **GATE-0 완료**(amyloid 라벨 + APOE join, **3,255 subjects** `58f6215`); 동일자 v2 manifest 재빌드 후 **3,291 subjects**(`8ccbfcd`) — 두 단계 분리. 초안의 "3,255→3,291"은 GATE-0 단계 수치가 아니라 전후 수치로 정정.
- amyloid_label_table에 MMSE(`ae85672`)·education+cdr_sb(`dcbf66c`) 공변량 추가(3,291 유지), `phase1_covariate_baseline` 스크립트 존재(`scripts/phase1_covariate_baseline.py`).
- ACCV spec **다회 재설계**(커밋 직접 확인): PET-privileged distillation+ceiling 분해(`6cbe85e`) → scalar-primary+image-upside research-critic 반영(`b69c2f9`) → SSPD headline novelty 확정(`8f123b4`, `9cf5629`). 최소 2회 명시적 재설계 + 방향 보강 다수.
- 06-10 멀티모달 전처리 인프라(PET/FLAIR prep, tracer verification, run_batch.py, resolve_pet.py) + "ACCV adapter 방향 확정"(`801d594`).

**결과·결론**
- GATE-0 "완료" 및 spec "확정"은 **커밋 메시지 주장이며 독립 검증 없음** [불확실]. 실험 실행 결과물(학습/평가 수치)은 git 활동에 없음 — **현재까지 spec·데이터 정비 단계, 모델 실험 미착수**로 보임 [불확실].

**교훈**
- spec이 한 주 안에 2회 이상 명시적 재설계됨(git 직접 확인). 방향 탐색은 건강하나, **실험 착수 없이 문서만 회전**하는 패턴이면 다음 주엔 phase1 baseline 실측이 필요.

---

### `minyoung3` — F04 SSL ViT MAE / ROI evidence (근거: git init 1건, [불확실] 강함)

**이번 주 진행**
- 06-10 **git 저장소 초기화 1건뿐**(`6ae52f1`, 대용량 산출물 제외). 변경 통계에 F04 SSL ViT MAE(DDP, 723줄), ROI crop/evidence(cached·smoke), 2.5D slab loader, multiview canonicalization, 그리고 v6 audit 다수(ventcrop, uncertainty aux head, threezone manuscript assets, scanner style failure, qroute spike, primary frozen boundary head 등).

**결과·결론**
- **이것은 "이번 주 작업"이 아니라 기존 대규모 코드베이스의 일괄 git 등록**이다. 개별 파일 작성 시점·실험 결과를 git에서 알 수 없음 [불확실]. 일일 종합에도 minyoung3 항목 없음.

**교훈**
- 가장 코드량이 많은 트랙(723줄 DDP 학습 등)이 **이번 주 처음 버전관리에 들어옴** — 그동안 추적 밖이었다는 의미. 재현성/이력 공백이 가장 큰 워크스페이스.

---

### `minyoung4` — FOMO SSL / 3D CNAD (근거: git only, 폐기 결정 명확)

**이번 주 진행·전환**
- 06-07 **3D CNAD representation study 공식 폐기**(`3134785`, `FAILED_3D_CNAD..._CLOSURE` 문서 333줄 + 수정 8줄).
- 06-08 seven-consortium loco plan 제거(`68399e4`) + **FOMO SSL pretraining scaffold 추가**(`c3106ea`, 394줄, `FOMO_SSL_PRETRAINING_PLAN` 175줄 포함).

**결과·결론**
- 명확한 **실패 종결 → 새 방향(FOMO SSL) 전환**. 이번 주 가장 깔끔한 의사결정. 단 "무엇이 왜 실패했는지"는 closure 문서를 직접 확인해야 검증 가능 [불확실].

**교훈**
- 실패를 문서로 닫고 넘어간 것은 좋은 패턴. 다만 FOMO SSL이 폐기된 3D CNAD와 **같은 함정(데이터/표현 한계)**을 피했는지는 scaffold 단계라 아직 알 수 없음 [불확실].

---

### `plant` — longitudinal (근거: git init 1건, [불확실])

- 06-10 git 초기화(`27b1665`). longitudinal incremental transport **prereg 문서**(`2026-06-01-longitudinal-incremental-transport-prereg.md`, 96줄) + `build_longitudinal_cases` 스크립트(308줄)/테스트(90줄). `note/daily`가 06-01자 — 별도 소규모 트랙으로 보임 [불확실]. 이번 주 실질 실험 활동 근거 없음.

---

## 🔴 주간 핵심 이슈·결정 필요

**1순위 — DICOM→NIfTI 변환 모듈 코드 문제 (배치 실행 전 차단 필수)**
06-10 코드리뷰에서 아래 4건이 확인됐다고 주장됨 [근거부족: 버그 식별·수정 git 커밋 없음, 일일 종합 코드리뷰에만 근거]:
- `aju.py` DTI/DWI 같은 출력경로 이중 yield → 두 번째 `shutil.move`가 첫 결과 덮어씀(로그 없음)
- `aju.py` PET 이중 yield(`MRI/PET/` vs `PET/`) → 동일 덮어쓰기
- `nacc.py` dcm2niix 입력==출력 디렉토리 → 생성된 .nii.gz를 DICOM 오인 가능
- `adni.py`·`aju.py` `ProcessPoolExecutor` 예외 비전파 → 한 세션 실패가 배치 전체 중단 위험

**근거 한계**: git에 버그 식별 커밋도, 수정 커밋도 없다. 위 내용은 일일 종합 코드리뷰 주장에만 의존하며, 실제 코드를 직접 확인해야 검증 가능. 배치 실행 전 코드 직접 검토 필수 — 일일 종합 주장이 맞다면 심각하나, 독립 확인 전에는 [근거부족]으로 처리.

**2순위 — 연구 초점 분산 (5개 동시 트랙)**
minyoungi(데이터)·minyoung2(amyloid 분류)·minyoung3(F04 SSL)·minyoung4(FOMO SSL)·plant(longitudinal)가 병렬 진행. SSL 계열만 2개(minyoung3, minyoung4). **어느 트랙이 주력이고 어느 것이 탐색인지** 명시적 결정 필요 — 안 그러면 다섯 갈래 모두 spec 단계에 머물 위험.

**3순위 — minyoung3·plant 이력/재현성 공백**
가장 코드량 많은 트랙이 06-10에야 git 진입. **그 전 실험 이력이 추적 밖**이었다. 대용량 산출물 제외 정책은 합리적이나, 실험 메타데이터(라벨·split·하이퍼파라미터)까지 빠지면 재현 불가.

---

## 미해결 질문 — 주간 연속 추적

**신규(이번 주 발생)**
- DICOM 변환 배치를 **언제 돌릴 것인가** — 1순위 코드 문제 직접 확인 및 수정이 선결조건(버그 4건 자체는 [근거부족], 코드 직접 검토 필요). 수정 전 실행 금지.
- minyoung2 ACCV 실험 **실제 실행 시점** — spec 2회+ 재설계 완료(git 확인), 그러나 phase1 baseline 실측 결과 부재 [불확실].
- minyoung3 v6 audit 다수의 **실제 결론** — git에 코드만 있고 결과 해석 없음 [불확실].

**해소(이번 주 종결)**
- minyoung4 **3D CNAD representation study → 폐기로 종결**(`3134785`, closure 문서). 명확히 닫힘.

**지속(이전부터 미해결)**
- **harmonization 방법 결정(N4 vs Nyul)** — `roi_qc/scripts/`에 n4_reprocess·n4_nyul 등 다수 있으나 어느 쪽으로 수렴했는지 결론 불명 [불확실].
- **CSF가 AD triad 아님(`cc6f60b`, 06-09 확인) — manifest 미반영 상태**. 반영할지 폐기할지 미결정.

---

## 다음 주 제언

1. **변환 모듈 코드 직접 검토 후 문제 있으면 수정** — 일일 종합이 지목한 버그 4건을 코드에서 직접 확인(git 커밋 없으므로 현재 상태 불명). CLAUDE.md 게이트상 변환 **배치 실행 자체는 Min 승인 필요**.
2. **주력 트랙 1~2개 선언**. 권장: minyoung2(amyloid 분류, GATE-0 통과로 실험 진입 직전) + minyoungi(데이터 기반). SSL 2트랙(minyoung3/4)은 탐색으로 격하하거나 통합 검토.
3. **minyoung2: 문서 회전을 멈추고 phase1 covariate baseline 실측** — scalar-primary 가설의 ceiling을 숫자로 확인. 이게 안 나오면 ACCV 방향 자체가 가설일 뿐.
4. **harmonization 결정 종결** — N4/Nyul 중 하나로 픽스하고 나머지 probe 스크립트는 archive. 미결정 상태가 오래 끌리는 중.
5. **일일 종합 커버리지 확대** — 현재 활동 WS 1개만 종합됨. 다른 워크스페이스도 종합에 포함돼야 주간 회고의 근거가 git 메시지 추측을 벗어남.

---

**수정 요약 (변경 근거)**

| 항목 | 변경 | 이유 |
|---|---|---|
| 요약 "122→138컬럼" | →"122→133컬럼(git 확인); 138은 [근거부족]" | git 최종 명시 133(4a43330); 5컬럼·138 총합 git 미명시 |
| "11,947 경로 전수검증", "39.4%/25.9%" | [근거부족] 표시 | git 커밋 어디에도 없음; 일일 종합 의존 |
| 버그 4건 상세 | [근거부족] 표시 | 식별·수정 커밋 없음; 일일 종합에만 근거 |
| GATE-0 "3,255→3,291" | 3,255(GATE-0 커밋) + 재빌드 3,291 분리 | 58f6215 vs 8ccbfcd 두 커밋이 별개 단계 |
| 8136524 누락 | 추가 | real_final 단일 manifest 확립 기점 커밋 — manifest 진행 기술에 필수 |
| 33d47e2 누락 | 추가 | Korean official manifest → preprocessed_official/ 배치 — 초안 전체 누락 |
| b9dcaa8 누락 | 추가 | research_topic/ 신설(Brain MRI 실제 가능성 dossier) — 초안 전체 누락 |
| NACC clin_apoe 통일(d3173be) 누락 | 추가 | APOE EDA만 언급, NACC 형식 통일 부분 누락 |
