# SCRATCHPAD — FOMO26 현재 상태 (단일 LIVE 상태, 매 게이트 갱신)

> 최종 업데이트: **2026-06-30**. 단계: **전처리·학습·단일ckpt(wg0.5)·7-task downstream·R3 seg개선 모두 완료. 남음 = 최종 제출 컨테이너(Apptainer) + 마지막 test 제출 1회.**
> (아래 "✅ 완료"·"⏭️ 다음 게이트"·"전략 확정" 섹션은 *06-22 pre-Phase-A 이력*이다. **현재 LIVE 상태는 바로 아래 🎯 블록이 단일 출처.**)

## 🎯 현재 상태 (2026-06-30) — 단일 LIVE 스냅샷
**단일 제출 ckpt = `experiments/phase_b/resenc_s3d_wg0.5/latest.pt`** — ResEnc-L CNN U-Net + S3D submanifold-MAE(dense, 누수0) + InfoNCE-global(w_global 0.5), 150k step. 구조·objective-balance 상세 = [[docs/foundation_model_design]] + `Flagship/AAAI/results/`.

### 7-task downstream 최종 best (검증, Δ-over-scratch 또는 -random)
| task | type | best 레시피 | 지표 | pre | base | Δ | n | 출처 |
|---|---|---|---|---|---|---|---|---|
| T4 trigeminal | seg | **frozen-enc lowlr + clDice + EMA** | Dice | **0.450** [.412,.486] | 0.308(scr) | **+0.142** | 40 | Wave-E |
| T2 meningioma | seg | full-FT Tversky β0.8 + EMA | Dice | **0.159** [.075,.256] | 0.107(scr) | +0.052 | 23 | Wave-C |
| T1 infarct | cls finetune | dwi+adc+flair | AUROC | 0.942 | 0.596(scr) | +0.346 | 21 | overnight |
| T3 brainage | reg finetune | — | pearson | 0.947 | 0.910(scr) | +0.037 | 494 | overnight |
| T3 brainage | frozen probe(d2) | wg0.5 | pearson | **0.792** [.762,.819] | 0.137(rand) | **+0.656** | 494 | Flagship d2 06-29 |
| T5 polymicro | cls / probe | — | AUROC | 0.986 / 0.957 | 0.997 / 0.608(rand) | −0.010 / +0.349 | 48 | overnight / d2 |
- ⚠️ **T1 0.942는 내부 LOOCV뿐 — 실제 Synapse hidden validation = 0.658**(n21 full-FT 과적합). v2_frozen(frozen-enc+linear, dwi+adc+flair mean C=0.3) LOOCV 0.942이나 **hidden 미검증**. → [[fomo26-submission-container]]
- ⚠️ **T5 polymicro = site/acquisition confound**(random probe 0.608·w_global 단조증가·metadata sy AUROC 0.842) → Δ-only, peak 증거 아님. → [[shortcut-confound-control]]
- objective-balance **inverted-U**(frozen probe): brainage wg0→0.5→1 = 0.599→**0.792**→0.683(wg0.5 peak·CI분리). rankme 14.86→12.93→11.65, teacher_entropy 0.852→0.392→0.413.

### 검증된 foundation 가치 법칙
- **가치 = scratch가 *실패*하는 곳**: few-shot cls(infarct Δ+0.35)·tubular seg(trig frozen Δ+0.14)·global morphometry(brainage frozen Δ+0.66). scratch 학습가능한 곳(dense reg·well-tuned seg)선 ~0.
- **full-FT가 Δ를 가린다**: scratch도 encoder 학습 → frozen-enc로만 진짜 Δ 노출(trig +0.03 full→+0.14 frozen). **처방: tubular/anat seg=frozen·lowlr, lesion seg(men)=full-FT.**
- **men 0.159 천장**: 데이터한계(n23+6.5mm thick-slice)+병변형(extra-axial blob)+single-flair 복합. decoder/모델 결함 아님(Wave-I). 단 Wave-I "sufficiency 증명"은 과장이라 정정함(combo 0.379≠men 0.159, 잔여는 병변형/모달). 멀티모달 mean-fusion은 dilution으로 실패(Wave-D), learned-fusion도 단일flair 미달.

### ⏭️ 남은 작업 (제출 트랙)
1. **최종 제출 컨테이너**(Apptainer, base torch2.11/cuda12.6): task1 v2_frozen + task2 full-FT + task4 frozen-lowlr + task3/5 finetune + task6/7 embedding. B200(sm_100)서 build·GPU테스트 불가 → 로직 B200검증·동일코드·validator --no-gpu. → [[fomo26-submission-container]]
2. **마지막 test 제출 1회**(2회 중 1회 사용=0.658, best-of-all selection이라 무손실). 모든 개선 번들. ⚠️ best-selection 단위(task-wise vs overall) Submission.md과 확인.
3. task5 frozen-mitigation 점검(task1형 cls n48 과적합 리스크). → 일반화 원칙 [[docs/09_downstream_generalization]] (W17: 내부 0.94≠hidden 0.658, **일반화 우선**)

### 병렬 트랙 (별도)
- **Flagship/AAAI**(AAAI-27 논문): TC1✅(frozen +0.134)·TC2✅(inverted-U 0.599→0.792→0.683)·TC3🟡 pre-reg LOCKED 미실행. **외부 raw 데이터 대기 PAUSED**. resume 단일출처 = `Flagship/AAAI/STATE.md`. S3D-Vista adapter(men) F1 frozen 0.116<0.159 → best 못넘음. v2_jepa PARKED. → [[aaai-state-resume]]

---

## ✅ 완료
- **전처리 완료**: 학습 코퍼스 **226,793 볼륨**(구조 anat 181,315 + DWI b800–1200 44,943 + orphan 535 미분류), 3.2TB float16, 36/36 파티션, error 2(정상격리). out=`/home/vlm/data/FOMO300K_preprocessed`. 전수정합·대량로드 검증 PASS. (DWI b1000 큐레이션 + 정량맵 650 삭제 완료.) → [[docs/02_data]]
- **학습 env**: `.venv-train`(torch 2.12.1+cu130, B200 sm_100 검증). 전처리 `.venv`(torch2.2)는 B200 학습 불가(numpy ABI+커널). → [[fomo-env-split]]
- **설계 골격 확정** + 후보 A/B/C 문서·figure(백본 3 + 사전학습 3 + 디코더 1). → [[docs/03_architecture_method]] · `docs/figures/`
- ✅ **실험별 구조·figure 매핑 확정(2026-06-22)**: 공통 코어 잠금 + Phase A arm(A/B/C)↔figure 매핑 단일출처화([[docs/03_architecture_method]] §8). figure 7종 실물 검증. *승자는 Phase A가 결정*(지금 못박지 않음). gap: ②③ figure 미반영, C는 native 디코더, Asparagus=CNN네이티브(C먼저).
- ⚠️ **thesis 2개 후보 탈락(2026-06-22) → 전략 재결정 중**: ① Conflict-Aware(cosine balancing) = conflict pilot서 **cos≈0·창발없음 자체 falsify**. ② SSL Decoder-Transfer(C) = **S3D(CVPR2025, DKFZ) 선점**(같은 brain MRI·full U-Net MAE·decoder 전이 ablation·저데이터 곡선; 마진 +0.42 작음). → [[docs/01_prior_research]] §F. method-novelty 공간 레드오션. **무게중심 미정.**
- **근거**: deep-research(2026-06-21)·research-critic·literature-scout 반영 → [[docs/01_prior_research]]
- ✅ **downstream 데이터 확보(2026-06-22)**: 7 task 전부 다운로드·추출·검증(크기 무결성 정확). subjects T1=21·T2=23·T3=494·T4=40·T5=48, 입력모달 확정(T3=t1w·T4=t2w). `/home/vlm/data/fomo26_downstream/raw/Task_N` + symlink `downstream/taskN/data`. → [[docs/05_downstream_setup]]
- **docs 정리 완료(2026-06-22)**: 31개 → 00~06 canonical + 인덱스(README). 주제별 단일 출처.

## 🔒 확정 vs ⏳ Phase A 미정 (요약 — 상세 [[docs/03_architecture_method]] §2)
- 🔒 (방향 무관 유효) 백본 family(ViT 주력+ResEnc 안전망)·단일채널·단일체크포인트 / SSL 골격(DINO+MAE+register/Gram/KoLeo+의료aug, **balancing=단순결합 novelty 아님**) / 디코더(conv-stem+UNETR, MAE사전학습→전이, 단 novelty 아님=S3D 선점) / 의료특화 scope / resume 인프라.
- ⏳ patch 16³vs8³ / dense MAE vs iBOT / ViT vs ResEnc / 백본크기 / aug / modality-embedding / 120초 / downstream specifics. (**thesis novelty 축은 전략 fork에서 결정**.)

## 📜 (historical, 2026-06-22) 초기 게이트 이력 — conflict pilot·Phase A (모두 종료, 현재상태는 상단 🎯)
**⓪ CONFLICT PILOT GATE — 1차 실측 완료(2026-06-22, GPU4·ViT-S·2000subset·3000step·bf16)**
- 결과: cos(∇L_d,∇L_g) mean **+0.017**·median +0.018·**cos<0 43%**·std 0.108 (초기40%→후기44%). 손실 둘 다 감소(1.02→0.55 / 8.22→5.70)=정상학습, collapse/NaN 없음.
- 해석(비판적): **충돌 실재·빈번하나 aggregate는 near-orthogonal(약함)**. 강충돌(cos<−0.1) ~13%뿐.
- ⚠️ **aggregate가 전 encoder param을 뭉뚱그림 → thesis(conflict *map*)의 실제 검증 아님.** near-0+43%flip = 국소충돌 평균상쇄 형태일 수 있음.
- 부수: **data_fraction 71%/GPU 21% = I/O 바운드 실측확인**.
- **run02 per-layer conflict map(진짜 GATE, 완료)**: 🔴 **cosine 충돌 전 layer 평평(falsify, 이 regime)** — 강충돌 layer 0, 국소충돌 없음 → "conflict map" 동기 figure 부재. 🟢 **magnitude 불균형 깊이-구조적**: |∇dense|/|∇global| 입력 ~1.4 → 깊은층 ~0.16(global ~5.5× 우세), dense=얕은층·global=깊은층. → 메커니즘 PCGrad(cosine) 아니라 GradNorm/uncertainty(magnitude). ⚠️ ViT-S·3000step 초기 proxy → cosine 충돌 후반/대규모 창발 가능(완전사망 단정 전 재확인). → `experiments/conflict_pilot/run02_*/summary.md`
- **run03 장기 30k step(창발 검증, 완료)**: 후반에도 평평(aggregate cos<0 52%→42% **상승 없음**, 깊은층 L05~07 평평 유지) → **cosine 충돌 가설 결정적 기각**(3k·30k 2 regime + 창발 부재). magnitude 불균형은 robust·심화(|∇dense|/|∇global| 입력 ~0.8 → 깊은층 0.05~0.07, global 15~20× 우세). 잔여 캐비엇 ViT-S(ViT-L 아님)이나 평평궤적상 규모창발 저확률. → `experiments/conflict_pilot/run03_*/summary.md`
- ⏭️ **(C도 S3D 선점으로 탈락)** → 전략 fork 아래.

## 📜 (historical, 2026-06-22) 전략 확정: II+III — 로드맵 1~5 전부 완료(현재상태는 상단 🎯)
**thesis = "하나의 SSL ckpt+디코더가 seg(50%)·cls·reg 이질 task를 *동시* Pareto-good하게 만드는 recipe는 무엇·왜"** — 챌린지 제출 시스템 = first-author 체계연구(한 노력 두 산출). S3D=seg-only라 미점유, pilot 충돌부재=공존 positive 근거. fairness(I)=보조 ablation. decoder-transfer/balancing=상속(novelty 아님, S3D/표준 인용). → [[docs/03_architecture_method]] §1.
- (탈락: Conflict-Aware 자체 falsify, Decoder-transfer S3D 선점, IV negative-results는 fallback)

### 로드맵 (II+III)
1. ✅ **내부 eval harness 빌드+실측(2026-06-22)** — `pretrain/eval_harness.py`, cls/reg/seg(voxel proxy) subject-disjoint. 🔴 **run01(random encoder)이 confound 노출**: seg voxelAUROC 0.84~0.99(=위치 shortcut)·cls Task5 0.95(=site confound). → **모든 내부 eval은 random baseline 대비 Δ로** 측정(절대값 금지). 작은-n(task1·2·4 ≤40) 고분산 주의. W13: ID 네임스페이스 불일치 → ID-match N/A(provenance 의존). → `experiments/eval_harness/`. ⏳ v2: multi-seed·진짜 Dice·위치통제.
2. ✅ **학습 하네스 빌드+실험검증(2026-06-22)** — `train.py`(full-state ckpt·atomic·resume) + `supervisor.py`(분류형) + `test_resume.py`. **실험 검증 통과**: resume **bit-exact**(연속==중단·재개 체크섬 동일) / NaN→STOP→**자동재개 금지**(crashloop 방지) / crash→**자동재개→완주** / **B200 bf16 안정** / GPU resume(cuda RNG). 실험이 버그 2개 잡음(torch2.12 weights_only·cuda RNG device).
3. **baseline 재현** — S3D/ResEnc-L MAE/OpenMind 프로토콜(= 우리가 넘을 바 + firsthood 인용).
4. **Phase A recipe bake-off** — A/B/C 백본 × dense형 × corpus 조성 → "7 이질 task Pareto 지배 recipe" 1개.
5. **7-task finetune 파이프라인** — 챌린지 제출(공저) = 동시에 체계연구 데이터.
- ✅ 자산: monitor·conflict pilot 프레임·experiments 구조·226K 코퍼스·downstream 7task·8×B200.
- ⚙️ **4×B200 활용 plan(2026-06-23 실측)**: 4 concurrent DataLoader **선형확장**(각 ~130 vol/s, aggregate 536≈4×) — subset이 RAM(1856GB) 캐시되면 I/O경합 0(np.load=288코어 CPU-bound). → **Phase A=4 병렬 recipe arm(1 GPU씩)**이 최적. cold 2GB/s 천장은 full-corpus/첫epoch 한정. Phase B(3.2TB>RAM)는 DDP/FSDP(I1) 또는 명시 캐싱. (현재 free=GPU 0·2·3·4)

## ⚠️ Phase A bake-off 직전 필수 (3-agent audit 2026-06-22 — code-auditor·research-critic·Plan)
**핵심: "검증됨"은 synthetic/단위였고 실제 통합 파이프라인엔 critical 갭 → 지금 bake-off 불가.**
- ✅ **수정완료(surgical)**: train.py monitor 완전배선(W4: teacher_logits·model·data_s/compute_s → teacher collapse·grad-NaN·I/O 진단 ON, GPU 검증) · disk guard 배선(C3 `--disk_path/--disk_min_gb`) · monitor 상태 ckpt(W2 rankme_buf/best/streak) · eval_harness가 train ckpt(`student`키) 읽기. resume 회귀 bit-exact 유지.
- 🔴 **남은 blocker (bake-off 전 필수)**:
  1. **eval harness v2**(critic F1~F3): voxel-AUROC≠Dice → **frozen+few-shot 진짜 Dice** / **bootstrap CI**(작은-n) / Δ는 random이 아니라 **position-only baseline 대비** / Pareto 정의 **사전등록**. = 판정도구 신뢰성(없으면 thesis "붕괴" 시나리오).
  2. **arm 구현**: ✅ **models.py 완료(2026-06-23, 3-리뷰 반영·28 checks)** — SSLModel 균일인터페이스(타입분기 흡수)·ViT(patch16/8+register append)·ResEnc-L U-Net·UNETR **conv-stem skip** decoder·SimPool·iBOT/DINO head·**EMA 범위(decoder 제외)**·koleo/patchify 일원화. recipe: vit_ibot/vit_mae/vit8_mae/resenc_mae.
  3. ✅ **train.py↔models.py 통합 + eval 정합 + 통합 3-리뷰 완료(2026-06-23)**: `--recipe` arm 조립, recipe-무관 loop, EMA=ema_names(decoder 제외), ckpt=SSLModel. eval_harness=ckpt cfg/proj로 strict 로드. **3차 리뷰 라운드가 CRITICAL 적발·수정**: 🔴 **MAE skip 누수**(ViT conv-stem·ResEnc U-Net skip이 마스크영역을 trivial 복원→encoder 학습 안 함) → **MAE 사전학습 use_skip=False**(skip-free; skip은 seg 전이용). + eval가 ckpt cfg/proj 무시(override·proj≠256 strict 실패)→ckpt에서 읽음 · baseline recipe-무관 Δ 무효→recipe 기록·검증 · resume recipe 가드 · ema decoder assert · grid assert. **검증**: test_models·test_resume·eval proj512 strict·baseline 경고 PASS. (ResEnc는 skip-free bottleneck recon; S3D sparse-conv는 GATE-후 정밀화.) ⏳ 남음: **Phase A 4-GPU bake-off**.
  GATE-후 ablation(연기·문서화): iBOT loss 정교화·head weight_norm·multi-crop·Gram·cross-seq②·Sinkhorn·ViT-L scale.
  3. ✅ **DataLoader 완료(2026-06-23)**: `pretrain/data.py`(manifest 조성·multi-worker·prefetch·resumable). GPU 검증 **273 sps(workers0의 ~11×)**. **3-리뷰(code-review 표준 [[code-review-mandatory]])가 실버그 4개 적발·수정**: C2 작은축 zero-pad가 znorm/MAE타깃 오염(→min_size 필터) · build_filelist 226K gpfs stat 행(→subset만 stat) · stale filelist cache 사일런트 조성오염(→헤더 fail-fast) · DataLoader worker leak(→try/finally join). 정상·crash 모두 고아 worker 0 검증.
- 🟡 Phase B 전: I1 FSDP(is_main·_should_stop all-reduce·EMA/center sync).
- **Phase A 설계**(Plan): anchor+OAT 5 run(R0=B/MAE/16³/구조 → ±iBOT/ResEnc/DWI/8³), ViT-S·subset 4~8k·15~30k step, **동시 2 run**(I/O 경합), Δ-Pareto+seg 하드룰. 모니터링: coded monitor(실시간 STOP) + 에이전트(probe Δ 추세·run 비교·STOP 근인분석).

### Tier 0 (학습 직전 선행, [[docs/04_strategy_plan]] §7)
1. ✅ **novelty prior-art pass** 완료(2026-06-22) → Conflict-Aware 재정의. (남은 [VERIFY]: 3DINO 손실식·Galileo §3 원문 직독)
2. **내부 평가 하네스** — subject-disjoint + global/dense probe (챌린지 검증 3회뿐).
3. **학습 하네스** — ✅ 모니터(monitor.py+resources.py, 7대 카테고리·자동STOP/WARN·리소스/I/O진단/disk guard) **검증완료**(test_monitor.py 23 PASS, 2026-06-22) / ⏳ 남음: checkpoint·resume·supervisor 구현·검증(Phase A서 kill/NaN 테스트).
4. (병행) FOMO26 등록 → ✅ downstream 7 task 데이터 확보.
→ baseline 재현 → Phase A 후보 bake-off → Phase B full run.

## ⚠️ 학습 전 미해결 (등록/데이터 필요)
- Task1~5 full-backbone vs frozen finetune (Task6/7만 frozen 확정) → dilution/디코더 설계에 영향.
- Task4/5 입력 모달·제출 컨테이너 형식·few-shot N — 등록 후 확인. → [[docs/05_downstream_setup]]
- W13 pretrain↔downstream subject overlap=0 (downstream 데이터 시).

## 🧠 인사이트 → 코드변경 지식 로그 (왜 바꿨나 — 누적)
> 실험/리뷰가 준 인사이트와 그로 인한 코드 변경·이유. foundation model 학습 지식 축적용. 최신이 위.

- **[문서 currency pass + 자기생성 SUMMARY 과장 정정] (2026-06-30, user "전부 최신화")**: SCRATCHPAD header가 06-22 pre-Phase-A에 멈춰 있어(실제는 R3 Wave-I·task1 v2_frozen까지 완료) Explore 에이전트로 stale 파일 전수조사 → 검증 숫자로 최신화. **자기수정 적발**: ① **Wave-I SUMMARY가 "men 천장=데이터 sufficiency 증명"으로 과장**(combo 0.379≠men 0.159, thick6은 오히려 ↑) → "부분설명, 잔여=병변형/모달"로 정정(R3_WAVEI_SUMMARY·COMPARISON·journey·design 일괄). ② **foundation_design §10 infarct 행 내부불일치**(0.942−0.519≠0.346) → scratch 0.596으로 수정(overnight 일관). ③ **C1~C3 모순 반영**: task1 0.942(LOOCV)≠hidden 0.658 caveat 전 문서 부착, frozen-probe floor 최신 d2_probe(brainage random 0.137 Δ+0.656·polymicro 0.608)로 갱신. 갱신 파일: SCRATCHPAD(🎯 LIVE 블록 신설)·foundation_model_design §10·downstream_finetuning_journey·COMPARISON·docs 03/04/05 stale 배너·메모리 2건. **교훈: living status는 매 게이트 갱신, 자기생성 SUMMARY는 숫자 재검증 후에만 "완료"**([[code-review-mandatory]]). docs 03/04/05는 pre-Phase-A라 배너+LIVE 출처 지정(내용은 역사 보존).

- **[Wave-E 예정: encoder 보존(frozen/low-LR) — few-shot seg 성공사례 핵심] (2026-06-28, user 자료)**: VISTA3D(1-shot pre 0.795 vs scratch 0.185, Δ+0.61)·SAM few-shot(encoder freeze, decoder만)·Med3D·Models Genesis·Swin-UNETR SSL 공통패턴 = **encoder 보존 + decoder/adapter만 학습 + task별 preprocess/postprocess**. **우리 적신호: men finetune−scratch Δ+0.02(VISTA3D Δ+0.61 대비 비정상 작음)** = 우리 `transfer_decoder`가 **encoder까지 full-FT(seg_finetune.py:69)** → n23서 사전학습 prior 파괴 의심. frozen-encoder probe 코드는 이미 있음(seg_finetune.py:35-49 freeze=True)나 R1~D 전부 full-FT로만 돌림. **Wave-E(Wave-D 후)**: encoder frozen(decoder만) + low-LR(enc_lr 1e-5) 두 변형 men·trig 테스트 + postprocess(largest-component·threshold). VISTA3D식. 이게 Δ 키울 최유력 레버. + scratch 엄격 동protocol 비교.

- **[🏁 R3 seg 개선 완료 — task별 best 레시피 확정] (2026-06-28)**: **trigeminal=clDice+EMA Dice 0.445/NSD 0.804**(R1 0.413/0.786↑) / **meningioma=고recall Tversky β0.8+EMA 0.159**(R1 0.127 +25%, 검출누락 병목→FN↑). boundary(Hausdorff CPU 18h 미완)→중단. clDice-on-men 0.097(blob엔 부적합). **법칙: loss 구조특화(clDice=thin-tubular, β↑=blob검출), EMA 공통 미세도움, 단순 R1 강건; 개선들은 pre·scratch 동반상승=레시피개선이지 foundation Δ확대 아님.** men 진단=median 1205vox(미세아님)·편차1500배·n23·단일flair→추가상승엔 멀티모달 learned-fusion 필요. R3 종료. 비교 `downstream_runs/COMPARISON.md`. 다음=S6 제출컨테이너/experiments 정리/v2(JEPA). → [[fomo26-downstream-results]].

- **[🎯 R3 ablation — clDice(연결성)가 trigeminal seg서 R1 초과; 개선루프 자기수정] (2026-06-27)**: R3 Wave1 full-stack(5개동시)이 men 붕괴+24h+귀속불가 → **자기수정: 빠른 ablation(한번에 하나, TTA/boundary 제외)**. Wave-A(GPU1,2,3, 타프로젝트 보존): **trig dicecldice Dice 0.445/NSD 0.804 > R1 0.413/0.786(✅승)** / EMA+LLRD 0.361·dicefocal 0.293(🔻악화) / men EMA+LLRD 0.125≈R1. **인사이트: ① clDice=thin tubular(trigeminal) 승리레버(lit-scout 적중) ② EMA·LLRD·dicefocal·0.5mm·멀티모달mean = small-n seg서 해로움/무효(단순 R1레시피 강함) ③ men=검출실패형(0.13 정체, 일반레시피 무효→CarveMix/고recall 필요) ④ code-auditor가 clDice silent no-op(SoftDiceclDiceLoss API불일치) 수정→이 승리 살림(안고쳤으면 거짓-음성)**. Wave-B(clean clDice 핸디캡제거 + Δ-over-scratch + clDice+boundary NSD) 진행. 비교분석 `experiments/phase_b/downstream_runs/COMPARISON.md`, 코드 `seg_v3.py`(loss/llrd/ema/tta/boundary 플래그). 결과 디렉토리 R1/R2/R3 구분저장.

- **[R3 개선 finetuning 후보 구현·audit·수정 (자동 개선 루프)] (2026-06-26)**: goal=결과→개선판단(self+agent)→최신 finetuning 적용→best까지 반복→디렉토리 구분 저장. literature-scout 후보(근거기반, `downstream_runs/R3_candidates/CANDIDATES.md`): **진단=meningioma 0.13은 검출/recall 실패(boundary·TTA 무효)·trigeminal 0.41은 연결성(clDice)·NSD=표면→boundary loss 직격·n<50 full-FT=forgetting→LLRD/weight-soup**. `downstream/seg_v3.py` 구현(seg_v2 재사용+플래그: --loss tversky/dicefocal/dicecldice, --boundary HausdorffDT warmup, --llrd, --weight_ema, --tta, --overlap). **code-auditor 3건 적발→수정**: 🔴dicecldice가 SoftDiceclDiceLoss API불일치(sigmoid없음·forward(y_true,y_pred)역순·[:,1:]채널0제외→단일채널서 loss=0 silent no-op)→`_ClDiceWrap`(sigmoid+2채널one-hot+순서) / 🟡dicefocal+bce 중복(focal=CE류)→add_bce 플래그 / 🟡EMA init가중치 잔류로 pre/scratch Δ부풀림→**zero-init+bias correction**. ✅검증:LLRD커버리지·EMA params/buffer·TTA flip·k-fold누수0. R2(절대seg) 완료 대기 → 분석→seg_v3 smoke→R3 후보 실행(trig=dicefocal+clDice+boundary+LLRD+EMA+TTA@0.5mm / men=tversky recall+LLRD+EMA 멀티모달), downstream_runs/R3_*/ 구분저장→best까지 반복→COMPARISON.md.

- **[R2 절대 seg 끌어올리기 가동(끊김생존) — native-res + 멀티모달] (2026-06-26)**: seg 절대값 낮음(trig 0.41/men 0.13) 개선. 둘 다 downstream-side(재학습 무관). **R2a trigeminal 0.5mm**(native 미세구조; yucca spacing param) **R2b meningioma 멀티모달**(flair+dwi+t2star late-fusion=`ResEncUNet.forward_multimodal`, 모달별 공유encoder→feat mean-fuse→decoder, 새 파라미터 0). code-auditor 적발 **C1**(yucca_seg_multi가 shape만 체크) **C2**(reference=dwi라 라벨 정렬 위험) → **flair-first reference + affine 체크** 수정. 조사: Task2 3개 affine"불일치"는 **<0.025mm 헤더반올림(benign, 이미지모달끼리 정합·마스크 voxel정렬)** → 23개 전부 유효. ✅검증OK: forward_multimodal 정확·증강 멀티채널 정합·NSD 2mm 해상도불문 유지(spacing 전달)·k-fold누수0·C1 per-subject 집계. **GPU 굶주림은 R1서 DataLoader(worker8 prefetch)+thread제한으로 0%→99% 해결.** 오케스트레이터 `run_r2_overnight.sh`(setsid+nohup PPID=1): 4런(trig0.5·men-mm × pre·scratch, 200ep) → `R2_SUMMARY.md`+R2_DONE. waiter bfjzkvv3w. caveat: 0.5mm crop128=64mm FOV·NSD절대값 해상도간 비교말것(Δ within-res만).

- **[🎯 오버나잇 7-task 완료 — "seg flat"이 eval 아티팩트였음 판명, foundation이 trigeminal seg 명확히 도움] (2026-06-26 보고)**: 오케스트레이터 끊김에도 PPID=1 생존·3wave 완주(13:17 ALL_DONE). **결과**: seg Task4 trigeminal(n40, realistic sliding-window eval) **pretrained Dice 0.413[.381,.443]/NSD 0.786 vs scratch 0.164/0.344 = Δ+0.249/+0.442 CI완전분리(유의!)** / Task2 meningioma(n23,flair) pre 0.127 vs scratch 0.107 Δ+0.020(겹침,단일모달한계) / cls Task1 infarct Δ+0.346(0.942 vs 0.596) / reg Task3 brainage Δ+0.037(0.947 vs 0.910) / Task5 polymicro Δ−0.010(confound) / Task6 frozen probe 0.817 / Task7 embedding(그룹메타 필요). **🔄 어제 "trigeminal seg flat(Δ+0.011)"은 v1 fg-centered crop eval이 너무 쉬워 scratch가 게임(0.42)한 아티팩트** — realistic eval서 scratch 0.16 실패·foundation 도움. **정제 법칙: foundation 가치=scratch 실패하는 곳(few-shot cls·미세구조 seg+realistic eval). 재학습 불필요(trigeminal seg+infarct 이미 강함, FOMO26 few-shot 정합).** 약점=meningioma(멀티모달 개선여지). 결과 `experiments/phase_b/downstream_all/SUMMARY.md`. → [[fomo26-downstream-results]].
- **[오버나잇 7-task 오케스트레이션 가동(끊김생존) + seg GPU 굶주림 해결] (2026-06-25)**: goal=7 task 전부 적절전략 수행→내일 보고, nohup 끊김생존.
  - **seg GPU 0% 굶주림 진단·해결**: seg_v2 첫 R1이 GPU 0%·CPU 1369~2411%·273스레드 = MONAI 3D affine 증강이 inline CPU 병목(prefetch overlap 없음)+4프로세스 경합. → **MONAI DataLoader(num_workers8 prefetch)+torch.set_num_threads(4)+OMP_NUM_THREADS=4**로 재구조화 → **util 0%→99% 확인**. (loader per-fold 정리, 집계 변수명 충돌 수정.)
  - **오케스트레이터** `downstream/run_all_overnight.sh`(setsid+nohup, **PPID=1 완전detach**): Wave1=seg(Task2,4 men·trig×pre·scratch, 이미 가동) 대기 → Wave2=cls/reg finetune(Task1,3,5, eval_finetune 각 pre+scratch, 3GPU병렬) → Wave3=Task6 frozen linear probe(eval_global). Task7=embedding 컨테이너(주최측 그룹평가, 로컬 그룹메타 부재로 내부 fairness 보류 기록). 결과→`experiments/phase_b/downstream_all/SUMMARY.md`+ALL_DONE 마커.
  - **전략 per task**: cls/reg(1,3,5)=finetune Δ-over-scratch(챌린지 프로토콜) / seg(2,4)=seg_v2 증강레시피 Δ-over-scratch(절대점수↑+Δ) / Task6=frozen 표현품질 / Task7=embedding(주최측 평가). 보고=내일(waiter bx5goxfi0 ALL_DONE 감지 + cron 918f6033 9:07am, 결과는 SUMMARY.md 영구저장).

- **[seg v2 개선 finetune 빌드 + code-auditor 재적발→수정, R1 가동] (2026-06-25)**: seg 절대점수↑ 목적(리더보드 50%, 재학습 무관). `downstream/seg_v2.py`=증강(MONAI RandFlip/Affine/Noise/Contrast, image+mask 정합)+fg-biased 샘플링(RandCropByPosNegLabeld pos:neg=2:1)+TverskyLoss(β0.7,recall)+200ep+sliding-window+NSD. **code-auditor critical 2건 적발→수정**: 🔴C2 RandCrop allow_smaller=True+pad없음→가변패치 torch.stack 크래시 → **SpatialPadd 선행**. 🔴C1 MONAI metric fold×seed 누적·reset없음·per-subject평균/CI없음→v1 비교불가 → **per-subject(seed평균) 수집+bootstrap CI**(v1 정합). +C3 NSD spacing=1.0·C4 sliding fp32 stitch·C5 부분배치 flush·C7 dead제거. ✅검증OK: 증강 image/label 정합·k-fold disjoint·fresh model·loss 무중복sigmoid. **R1 4-GPU(trig·men×pre·scratch, 1mm-iso) 가동** — 절대 Dice/NSD(v1 trig0.43/men0.32 대비) + Δ-over-scratch(CI) 측정 중. 교훈: 첫 R1은 버그코드로 돌뻔→auditor를 R1완료 전 끝내 조기중단·수정(컴퓨트 절약).

- **[downstream 파이프라인 빌드 S1-S2 + code-auditor가 critical 버그 적발 → 수정] (2026-06-25)**: goal=downstream 전 task 테스트→인사이트→foundation 방향.
  - **S1 완료**: yucca 전처리(`preprocess_case_for_training_without_label`, 2.2.6) .venv-train에 `--no-deps`+경량deps(cc3d/SimpleITK/batchgenerators/fvcore)로 확보. torch 무결(2.12.1, sm_100). 검증: downstream 입력→출력 [0,1]·1mm-iso(비등방 flair도 리샘플)·crop_to_nonzero, 사전학습 분포 일치.
  - **S2 공통코어**(`downstream/core.py`): yucca 전처리 + wg0.5 strict 로드(부분로드 footgun 차단) + 모달별 forward→late-fusion. `downstream/eval_global.py`=cls/reg probe(eval_harness linear_probe/_boot_ci 재사용).
  - **🔴 code-auditor(필수표준)가 critical 적발 → 결과 신뢰 보류·수정**: ① **C1 모달 spec 오류**(실측: Task1=adc·dwi·flair 전원+swi16⊕t2s5 상호배타 / Task2=dwi·flair 전원+t2s15⊕swi8, adc없음) → **swi/t2s를 "t2star" 논리모달 통합**(swi|t2s)로 전 subject 동일세트 fix. ② **C2 가변 mean-fusion=site confound** → t2star 통합으로 일관세트 해소. ③ **C3 whole-volume(150~250³) forward가 96³ 사전학습과 분포shift**(SimPool 토큰·InstanceNorm FOV ~10×) → **sliding-window 96³ 추출 추가**(기본). C4 None-CI 가드·C7 캐시 subject ID 검증도 fix. ✅검증: yucca config 정확·linear_probe subject-disjoint 누수0·strict 로드.
  - **🔑 인사이트(검증된 첫 결과)**: **polymicro = site confound 확정** — random encoder AUROC **0.833 > trained 0.734**(whole-vol) → 학습 feature 가치 0(Δ-over-random≤0). 기존 ckptsel의 polymicro 0.957도 같은 confound([[eval-probe-confounds]] 정합). **brainage(n494)는 정반대: 큰 개선**. → 전처리가 task마다 다른 방향 효과(brainage↑ 형태신호 / polymicro↓ confound제거).
  - **✅ Δ-over-random 완성(수정코드: sliding-window+정확모달, trained vs random-init)**: **brainage reg(n494) trained 0.867[.847,.885] vs random 0.541[.470,.599] = Δ+0.326 CI완전분리 = 강한 진짜 신호** / infarct cls(n21) 0.817 vs 0.481 Δ+0.336(방향양수나 n21 CI겹침) / **polymicro cls(n48) 0.946 vs random 0.868 = Δ+0.078 = 대부분 site confound**(random이 이미 0.868, 기존 0.957도 동일). brainage sliding 0.867≈whole 0.858(C3 우려 brainage엔 무영향). 결과 `experiments/phase_b/downstream_feat{,_random}/`. **foundation 인사이트: 모델 진짜 가치=global morphometry(brainage 강함, [[t1-morphometry-saturation]]·[[multimodal-headroom]] 정합); cls는 n·confound로 내부평가 한계.** seg(50%)=다음.
  - **🔴 seg trigeminal yucca 재측정(중대 정정, `downstream/eval_seg.py`)**: yucca(1mm-iso+fg-centered crop128)서 **pretrained Dice 0.432[.40,.46] vs scratch 0.421[.39,.45] = Δ+0.011(CI겹침=무의차)**. **절대 Dice 9× 폭등(0.047→0.432)=proper 전처리가 trigeminal seg를 실작동시킴**, 단 **scratch도 0.421로 따라옴**. → **이전 "trigeminal 첫 positive transfer(Δ+0.047/+0.070)"는 전처리 아티팩트**(구 resize가 scratch를 0으로 망가뜨림). proper 전처리선 foundation 이득 ~0. = **장기발견 재확인: SSL은 global엔 강하나 dense seg는 well-tuned scratch 못 넘음**([[fomo-architecture-decision]]). **meningioma(flair,yucca,n23) 확인: Δ+0.005**(pre 0.317≈scratch 0.312, floor 0.322 근처) → **두 seg task 모두 foundation 이득 ~0 확정**. **함의: seg 50% 가중인데 foundation 이득 ~0 = 핵심 위협.**
  - **📊 downstream 종합(goal 엔드포인트, yucca·Δ-over-baseline)**: [frozen probe] brainage reg Δ+0.326 / infarct cls Δ+0.336 / polymicro Δ+0.078(confound) / [seg finetune] trigeminal Δ+0.011 / meningioma Δ+0.005. blog 문서 `docs/foundation_model_design.md`.
  - **🔄 정정(cls/reg FINETUNE Δ-over-scratch, `eval_finetune.py`)**: infarct(n21) pre0.904 vs scratch0.519 **Δ+0.385**(scratch실패) / **brainage(n494) pre0.945 vs scratch0.906 Δ+0.038**(frozen 0.33→붕괴) / polymicro 0.986 vs scratch1.000(confound). → **"global강·dense약"은 frozen 착시. 진짜 법칙: Foundation 가치 = scratch가 *실패하는* 저데이터·sparse-label서 큼**(infarct), scratch 학습가능한 곳(dense seg·데이터많은 reg)선 ~0(brainage도 finetune서 붕괴). seg flat=foundation결함 아니라 seg가 sample-efficient. **함의: ① seg-Δ목적 재학습 헛수고 가능성↑ ② foundation 진짜가치=few-shot cls(FOMO26 정합) ③ 절대 seg↑는 downstream레시피(증강·해상도)로.** → [[fomo26-downstream-results]].
- **[체크포인트 선정 게이트(150k 3-variant)] (2026-06-25)**: 사전학습 3종(pure=wg0/wg0.5/full=wg1) 150k DONE → 단일 ckpt 선정 위해 4-GPU 병렬 eval.
  - **글로벌축(cls/reg, 완료·결정적)**: brainage reg(n200, 유일 검정력) **wg0.5 0.668[.57,.75] >> full 0.547[.44,.64] > pure 0.498[.38,.60]**(wg0.5가 pure와 CI 거의 비겹침). polymicro AUROC(n48) wg0.5 0.957≈full 0.984 >> pure 0.793. infarct(n21) wg0.5 0.72 최고이나 CI 거대(검정력 부족). → **wg0.5 = 균형 단일 ckpt 명확 우위**(글로벌 압력 0=pure는 global 약함, 1=full은 과압). 결과 `experiments/phase_b/s3d_ckptsel/{pure,wg0.5,full}/eval_results.json`.
  - **seg축 meningioma(task2, n23)**: full 0.107[.05,.18] > wg0.5 0.079[.03,.14] > pure 0.076[.04,.12] (scratch 0.115, 전부 CI 동률권, Δ full−0.008). InfoNCE-global이 seg 안 깎음(pure<full).
  - **🎯 seg축 trigeminal(task4, n40) — 이 프로젝트 첫 명확한 POSITIVE TRANSFER**: **pure Dice 0.070[.053,.089] / full 0.024[.014,.034], scratch=0.000[0,0]** → **Δ pure+0.070·full+0.024 모두 CI가 0 비포함 = 유의**. 같은 eval이 pretrained엔 신호(0.07) scratch엔 0 → eval 아티팩트 아님(진짜 모델역량). = **from-scratch가 완전실패(0)하는 초소형 task를 사전학습이 가능케 함** = foundation model 가치 첫 입증(meningioma는 강한 scratch nnU-Net에 막혔으나 trigeminal은 scratch 바닥). ⚠️ 절대 Dice 0.07 낮음(crop128, 임상강도 아님)·n40 → 주장은 Δ-over-scratch(가치)지 절대품질 아님. wg0.5 trigeminal 진행 중.
  - **✅ 단일 ckpt 확정 = wg0.5** (3-variant 완전 매트릭스): trige wg0.5 0.047(+.047). 단일 dominator 없음(pure=dense편향·global최악, full=global편향·trige최약, **wg0.5=Pareto sweet spot**). 검정력 있는 brainage(.668)·infarct(.721) 승 + 두 seg 모두 positive transfer + 어느 축도 안 무너짐. seg는 n23/40 CI로 meningioma 무구별, 실 margin은 native-res finetune서. ckpt=`resenc_s3d_wg0.5/latest.pt`.
  - **전처리 스펙 코드확정(`preprocess_fomo300k.py`)**: Yucca 4-step = **crop_to_nonzero**(skull-strip 아님!) → volume_wise_znorm[0,1] → 1mm-iso+RAS → float16. No N4/skull-strip(이전 요약 정정). Yucca=Asparagus 동저자 → 공식 전처리와 동일·비교가능. **컨테이너 전처리=이 4-step 복제**(코드 보유), seg 출력만 원본공간 resample-back. 빠름(SynthStrip 불필요→120초 예산 유리).
  - **downstream 데이터 실측 inspect 완료(2026-06-25)**: 7task 구조·모달·라벨·차원 확인. 함정: Task1=cls지만 seg.nii.gz 13개 보너스, **Task4 seg=멀티클래스{0,1,2}**, 영상 극이질·비등방(T1/2 FLAIR 슬라이스 5.6mm, T4 360×512×512 거대), 명명불일치(label/labels.txt·ses-01/ses_01·sub-NN/sub_NN), downstream 입력=raw강도·skull-strip 제각각→yucca로 일관처리.
  - **downstream 방향·통합 전부 확정(user, agent조사 검증)**: ① **custom 파이프라인**(우리 ResEnc-L 그대로, Asparagus 미채택) — Asparagus는 구조불일치(5≠6스테이지)+torch2.6핀/B200충돌로 드롭인 불가, **참조용만**(seg recipe 160³/sliding-window/NSD, predict 스크립트). → [[fomo26-downstream-decision]] ② **제출=Apptainer 컨테이너, 베이스 `pytorch/pytorch:2.11.0-cuda12.6-cudnn9-runtime`**(전7task 공식·드라이버≥560.28 사실상확정; 12.8은 ≥570 미확인 리스크 → 12.6 채택). 우리 모델 ops 전부 torch2.11 표준=포팅가능 검증. **B200(sm_100)서 cuda12.6 컨테이너 GPU테스트 불가 → 로직 B200/torch2.12 검증+동일코드 패키징+validator --no-gpu**. 추론코드 torch2.11-clean 작성. → [[fomo26-submission-container]] ③ 다음=wg0.5 trigeminal 확인→단일ckpt 확정→Synapse I/O규약 확보→seg(sliding-window/NSD)·cls·reg finetune+추론 컨테이너 빌드(하네스+code-review).
- **[S3D mid-training(step40k) 추이 — negative-transfer 해결 견고, 단 scratch와 동률(역전 아직)] (2026-06-24)**: meningioma seg-transfer 추이 skip-free Δ−0.046 → S3D step10k Δ−0.009 → **step40k Δ−0.006**(pretrained 0.082 vs scratch 0.088, CI 전면겹침). = S3D가 negative-transfer는 확실히 제거(pretrained Dice 0.016→0.082, 5×)하나 *clear positive 이득은 n=23+강한 scratch nnU-Net 한계로 내부 미입증*. 문헌 S3D +2 DSC는 native-res·실프로토콜서. 추이 안정→mid-eval 반복 불필요, **full 150k 후 최종 seg-transfer 1회**로 마무리. 결론=목표구조 건전(global 검증·seg 동률화), 실 margin은 챌린지 full-res finetune서.
- **[S3D 작동 — seg negative-transfer 해결(early signal)] (2026-06-24)**: s3d_pure(step~10k) seg-transfer(crop128/40ep) = **pretrained Dice 0.016(skip-free)→0.078(S3D, 5×↑)**, scratch 0.088, **Δ −0.046→−0.009**(CI 전면겹침=동률, 더는 scratch 유의우위 아님). = 자가구현 submanifold masked-conv가 skip-MAE feature를 seg-전이 우수하게 만듦 → **이 세션 진단(skip-free MAE=seg negative-transfer 원인) 정확 + 정공법(누수0 skip-MAE) 통함**. step10k 미성숙이라 full 150k면 scratch 넘을 가능성(문헌 S3D +2 DSC). n=23 한계 여전. **→ 목표구조 ResEnc+S3D-dense+InfoNCE-global 양축 검증 단계 진입**(global=InfoNCE reg 0.63✅ / seg=S3D가 negative-transfer 제거✅). full 150k 완료+seg-transfer 재검증이 최종.
- **[S3D seg-fix 착수: spconv 불가→자체 submanifold masked-conv 구축·검증] (2026-06-24)**: seg negative-transfer 원인=skip-free MAE → S3D식 skip-MAE 필요. **spconv는 B200/cu130서 크래시**(SIGFPE, cu120 prebuilt가 sm_100 미지원; 소스빌드 고위험) → 의존성 게이트가 doomed 빌드 차단. **수단 변경: 순수 PyTorch submanifold 근사**(`ResEncUNet.forward_masked`): 입력 마스킹+각 stage 후 masked re-zero(+mask max-pool)=sparse conv를 dense로 근사. **검증: masked 입력 +100 변경 시 recon 차이 0.000e+00(누수 완벽 0) vs 일반 forward 11.99** = skip 켜고도 누수0(S3D 핵심속성 달성). 커밋 ca71a5c. **남은 통합(다음)**: ① recipe `resenc_s3d`(ResEnc use_skip=True) + SSLModel이 forward_masked로 recon ② train.py MAE 경로가 block_mask→vis로 forward_masked 호출, L_d=masked-voxel MSE ③ 전체 226K 사전학습(InfoNCE-global 병행) ④ seg-transfer eval(전체 skip-U-Net 전이 vs scratch, crop128/150ep)로 negative-transfer 해소 확인. .venv-train 청정(spconv 제거, torch 무결).
- **[목표구조 확정: ResEnc+MAE+InfoNCE — 본학습 ckpt eval 종합] (2026-06-24)**: Phase B 본학습(전체 226K, 150k step) + 비교/balance + 검증 eval 종합 결론.
  - **global 축(cls/reg) ✅검증**: InfoNCE global이 brainage **reg 0.02(no-global=pure-MAE)→0.63(Δ+0.43 유의)**. **InfoNCE-global 추가가 seg를 안 깎음**(pureMAE seg 0.010 == infonce seg 0.010) = *양축 공존* 확인. w_global sweet spot 0.5(cls 0.977)~1.0(reg 0.63); wg2 과weight로 저하.
  - **vs ViT-iBOT(3DINO식 dim768)**: ViT-iBOT reg 0.484(Δ+0.43)로 global 작동하나 **ResEnc+InfoNCE 0.632가 우위**(CI 겹침). ViT는 seg-encoder 기여 0(frozen). → ResEnc 채택(global 동급+, seg 우위, 문헌 SOTA).
  - **seg 축 = 현 사전학습이 negative transfer(유의 확정)**: meningioma 150ep·crop128서 pretrained 0.016 vs scratch 0.081, **Δ−0.065 CI 비겹침**(pre[.003,.032]<scratch[.036,.139]) → scratch 유의 우위. 150ep서도 pretrained 0.016 고정·scratch 계속↑ = **finetune길이·해상도 둘 다 기각**, 사전학습 *방식*(skip-free MAE)이 dense-판별 나쁜 sticky feature 생성 = seg 적응 저항. S3D(+2 DSC) 비결=**sparse-conv MAE(skip 켜고 누수없이)**를 우리가 안 함. → **seg엔 sparse-conv S3D 재설계 필수**(큰 빌드). [이하 구 기록]
  - **(구) seg 측정한계 추정**: meningioma서 pretrained가 scratch를 **4 프로토콜 전부 못 이김**(frozen / W3 fresh-decoder / decoder-transfer crop96 0.010 vs 0.024 / **crop128 0.016 vs 0.061, Δ−0.046**). crop128서 *해상도가 한계 일부였음 확인*(scratch 0.024→0.061↑, 96³ resize가 신호 깎음) — 그러나 pretrained는 여전히 ≤scratch(격차 더 큼). = "잘튜닝 from-scratch nnU-Net 깨기 어렵다" 문헌현상. **남은 confound(단정 보류)**: n=23 CI 겹침 / finetune 40ep(S3D·OpenMind 150) / **우리 MAE는 skip-free**(S3D sparse-conv는 skip 켜고 사전학습→seg-전이 우수). **→ seg 이득 실현하려면 S3D식 dense 재설계 필수: sparse-conv MAE + 150ep finetune + full-res.** trigeminal 전부 0(초소형, 128³도 불가). backbone은 ResEnc 유지(문헌 SOTA+frozen Δrand+0.022).
  - **결론**: 목표구조 = **ResEnc + MAE-dense + InfoNCE-global (w_global 0.5~1.0)**. 단일 ckpt가 global(검증)+dense(seg backbone) 양축. firsthood는 3DINO(npj2025) 선점→thesis 재프레이밍(계층백본 global+dense InfoNCE recipe + brain MRI Pareto 정량화 + 진짜 reg). 남은 일: 챌린지 full-res seg finetune(decoder-transfer)·sparse-conv S3D·고해상 trigeminal.
- **[돌파구: InfoNCE가 ResEnc global을 살린다 — ResEnc+MAE+InfoNCE = 단일ckpt 양축 동시 학습 후보구조] (2026-06-23, 4-GPU 병렬탐색)**: ResEnc global(DINO self-distill)이 **DINO·sinkhorn·high-koleo·w_global5배·격리(w_dense=0) 5연속 사망**(L_g=ln1024 고정, t_ent=1.0, student uniform) → EMA-teacher self-distill이 CNN-SimPool서 부트스트랩 실패(teacher 정규화로 못 고침=student-side 문제). **negative 있는 대조(InfoNCE/MoCo식, `--global_mode infonce`)로 전환하니 살아남**: L_g 2.75→1.27↓, t_ent 0.82→0.70↓(teacher 판별적), **L_d(dense)와 동시 학습**(L_d 0.32). 격리(global-only)서도 L_g→1.05 → InfoNCE는 ResEnc서 부트스트랩됨(negative가 uniform collapse 원천차단). **→ 목표구조 후보 = ResEnc + MAE-dense + InfoNCE-global**(최고 seg[문헌+우리] + 작동 global[cls/reg], 단일 ckpt Pareto). w_global 1이 균형(rankme 11 유지; w3은 rankme↓). 본학습 `experiments/phase_b/resenc_infonce_full`(전체 226K) 가동. 비교 arm vit_ibot_full(3DINO식 양축 baseline)·balance 스윕(wg0.5/2.0) 병렬. ⚠️ 미검증: t_ent 하락이 *실제 cls/reg downstream 이득*으로 가나(ckpt 성숙 후 eval). 교훈 = CNN/계층 백본 global은 DINO self-distill(붕괴) 말고 **대조(InfoNCE/VoCo)**가 답 — literature-scout가 짚은 "계층 백본 global+dense 3D의료 선례 공백"을 InfoNCE로 메움(잠정).
- **[W3 full-finetune — SSL 사전학습이 seg서 from-scratch를 못 이김(이득 미입증/음수); 내부 seg eval 검정력 부족] (2026-06-23, ft-리뷰 PASS)**: meningioma full-finetune(encoder+decoder, pretrained vs random-init=scratch, n=23). Δ(pre−scratch) = vit_mae **−0.025** / vit8 **+0.011**(CI [.000,.188] 0포함=노이즈) / resenc **−0.051**. **scratch resenc 0.087이 전체 최고**(17 subject로 처음부터 학습한 U-Net이 모든 사전학습 arm 이김). → **이 스케일서 SSL이 seg에 측정가능 이득 0/음수**. "ViT-seg 0"이 frozen 아티팩트인지 의문 해소 = **아니오, full-FT서도 pretrained≤scratch**(frozen+finetune 두 방법 일치 → 신호 실재 가능성). ⚠️ 단 n=23 CI 거대(전부 ~0 걸침)+사전학습 약함(스크리닝)+96³ → "이득 0"인지 "n=23로 검출 불가"인지 단정 못 함; **공통 = seg 이득 입증 불가·신호 음수방향**. **결론**: 현재 SSL은 reg엔 이득(frozen Δ+0.2)·**seg(50%)엔 미입증** = global-유용/dense-무용 trade-off 극명. **내부 seg eval은 n=23로 검정력 부족 → seg recipe 선택을 내부지표로 신뢰 불가**(원리·문헌·챌린지 3회 검증 의존). 함의: seg-dense 재설계 필요(Swin 계층/dense SSL 강화) 또는 스케일(Phase B) 검증. 상세 SUMMARY.md. 교훈 = foundation model 가치(사전학습→downstream 이득)를 50% 축서 못 보이면 thesis 위협 — Phase B 전 반드시 해결할 핵심 리스크.
- **[seg-finetune 실측 — ViT encoder는 seg 기여 0(decoder/raw-skip 아티팩트), resenc encoder만 seg 기여; global↔dense trade-off 실증] (2026-06-23, 3-리뷰 하네스)**: few-shot finetune(frozen encoder+fresh skip-decoder, 진짜 Dice, Δrand=trained−random_encoder). **meningioma**: ViT random_enc(0.056~0.065) ≥ trained(0.036~0.062) → **Δrand ≤ 0 = ViT pretrained 토큰이 seg에 기여 0**(conv-stem raw-input skip이 다 함, 리뷰어② C1 실측 확정). **resenc만 random_enc 낮고(0.012, ResEnc random skip 무정보)→trained 0.034, Δrand+0.022(최대)·CI 최협** = **encoder가 실제 seg에 기여(CNN-seg 첫 긍정 증거)**. trigeminal 전 arm 측정불가(skip-decoder로도; ViT 0.011도 skip-주도). **thesis 핵심 발견**: ViT=global(reg vit8 0.640 최고)·dense(seg) 0 / resenc=dense(seg) 양수·global 사망 → **단일 encoder가 global+dense 동시 Pareto-good인 arm이 이 스케일서 없음** = II+III thesis가 묻는 task-type trade-off 실증(conflict-pilot 깊이분업과 일치). ⚠️ 절대 Dice 작고(0.03~0.06, frozen+few-shot+96³) ViT/ResEnc decoder 비대칭(C2)이라 절대 비교 불가; Δrand+random_enc gate가 정직한 신호. **미검증(Phase B 전 필수, W3)**: "ViT seg 0"이 *frozen* 한정인가(full-finetune서 encoder 적응하면 달라질 수도) → 상위 arm full-finetune로 frozen↔finetune 랭킹 상관 확인 필요. 교훈 = finetuned-decoder seg probe는 random_encoder 대조 *필수*(decoder가 raw-skip으로 saturation; random_enc 없으면 ViT seg를 과대평가).
- **[resenc는 "broken"이 아니다 — dense는 정상, global(DINO)만 사망; seg는 기존 ckpt로 검증가능] (2026-06-23, 성분분해)**: 손실 성분 분해(L_dense vs L_global) 결과 = resenc **L_dense(recon) 1.30→0.194로 정상 학습**(vit8 1.1→0.2와 동급), **L_global(DINO)만 6.9315=ln(1024) uniform 고정으로 사망**. 평평한 total은 죽은 global이 가린 것. 근인 = `_MLPHead`(plain MLP, L2-norm 없음)가 저-magnitude global vec서 평평 logit 생성 — 실측: resenc **proj_logit.std=0.0005** vs vit8 0.195(1/390), → teacher uniform → DINO gradient 0 → 부트스트랩 실패. 표준 DINO head(L2-norm bottleneck+weight-norm)는 입력불변 logit으로 *필요*하나 *불충분*(t_ent 다시 상승; 잔여 bootstrap 문제). **함의(우선순위 재조정)**: ① global 사망은 *cls/reg(무정보)만* 영향, **seg(dense)와 무관** → resenc global 깊은 디버그 *연기*(저가치) ② seg selection은 finetune 하네스(dense)라 **resenc 기존 ckpt로 CNN-seg 지금 검증 가능** → "CNN seg 약함"은 아직 미판정(frozen 선형+bottleneck probe의 0.001은 probe 한계). head fix는 ckpt 호환 깨져 일단 revert(근인은 models.py 주석+여기 보존). 교훈 = total loss만 보면 오진(성분 분해 필수); DINO head L2-norm은 저-magnitude pooling(SimPool)서 필수.
- **[Phase A 4-arm bake-off 1차 결과 — recipe 선택엔 부족, eval 보완 필요] (2026-06-23, research-critic 검증)**: OAT(ViT-S·4000subset·8000step·crop96) 4 arm(vit_mae/vit_ibot/vit8_mae/resenc_mae) eval v2(+cls/reg CI). **결과**: cls 2개 모두 *무정보*(infarct n21 전 arm CI가 chance 포함; polymicro random ViT 0.97=site confound 지배). **reg(유일 검정력)**: vit8 0.640≈vit_ibot 0.566>vit_mae 0.498>resenc 0.368, vit8>resenc 유의. **meningioma seg**: ViT 3종 Δfloor+0.04~0.05(CI>0 실질, 상호 무구별), resenc≈0. **trigeminal 전 arm 0**(crop128도). **결론(critic 수용)**: ① vit8_mae는 *reg-주도 선두*일 뿐 검증된 3-type Pareto 승자 아님 ② **resenc는 broken run**(t_ent 1.0 global 미engage + MAE loss 8.2→7.13 거의 정지; ViT는 같은 DINO로 정상) → "CNN seg 진다"는 결론 *무효*(미검증). 게다가 seg probe가 ResEnc coarse bottleneck만 씀(skip 미추출) ③ 어떤 arm도 Phase B 커밋 불가. **교훈**: 내 예비결론(vit8 승자)이 과도 — 같은 artifact 논리를 내 headline엔 안 적용(F1), cls/reg CI 미구현이 사전등록(F3) 위반이었음 → 비판검증 필수성 재확인. 상세 `experiments/phase_a/eval_bakeoff/SUMMARY.md`. **다음**: resenc 학습실패 진단·재학습 + ResEnc skip-feature 추출 + 고해상/nonlinear seg probe + probe↔finetune 상관 → 그 후 recipe 선택.
- **[eval v2 cls/reg bootstrap CI — 사전등록 위반 버그 수정] (2026-06-23, critic F2)**: 인사이트 = seg엔 bootstrap CI 넣었으나 cls/reg는 점추정만(`ci:null`) → [[eval-probe-confounds]] F3(작은-n 점추정 금지) 위반, 2/3 task type을 노이즈로 랭킹. 변경 = `linear_probe(return_preds=True)`로 OOF preds 반환 + eval `_boot_ci`(subject resample 1000×, AUROC는 단일클래스 제외). 효과 = infarct n21 CI [.13,.77]=chance 포함이 드러나 "vit8 cls 승리" 허상 노출. 교훈 = 측정도구 사전등록 기준을 *전 task type*에 일관 적용.
- **[monitor collapse STOP false-positive — emb_std_min(단일스텝 min)은 노이즈, mean으로 STOP] (2026-06-23, bake-off 중 적발, 리뷰 PASS)**: 인사이트 = resenc_mae(SimPool)가 step83에 `emb_std_min=0.0099<0.01` collapse STOP(exit3)으로 죽음. metrics.jsonl 궤적 = emb_std_min이 0.0099~0.0151 좁은 밴드 *진동*(노이즈), emb_std_**mean** 0.028·rankme 13.8 건강 → **진짜 collapse 아님**(차원 최솟값은 SimPool서 노이즈 큰 단일-스텝 통계). 실증: resenc서 emb_std_min<0.01 dip **17회**(구 규칙=17 false STOP), emb_std_mean은 0.010~0.019 밴드 발생 0회. **이게 critical**: 그대로면 *CNN arm(=해상도상 seg 승자 후보)을 잘못 탈락* → bake-off 결론 오염. 변경 = `emb_std_min` STOP→**WARN**(단일차원 조기경보 유지) + 견고 집계 `emb_std_mean<0.01`을 STOP. 진짜 collapse 3모드(full→mean, rank→rankme_drop>30%, single-dim→WARN) 커버 유지(약화 없음). test_monitor 회귀 3종 추가(진짜 collapse STOP / noise dip 통과 / 임계 0.01 양방향 경계 잠금). code-auditor 리뷰 PASS(emb_std_mean은 비정규화 입력이라 건강 recipe 3~36× 마진, 오발 없음). 교훈 = monitor 임계는 *지속성/집계* 기반이어야(단일-스텝 min+hard threshold=노이즈 오발).
- **[patch16 ViT는 작은 구조 seg에 해상도 부족 — bake-off 설계 인사이트] (2026-06-23, eval v2 실측)**: 인사이트 = R0(vit_mae patch16) trigeminal Dice=0.000(random도 0.000). patch16 → dense grid 6³ → 1토큰=16³voxel인데 삼차신경 ~1-2voxel → frozen encoder가 선형 probe로 *원리적으로* 분리 불가. meningioma(더 큼)는 Δ+0.053으로 신호 잡힘. 함의 = **seg-50% 리더보드(특히 task4 trigeminal)는 fine spatial resolution 필수** → Phase A bake-off서 **patch8(grid12³)·ResEnc(CNN, finer bottleneck)가 vit16 대비 seg 우위 예상**. 코드 변경 아직 없음(가설) — bake-off로 검증 예정. 단 96³ crop 자체도 trigeminal엔 coarse(고해상 eval 필요할 수 있음).
- **[eval v2 — 표현력 decoder는 함정, 선형 probe로 재설계 ✅검증완료] (2026-06-23, 3-리뷰+재검증 7/7 PASS)**: 인사이트(리뷰어②, 가장 중요) = 1차 구현 `_SegProbeDec`(3×3 conv ×4단)는 **표현력이 커서 random encoder로도 grid좌표+conv bias만으로 "병변이 여기 있다"는 공간 prior를 학습** → voxel-AUROC와 *똑같이* 재포화(shortcut을 metric→decoder로 옮겼을 뿐). 변경 = ① decoder를 **1×1 conv only 선형 readout + 파라미터 없는 trilinear upsample**(`_SegLinProbe`, DINOv2/SAM 표준)로 교체 — 공간 커널이 없어 학습된 위치 blob 못 그림 → Dice가 encoder per-voxel 분리력에서만. ② **constant-prior floor**(encoder 없이 train평균마스크>0.5) 동시 보고 → decision = `Dice − floor`. ③ empty-GT val skip+n_empty 로깅. ④ per-sample soft-Dice. ⑤ decoder init seed×3 평균. ⑥ Δ-over-random metric 가드. ⑦ minibatch(OOM). 구 voxel_probe DEPRECATED. **✅실데이터 검증(R0 vit_mae step8000)**: random encoder seg Dice = **0.000**(task2·4 모두; 구 voxel-AUROC는 random 0.84~0.99) → **위치 shortcut 완전 제거, metric 타당**. R0는 meningioma Δfloor+0.053 CI[.011,.109](0과 유의 구별)·brainage Δ+0.223·cls는 random 이하(MAE global 약점+site confound). ⚠️ crop³ resize=full-res 근사(docs/06 기록). W1(n=23/40)→CI 겹치면 recipe 랭킹 보류.
- **[eval v2 real-Dice 최초 동기] (2026-06-23)**: 인사이트 = R0 실측서 seg voxel-AUROC proxy가 위치 shortcut에 *포화*(task4 random 0.986 천장) → encoder가 학습돼도(reg Δ+0.196) **seg(50%)를 판정 못 함**. 이유 = 측정 못 하면 recipe 선택·scale 불가, 검증 3회뿐이라 내부 eval이 리더보드(Dice) 반영해야. → 위 항목으로 진화.
- **[MAE skip 누수→skip-free] (2026-06-23)**: 인사이트 = conv-stem/U-Net skip이 마스크 영역을 인접 clean으로 trivial 복원 → encoder가 dense 안 배워도 loss↓(SSL 무효). 변경 = MAE 사전학습 `use_skip=False`(skip은 seg 전이용만). 이유 = MAE non-trivial 조건(masked 영역은 토큰/bottleneck으로만 복원).
- **[collapse STOP init 유예] (2026-06-23)**: 인사이트 = SimPool 풀링 출력이 *init*서 저분산 → step0 false collapse STOP. 변경 = emb_std_min alert를 baseline_warmup 후에만. 이유 = collapse는 학습 현상이지 init 상태 아님.
- **[EMA 범위 decoder 제외] (2026-06-23)**: 인사이트 = MAE decoder를 EMA하면 teacher에 무의미 weight·ckpt 오염. 변경 = ema_param_names가 decoder 제외(ResEnc는 encoder만) + build_models fail-fast assert. 이유 = teacher=encoder+global 표현용, recon은 student-only.
- **[DataLoader 226K stat 행→subset만] (2026-06-23)**: 인사이트 = build_filelist가 manifest 전행 gpfs stat → 분 단위 행. 변경 = status=ok 신뢰, stat은 subset에만. 이유 = gpfs stat=네트워크 왕복.
- **[data_fraction 의미] (2026-06-23)**: 인사이트 = prefetch 후 data_fraction은 "디스크부하"가 아니라 "GPU 굶김 잔여"(cliff 거동). 변경 = 주석 명확화 + stall_ratio를 I/O 1차 경보로. 이유 = 오독 방지.
- **[torch2.12 weights_only / cuda RNG device] (2026-06-22)**: 인사이트 = resume이 weights_only=True·RNG ByteTensor cuda이동으로 실패(실험이 적발). 변경 = weights_only=False + RNG `.cpu()`. 이유 = 자체 신뢰 ckpt·RNG는 CPU여야.

## 핸드오프 노트
- git: AD 작업은 태그 `exploratory-v1/rtssl-v1/experiments-v1/fomo-planning-v1` 보존. 현 tree = FOMO only.
- 실행: 학습/GPU는 `.venv-train/bin/python` (전처리만 `.venv`). bf16 필수.
- 위험/모니터는 [[docs/06_risk_register]](W1~15 + monitor.py).
