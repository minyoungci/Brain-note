# 일일 종합 — 2026-06-10 (KST)

> LLM 자동 종합(gen→critic→synth). **검토용 초안이며 미검증 주장이 있을 수 있음.**
> 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` synth=`claude-opus-4-8` · 활동 WS 1개

You've hit your session limit · resets 7pm (Asia/Seoul)

---
<details><summary>워크스페이스별 검증 분석 (펼치기)</summary>

# === minyoungi ===
## 진행 요약

핵심 성과 세 가지: ① manifest 127→138컬럼 완성, ② AJU/ADNI/NACC용 DICOM→NIfTI 변환 모듈 신설(`preprocessing/dicom_to_nifti/`), ③ raw_*_path 5개 컬럼 부착 및 현재 도달 가능한 11,947경로 전수검증. DICOM 변환 배치 미실행으로 raw_t1_path 39.4%, raw_flair_path 25.9%에 머무는 것도 정확하다.

**[수정]** 초안이 "오늘 핵심 성과" 항목에 ADNI APOE 94%(커밋 `d524987`, 06-07)와 A4/NACC amyloid PET(커밋 `3ce30c0`, 06-06)을 나열하나, 이는 06-10 당일 작업이 아니라 캐치업 기간의 커밋이다. 오늘(06-10) 실제 manifest 변경은 **127→133**(OASIS 임상 보강, `4a43330`) + **133→138**(raw_*_path 부착, `8c121b8`) 두 단계이며, 초안의 "127→138" 단일 묶음은 오늘의 기여만으로 서술하는 오해를 유발한다. 표현을 "오늘 작업: 127→138 / 누적 구성: ADNI APOE·A4 amyloid는 캐치업 기간 커밋"으로 분리해야 정확하다.

---

## 코드 리뷰

### [버그/데이터오염] aju.py — DTI·DWI 중복 덮어쓰기
`preprocessing/dicom_to_nifti/aju.py:30–34`

```python
MODALITY_MAP = {
    "DTI": "dwi",
    "DWI": "dwi",
}
```
같은 세션에 `MRI/DTI/`와 `MRI/DWI/`가 공존하면 `iter_dicom_sessions`가 동일 출력 경로(`output_path(…, "dwi")`)를 두 번 yield하고, 두 번째 `shutil.move`가 첫 번째 결과를 조용히 덮어쓴다. `sorted()` 순서에 따라 선택이 결정되며 로그가 남지 않는다. first-wins 가드 또는 명시적 경고 로그 필요. **diff 확인.**

---

### [버그/데이터오염] aju.py — PET 이중 yield (신규)
`preprocessing/dicom_to_nifti/aju.py:28–34, 65–76`

```python
MODALITY_MAP = {
    ...
    "PET": "pet",        # MRI/ 하위 PET/ dir 처리
}
...
pet_dir = visit_dir / "PET"
if pet_dir.is_dir() and any(pet_dir.iterdir()):
    yield site, subj, visit, "pet", pet_dir   # 최상위 PET/ dir 별도 처리
```
일부 AJU 사이트가 `MRI/PET/`와 `PET/` 두 위치에 모두 파일을 두면 `"pet"` 모달리티가 두 번 yield된다. 두 번째 `shutil.move`가 첫 번째 산출물을 덮어쓰며 어떤 쪽이 최종 파일인지 추적이 불가하다. 파일 헤더의 입력 레이아웃 주석(`MRI/{modality_dir}/` vs `PET/`)이 상호 배타적임을 가정하지만, MODALITY_MAP에 `"PET"`가 남아 있는 한 구조적으로 이중 yield가 발생 가능하다. DTI/DWI 중복과 동일한 수정 방향. **diff 확인.**

---

### [버그] nacc.py — dcm2niix 입력·출력 디렉토리 동일
`preprocessing/dicom_to_nifti/nacc.py:86–90`

```python
cmd = [DCM2NIIX, "-z", "y", "-f", "%p_%s",
       "-o", str(tmp_path), str(tmp_path)]   # 입력 == 출력
```
zip 해제 결과와 출력이 동일한 `tmp_path`를 가리킨다. dcm2niix 버전에 따라 이미 생성된 `.nii.gz`를 DICOM으로 오인하거나 파일명 충돌이 발생할 수 있다. 해제 경로(`tmp_path / "dcm"`)와 출력 경로(`tmp_path / "out"`)를 분리하는 것이 안전하다. **diff 확인.**

---

### [버그] adni.py·aju.py — ProcessPoolExecutor 예외 비전파
`preprocessing/dicom_to_nifti/adni.py:84–93`, `aju.py:111–122`

```python
result = fut.result()   # 미처리 예외(OSError, PermissionError 등) 재발생 → 루프 전체 중단
```
`convert_session` 내에서 처리되지 않은 예외가 발생하면 `fut.result()`가 이를 재발생시켜 대량 배치 전체가 중단된다. `try/except Exception as e`로 감싸 `stats["failed"]`에 집계하고 로그를 남겨야 한다. **diff 확인.**

---

### [수치불안정/재현성] adni.py·aju.py — 파일 크기 기준 NIfTI 선택
`preprocessing/dicom_to_nifti/adni.py:69`, `aju.py:94`

```python
best = max(niftis, key=lambda p: p.stat().st_size)
```
dcm2niix가 여러 NIfTI를 생성할 때(위상 인코딩 방향별 분리, 보조 시리즈 등) 파일 크기만으로는 완전한 3D T1w 볼륨이 선택된다고 보장할 수 없다. ADNI는 `load_dicom_list`에서 `num_dicom_files` 기준으로 1차 필터링하나, 변환 후 선택에도 시리즈 디스크립션 기반 필터(예: 파일명에 "MPRAGE"/"T1" 포함 여부)를 추가하는 것이 권장된다. **diff 확인.**

---

### [데이터누수 위험] enrich_oasis_data_files.py — ±365d nearest match 방향 미확인
`roi_qc/scripts/enrich_oasis_data_files.py` (diff 패치 30000B 한도로 내용 미수록)

연구 노트에서 "MRI 날짜 기준 ±365d nearest match"로 ANIMALS·CDR-SB를 조인한다고 명시했다. 세션 이후 최대 365일까지의 인지 검사 결과가 해당 MRI의 레이블로 붙을 경우, 이를 분류 타깃으로 사용하면 미래 정보 누수가 된다. **단, enrich_oasis_data_files.py 실제 코드가 diff에 없어 join 방향(단방향/양방향) 확인 불가.** [근거부족—코드 수준] 노트 기술 자체는 지지되므로 [불확실] 수준은 유지. 코드 확인 후 재평가 필요.

---

### [재현성] Korean manifest 빌드 스크립트 미추적
커밋 `33d47e2`: "Korean official manifest를 preprocessed_official/에 배치"

diff --stat 23개 파일 목록 어디에도 Korean manifest 생성 스크립트가 없다. INDEX.md에 "AJU 1,287 + KDRC 909 · 1세션1행 완전체(2,196 × 89)"로 명시되어 있으나 어떤 필터·조인으로 생성됐는지 추적 불가하다. 파일만 배치된 상태이며 재현 불가. **diff 확인.**

---

### [누락] NACC ses-1 274행 미해결
`preprocessing/dicom_to_nifti/README.md:54`

> `ses-1` sessions (274 rows) need a separate lookup; currently unresolved

해결책 없이 TODO로 잔존한다. 이 274행이 raw_manifest 커버리지 수치(raw_t1_path 39.4%) 분모에 포함되는지, 아니면 조용히 제외되는지 명확하지 않아 실제 커버리지가 더 낮을 수 있다. **diff 확인.**

---

## 놓친 것·결정 필요

| 항목 | 근거 | 위험도 |
|------|------|--------|
| **DICOM 변환 배치 미실행** — AJU/ADNI/NACC 변환 완료 전까지 raw_*_path 커버리지는 현재 값(39.4/25.9/6.3/13.2/6.9%)에 고정. 다음 전처리 단계 전체 블로킹 | 노트 §5, CLAUDE.md 사전승인 규칙 | **높음** |
| **Korean manifest 빌드 스크립트 없음** — 33d47e2에 결과 파일만 배치, 생성 코드 미커밋. canonical manifest로 선언했으나 재현 불가 | diff stat vs 커밋 메시지 불일치 | **중간** |
| **PET 트레이서 혼재 미확인 [VERIFY]** — AJU/KDRC 트레이서 확인 전 Centiloid 변환 보류 선언했으나 확인 작업 없음. 잘못된 SUVR이 manifest에 기재될 수 있음 | `preprocessing/README.md` §Known Risks 2 (diff 확인) | **중간** |
| **FastSurfer seg 없으면 PET SUVR BLOCKED** — 소뇌 reference region 확보 전략(어느 세션에 seg 있는지 인벤토리) 없음 | `preprocessing/README.md` §Known Risks 3 (diff 확인) | **중간** |
| **연구 착수 결정 미진행** — research_topic/ 에서 "shortcut-audit 즉시 가능" 결론 후 후속 진행 없음 | 노트 §캐치업 06-07 | **낮음** |

---

## 미해결 질문

1. **DICOM 변환 배치 언제 실행 승인?** 단일 세션 smoke test(각 컨소시엄 1개)를 먼저 실행한 뒤 배치 규모를 결정하는 것이 안전하다. 현재 smoke test 결과가 있는가?

2. **NACC ses-1 274행 처리 방법?** 별도 lookup 구조가 없는 상태다. 이 행들이 raw_manifest 커버리지 분모에 포함되어 있는가, 아니면 조용히 `NaN`으로 채워지고 있는가?

3. **Korean manifest (2,196 × 89) 빌드 스크립트 어디에 있는가?** 재현 가능한 코드 없이는 downstream에서 신뢰하기 어렵다. 스크립트를 커밋하거나 최소한 어떤 필터를 적용했는지 README에 기록해야 한다.

4. **OASIS ±365d join 방향이 단방향인가 양방향인가?** 분류 타깃으로 쓸 컬럼(ANIMALS, CDR-SB 등)에 미래 레이블이 들어오면 데이터 누수다. `enrich_oasis_data_files.py` 코드에서 `merge_asof` 또는 `direction` 파라미터를 직접 확인해야 한다.

5. **aju.py의 DTI/DWI 및 MRI/PET·최상위 PET 중복 세션이 실제로 존재하는가?** AJU raw 디렉토리 구조를 `find /home/vlm/data/raw/AJU -type d -name "DTI" -o -name "DWI" | head -20` 및 `find … -path "*/MRI/PET*" | head -10`으로 확인한 적 있는가?


</details>
