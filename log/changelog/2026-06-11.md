# 변경 노트 — 2026-06-11 (KST)

> 허브(구조·도구·문서·지식·설정)의 의미있는 변경 기록. 자동 생성 콘텐츠(log·notes·OVERVIEW)는 제외.
> LLM 생성→검증 초안. 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` · 구간 `2c81983f..0b4d0341`.

## 한 줄 요약

5개 연구 워크스페이스의 데이터·태스크를 한 표로 정리한 레퍼런스 문서(`learn/workspaces_data_task.md`)가 새로 추가됐으며, `plant` 디렉토리가 `microbrain`으로 재정의된 사실이 문서로 공식 기록됐다.

---

## 무엇이 바뀌었나

### `learn/workspaces_data_task.md` (신규)
- **공유 데이터 토대 표**: canonical parquet(`official_manifest_full_n4_real_final.parquet`, 13,022세션/7코호트/122컬럼, `.datadict.csv` 동봉)·입력 텐서 규격(192×224×192, 1mm RAS, z-score, brain-masked)·7코호트 N 분포·공통 타깃(`cdr_global`)·공유 함정(roi 미서명, site 누수)을 한 자리에 정리.
- **디렉토리별 매트릭스**: `minyoung2` `minyoung4` `minyoung3` `plant→microbrain` `minyoungi` 5개 디렉토리 각각의 연구 질문, 사용 데이터, 방법, 현재 상태를 한 행씩 기술.
- **횡단 연결 다이어그램**: minyoungi → minyoung2 → (minyoung4 / minyoung3 / microbrain) 트리 구조와 공유 manifest 관계를 ASCII로 표현.
- 기준일 명시(2026-06-11), `DASHBOARD.md`의 04_plant 행이 stale임을 경고 주석 포함.

### `README.md`
- 허브 최상위 디렉토리 일람표에 `learn/workspaces_data_task.md` 항목 1행 추가 — "디렉토리별 데이터·task 매트릭스 / 수동".

---

## 왜

- `DASHBOARD.md`의 04_plant 행이 옛 longitudinal 설계를 가리키고 있어 stale 상태였고, 실제 `plant` 디렉토리는 **microbrain**(bias 내성 micro 표현 연구)으로 연구 방향이 전환됐다. 이 불일치를 해소하면서, 동시에 5개 워크스페이스를 가로질러 "어떤 데이터로 무엇을 하는가"를 한눈에 보여 줄 통합 레퍼런스가 필요했다고 추론된다.
- 커밋 메시지의 `(plant→microbrain 반영)` 구문이 이 의도를 직접 지시한다.

---

## 알아둘 점·영향

| 항목 | 내용 |
|---|---|
| **최신 참조 문서** | `DASHBOARD.md` 04_plant 행보다 `learn/workspaces_data_task.md`가 최신. 워크스페이스 현황 파악 시 이 파일을 먼저 열 것. |
| **`plant` = `microbrain`** | 디렉토리 이름은 아직 `plant`이지만 연구 정체성은 microbrain으로 전환됨. P0 착수 대기 상태(설계 lock 전)이므로 기존 longitudinal 코드·노트에 의존하면 안 됨. |
| **`minyoung4` BLOCKED** | 매트릭스에 명시된 ⚠️ 상태 — 2회 피벗 기각(scanner-family 누수·morphometry 미초과), G0 coverage 미통과. 이 연구에 기여할 예정이라면 G0 coverage 통과 조건 확인 필요. |
| **`minyoung3` git 부재** | manuscript asset 생성 및 외부 morphometry bar(0.910) 달성 상태임에도 diff 기준 git 커밋이 없는 것으로 매트릭스에 명시됨. 코드·실험 재현성 추적 불가 상태. |
| **`minyoungi` Gate05b NACC 회귀** | b1 global ROI-cos이 ADNI·KDRC는 개선하나 **NACC 회귀**. `roi_final_ready` 전수 False 상태이며 ROI 기반 downstream 실험에서 이 플래그를 검사하지 않으면 미검증 데이터가 흘러들어 갈 수 있음. |
| **`roi_final_ready` 전수 False** | 모든 ROI 데이터는 사람 sign-off 전. |
| **문서 갱신 주기** | "수동" 유지 문서이므로 실험 상태가 바뀌면 이 파일도 함께 업데이트해야 함. 자동 동기화 없음. |

---
<details><summary>원본 커밋 목록</summary>

- 58faa48 docs: 워크스페이스 데이터·task 매트릭스 추가 (plant→microbrain 반영)

</details>
