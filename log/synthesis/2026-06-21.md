# 일일 종합 — 2026-06-21 (KST)

> LLM 자동 종합(gen→critic→synth). **검토용 초안이며 미검증 주장이 있을 수 있음.**
> 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` synth=`claude-opus-4-8` · 활동 WS 2개

## 오늘 한 줄 요약

minyoungi는 Claim Safety Controller를 하루 만에 v1→v4까지 진화시켜 **over-claim 0/90으로 FROZEN + 논문 초안**까지 완료(단, "clean win 아님 — safety↔completeness tradeoff"가 thesis), minyoung2는 **FOMO300K 전처리(3.2TB npy) + B200 학습 env 분리 구축**을 마쳤으나 둘 다 코드/아티팩트가 아닌 **문서 단계에서 수치 불일치·stale 잔존**이 공통 리스크로 남았다.

## 워크스페이스별 핵심

**minyoungi (Claim Safety Controller / 논문)**
- v4 L0 hard-block으로 over-claim 0/90 달성 + human spot-check 전건 ACCEPT → controller FROZEN, 논문 초안(METHODS/RESULTS + Figure/Table plan) 작성 완료.
- 핵심 발견: harmful over-suppression **~14–15/rep이 v1→v4 내내 미감소** → "clean win 아님". 이를 논문 thesis로 채택했으나, completeness 비교에 generation-base mismatch라는 미통제 bias 존재.

**minyoung2 (FOMO300K SSL 사전학습 준비)**
- 226K~227K 볼륨을 float16 npy로 변환(3.2TB), numpy2/torch ABI 이중 충돌을 전처리/학습 env 분리로 우회 해결.
- **이번 diff는 코드 변경 0** — 5개 파일 모두 문서. 다음 단계(SSL 사전학습 코드)는 아직 미착수.

## 주목·결정 필요 (우선순위순)

1. **🔴 [minyoung2] 학습 코퍼스 볼륨 수 확정 (226,908 vs 227,443)** — DWI에서 44,943 vs 45,478(+535) 분기. 4:1 소스 비율로 227,443이 우세하나 **diff만으로는 미확정**. 학습 시작 직전이라 잘못된 수가 manifest·downstream 전체로 전파됨. **결정: manifest.csv `status=ok` row 수 ↔ 디스크 `*.npy` 파일 수를 직접 대조해 단일 권위값을 고정**해야 함. (per-WS 분석도 "교차검증 필요"로 미해결로 남김 — 이건 사람이 명령 한 번으로 끝낼 일.)

2. **🔴 [minyoungi] thesis "safety↔completeness tradeoff"의 confound** — controller는 고정 n1 propose + 3 rewrite(다양성↓), generic/checklist는 독립 3-draw(다양성↑). 이 설계 차이가 completeness 격차(1.878 vs 2.622)에 **체계적 bias**를 준다. ablation 없이 tradeoff를 thesis로 확정하면 리뷰어가 "completeness 손실이 controller 탓인지 sampling 탓인지 모른다"고 칠 수 있음. **본문에서 bias 방향·크기를 논하거나 최소 1개 ablation 필요.**

3. **🟠 [minyoungi] v4 paid eval의 L0 활성화 경로 = 재현 불가 위험** — default config가 `precision.enabled: false`인데 같은 블록에 `l0_hard_block: true`. run config(`runs/.../config.yaml`)가 patch에 없어 override 여부 미확인. **default로 재현하면 v1 baseline만 나오고 v4는 재현 안 됨.** FROZEN 결과·논문의 토대이므로 재현 절차를 코드/config 수준으로 명시해야 함.

4. **🟠 [minyoungi] 제출처(AAAI main vs workshop) + Related work [VERIFY] 미실행** — 둘 다 선언만 있고 커밋 없음. 논문 단계 진입의 게이트. (당신이 놓쳤을 가능성: outline은 있으나 "확정 커밋"이 없어 실제로는 미결.)

5. **🟠 [minyoungi] AJU/KDRC NEEDS_AUDIT (PHI 부분DOB + temporal leak)** — 감사 미실행. DPO나 추가 실험의 **선행 조건**인데 v4 FROZEN 이후 멈춤. 데이터 거버넌스 리스크.

6. **🟠 [minyoung2] SSL 사전학습 코드 + W13 overlap-check의 순환 의존** — 다음 critical path. overlap-check는 FOMO26 등록·downstream subject 목록이 있어야 작성 가능한데 그게 아직 없음. 학습 시작 전 이 순환을 끊을 순서 결정 필요.

**(메타) 두 per-WS 분석이 공통으로 약하게 다룬 점:** 양쪽 모두 "수치를 권위 아티팩트(디스크/manifest/run config)에 대조"하지 않고 문서끼리만 비교해 "교차검증 필요"로 미루었다. 이건 분석의 한계가 아니라 **사람이 한 번의 대조로 닫을 수 있는 항목**들이다.

## 코드 리뷰 종합 (반복·심각)

- **[반복·심각] 문서/아티팩트 간 수치 불일치 + 단일 source of truth 부재** — minyoung2: 볼륨 226,908(커밋·SCRATCHPAD) vs 227,443(나머지 4파일). minyoungi: evolution 테이블이 n=1(v1·v2·v3)과 n=3(v3n3·v4)을 혼재해 "버전 개선 효과"와 "샘플 수 효과"가 뒤섞임. **공통 패턴: 숫자가 여러 문서에 흩어져 있고 권위 원본이 지정돼 있지 않음.**

- **[반복] stale 상태 블록 — 두 워크스페이스 모두** — minyoungi SCRATCHPAD Status가 여전히 "Step 2.x"를 가리킴(Step 6b 완료 후 미갱신). minyoung2 SCRATCHPAD "⚠️ 미해결 ①(numpy2/torch ABI)"가 **이미 해결됐는데 삭제되지 않아** 미해결처럼 읽힘. → **프로세스 수준 결함**: 단계 완료 시 상태/스크래치패드 정리가 누락되는 공통 습관. (per-WS 분석은 각자 1건씩 잡았으나 "둘 다에서 반복된다"는 점은 종합에서만 보임.)

- **[반복] 4중 copy-paste 중복 (minyoungi)** — build_controller_v{2,3,3_n3,4_n3}(126–141행) **+ run_controller_arm_v{2,3,3_n3,4_n3}(110–112행)** 모두 공통 함수 없이 버전별 독립. 집계 로직이 버전마다 갈라지면 재현성 위험 실재(v3n3·v4n3 completeness가 1.878로 동일한 게 우연이 아님을 역으로 확인해줌).

- **[심각·논문 일관성] v4 n=3 completeness = v3 n=3 (1.878 동일)** — L0 hard-block은 **over-claim만 제거, completeness는 미개선**. evolution(1.633→1.80→1.833)이 개선 추세였던 탓에 독자가 "v4=completeness 회복"으로 오독할 위험. 논문에서 "v4 기여 = over-claim 제거에 한정"을 명시해야 함.

- **[검증 공백]** minyoungi: soft_caveat 구현(LLM/템플릿)·test CI 연동·L0↔enforce_strict 파이프라인 순서가 **소스가 patch에 없어 전부 미확인**. minyoung2: monitor.py `.venv-train` import 미검증, disk guard 100GB의 구현 위치·발동 조건 부재.

## 미해결 질문 (연속 추적 — 어제 대비 신규/해소)

> ⚠️ **어제(2026-06-20) 종합은 401 인증 실패로 내용이 비어 있어 직접 대비 불가.** 대신 standing tracker인 **OPEN_QUESTIONS.md(2026-06-07)** 기준으로 신규/관련을 표시한다.

**신규 (오늘 발생, OPEN_QUESTIONS에 없음)**
- [minyoung2] 볼륨 최종값(226,908 vs 227,443), DWI +535 차이 원인(out-of-band 7,129 삭제와의 관계), W13 overlap 스크립트 기존 존재 여부, SSL 코드 베이스(scratch vs DINO/iBOT 포크), monitor.py 의존성 설치 여부.
- [minyoungi] precision.enabled override 경로, generation-base mismatch의 completeness 기여도, v4=v3 completeness 동일의 논문 서술, judge bias(길이·스타일) 통제 충분성.

**해소 (오늘)**
- [minyoung2] numpy2/torch ABI 충돌 → env 분리로 **해결**(단, 문서상 미해결 표기 잔존 — 정리만 필요).
- [minyoungi] over-claim 안전성 → v4 0/90 + human ACCEPT로 **닫힘**(controller FROZEN).

**관련/갱신 필요 (OPEN_QUESTIONS standing 항목)**
- 🟠 **"minyoungi 역할 경계 (README는 '실험 코드 없음')"** — 오늘 controller v1→v4 전 사이클이 명백한 GPU 실험. **README 진술이 사실과 모순됨이 재확인**됨 → source of truth를 "실험 WS"로 갱신 권장(사실상 evidence로 해소 가능).
- 🔴 OPEN_QUESTIONS의 minyoung2 항목(IMG-020/022 3D CNN, F10 통계)은 **오늘의 FOMO300K SSL과 다른 프로젝트 단계** — 오늘 활동으로는 진전 없음(별개 트랙임을 명시).
- KDRC 라벨 권위 모순(🔴)은 minyoungi의 KDRC NEEDS_AUDIT와 **같은 코호트** — 아래 시너지 참조.

## 워크스페이스 간 연결·시너지

- **거버넌스 게이트 패턴의 상호 이식.** minyoungi는 "gold-leak 5건 formal 금지", "NEEDS_AUDIT 전까지 DPO 금지" 등 **누수·PHI 게이트 규율**을 운영 중. minyoung2도 W13(pretrain↔downstream subject overlap)이라는 동형(同型)의 누수 게이트를 두고 있으나 **스크립트 미작성**. minyoungi의 게이트 정책을 minyoung2 overlap-check에 그대로 적용하면 학습 전 누수 통제를 정형화할 수 있음.

- **"정직성 footnote" 규율 → minyoung2 수치 불일치에 적용.** minyoungi는 bias를 footnote로 명시하는 습관이 정착됨. minyoung2의 226,908/227,443 분기도 같은 방식으로 **권위 아티팩트 대조 후 단일값 + 차이 사유를 명시**하면 동일 결함 클래스를 닫을 수 있음. [불확실 — 두 WS가 같은 사람/팀 규율을 공유하는지는 제공 자료로 확정 불가]

- **KDRC 데이터 공유 가능성.** minyoungi의 KDRC NEEDS_AUDIT와 OPEN_QUESTIONS의 "KDRC 라벨 권위 모순"이 **동일 코호트**를 가리킴. minyoung2의 downstream(치매 분류 추정)이 KDRC를 쓴다면 라벨 권위 결정이 두 WS의 공통 선행 과제 → 한 번의 KDRC 라벨 감사로 양쪽 blocker를 동시에 풀 수 있음. [불확실 — minyoung2 downstream의 KDRC 사용 여부는 오늘 diff에 없음]

- **라이프사이클 상보성 (의도된 시너지는 아님).** minyoungi=논문 집필/FROZEN 단계, minyoung2=인프라 구축 후 코드 착수 직전. 직접적 기술 의존은 없으나, **minyoungi의 "FROZEN 후 future work 5종 미구현"이라는 정체**와 **minyoung2의 "다음 = SSL 코드"라는 미착수**가 겹쳐, 다음 작업 배분(논문 마감 vs 신규 학습 코드) 우선순위 결정이 필요해 보임.

---
<details><summary>워크스페이스별 검증 분석 (펼치기)</summary>

# === minyoungi ===
## 진행 요약

2026-06-21 하루에 걸쳐 Claim Safety Controller v1→v4 전 진화 사이클과 논문 초안 작성이 모두 완료됐다.

**컨트롤러 고도화:** 30건 human spot-check FULL LOCK(v1 baseline) → v2 HYBRID soft-rewrite precision 레이어 구현 + paid eval(PARTIAL, enforce\_strict negation 버그 발견) → v3 버그픽스 + paid eval(PARTIAL, n=1 노이즈 한계) → v3 n=3 실행(OpenAI quota 소진 블로킹 후 충전 재개) → L0 soft-rewrite 구멍(e4\_04) 노출 → v4 L0 hard-block 구현 + n=3 paid eval(**over-claim 0/90, STOP POINT**) → v4 human spot-check 전건 ACCEPT → **Controller FROZEN**.

**집필:** `docs/PAPER_DRAFT_CLAIMTRAP_AD.md`(논문 전체 골격 + 검증 숫자) → `docs/PAPER_DRAFT_METHODS_RESULTS.md`(§3–§8 제출형 산문) → `docs/FIGURE_TABLE_PLAN.md`(Fig1–5, Table1–4) + data-asset audit(read-only). [근거부족: "오전–오후"/"저녁" 시간대 구분 — diff는 날짜만 기재, 작업 순서는 Step 번호로 추정되나 실제 시각은 커밋에 없음]

**핵심 결론:** controller = ceiling-dependent routing으로 over-claim-free 달성(e2\_03 포함) + harmful over-suppression **~14–15**/rep 잔존(v1 14와 동급, v4에서도 미감소) = "persistent safety↔completeness tradeoff". **clean win 아님**, 이를 논문 thesis로 확정. [수정: draft의 "~14"는 Step 5M의 "~14–15/rep" 기재를 단순화한 것으로 범위를 명시해야 함]

---

## 코드 리뷰

### `configs/claim_safety_controller.yaml` · precision 블록(66–141행)

**[버그 위험] `precision.enabled: false`와 `l0_hard_block: true` 공존**
`precision.enabled: false`가 기본값인데 같은 블록 내 `l0_hard_block: true`가 선언됨. `claim_safety_controller.py` 소스가 diff patch에 없어 `enabled` 플래그 체크 후 `l0_hard_block`을 읽는지 확인 불가. runs/claimtrap30\_controller\_v4\_n3/config.yaml(diff --stat 15행)이 patch에 없어 v4 paid eval이 run config에서 `precision.enabled: true`를 override했는지도 확인 불가. **재현 시 default config(`enabled: false`)로 실행하면 SCRATCHPAD Step 5i "precision OFF == locked baseline(30/30 parity)" 확인에 따라 v1 baseline이 재현되며, v4 동작은 재현되지 않음.**

**[수치 불안정] `strict_enforce_hardfail`의 `uncertainty_exempt` 플래그 단 1개**
`{phrase: "improves discrimination", uncertainty_exempt: true}`만 exempt 처리됨. "adds value"·"positive increment" 등은 exempt 없음. 부정 절에 등장하는 경우를 clause-level negation이 처리하는지는 소스 미확인으로 불명확. [불확실—소스 미확인]

**[재현성] `l0_forbidden_phrases`와 `strict_enforce_hardfail` 간 순서 의존성**
diff에서 `l0_forbidden_phrases`에 "supportable"·"conditionally supportable" 등이 명시됐고 `strict_enforce_hardfail`에 별도 multi-word 구문이 나열됨. L0 guard 체크와 enforce\_strict 체크의 파이프라인 실행 순서는 Python 소스가 patch에 없어 미확인. [불확실—소스 미확인]

---

### `src/controllers/claim_safety_controller.py` · `claim_rewriter.py` (+123/+72행)

**[잠재 위험] soft\_caveat\_claim의 구현 방식 미확인**
소스가 patch에 없으므로 soft rewrite가 LLM 기반인지 템플릿 기반인지 확인 불가. [근거부족: draft의 "LLM 기반일 경우" 전제 — 소스 미확인이므로 단정 불가] 다만 v4 n=3에서 L1+ over-claim이 0/90이었음은 SCRATCHPAD Step 5M에서 확인된다.

**[재현성/논문 일관성] `run_controller_arm_v4_n3.py`의 n=3 샘플링 방식** [수정: "데이터 누수 위험" 분류 오류 — 이 항목은 데이터 유출이 아닌 생성 기반 불일치 및 논문 일관성 문제]
SCRATCHPAD Step 5M에 "propose=generic n1 고정, rewrite 3회 재샘플"로 기재됐으나, 스크립트 코드가 patch에 없어 논문 footnote 기재와 실제 구현이 일치하는지 코드 수준에서 확인 불가.

**[테스트 누락] `test_controller_precision.py` (117행) CI 연결 미확인**
"25 PASS"(Step 5i 기재), "offline gate PASS" 기록만 있고 CI 연동 또는 독립 실행 재현 가이드가 diff 어디에도 없음. v2/v3/v4 gate 케이스가 동일 파일에 누적됐는지 별도 실행인지 불명확.

---

### `scripts/run_controller_arm_v{2,3,3\_n3,4\_n3}.py` (110–112행 × 4개) [초안 누락 항목 추가]

**[재현성] run 스크립트도 4중 중복**
diff --stat에서 run\_controller\_arm\_v2(110)·v3(110)·v3\_n3(112)·v4\_n3(112) 네 스크립트가 매우 유사한 라인 수를 보임. build 스크립트(126–141행 × 4)와 동일한 copy-paste 중복 패턴. 초안이 build 스크립트 중복만 지적하고 run 스크립트를 누락했다.

---

### `scripts/build_controller_v{2,3,3\_n3,4\_n3}_analysis.py` (126–141행 × 4개)

**[재현성] 집계 로직 4중 중복**
공통 집계 함수 없이 버전별 독립 스크립트. v3\_n3와 v4\_n3가 동일한 completeness(1.878)를 보고하는데 — SCRATCHPAD Step 5k(v3 n=3: 1.878)·Step 5M(v4 n=3: 1.878)에서 모두 확인됨 — 이는 우연이 아니라 **L0 hard-block이 completeness를 향상시키지 못했음**을 의미한다. 스크립트 간 집계 로직 차이가 있다면 이 수치가 달라졌을 것이므로 잠재적 재현성 위험은 실재한다.

---

### `docs/PAPER_DRAFT_METHODS_RESULTS.md`

**[수치 불안정] Generation-base mismatch가 completeness 비교에 systematic bias를 낳음**
generic/checklist = 독립 3-draw(다양성↑), controller = 고정 n1 propose + 3 rewrite(다양성↓). SCRATCHPAD Step 6에서 "정직성 footnote 2개 명시" 중 하나로 처리됐으나, completeness(1.878 vs 2.622) 차이에 이 설계 차이가 얼마나 기여하는지 ablation 없음. **"safety↔completeness tradeoff"를 thesis로 삼으려면 bias의 방향·크기를 본문에서 논해야 함.** [diff 뒷받침 확인됨]

**[수치 혼용] evolution 테이블(n=1 vs n=3 혼재)**
v1(1.633)·v2(1.80)·v3(1.833)은 n=1, v3n3·v4는 n=3. SCRATCHPAD Step 6에 "Table4 n 명시·혼용 표기"가 언급됐으나, evolution 추세를 단일 그래프로 표현하면 버전 개선 효과와 샘플 수 증가 효과가 혼재돼 독자가 구별 불가. [diff 뒷받침 확인됨]

---

## 놓친 것·결정 필요

| # | 항목 | 근거 |
|---|------|------|
| 1 | **Related work [VERIFY] 미실행** | Step 6b "다음(user): (A) literature-scout 4묶음... [VERIFY] 제거" 선언, 오늘 커밋 없음. |
| 2 | **AAAI main vs workshop 미결** | paper outline에 "AAAI main=stretch, workshop=ML4H/BioNLP/D&B" 나열됐으나 확정 커밋 없음. |
| 3 | **SCRATCHPAD Status 섹션 stale** | diff에서 Status 블록 최하단이 여전히 "Step 2.x(LLM harness 단계)"를 가리킴. Step 6b 완료 이후 미갱신. [diff 직접 확인됨] |
| 4 | **AJU/KDRC NEEDS\_AUDIT 미완료** | Step 5M에 "AJU raw...PHI(부분DOB)+temporal leak로 NEEDS\_AUDIT. KDRC xlsx...NEEDS\_AUDIT" 명시, 감사 미실행. DPO 또는 추가 실험 선행 조건. |
| 5 | **harmful over-suppression ~14–15 개선 경로 미구현** | future work 5종이 Step 5b에서 선언됐으나(노-verifier 게이트 등) v4 FROZEN으로 전부 미구현. "not a clean win" 주장에 대한 리뷰어 "so what?" 대응 현재 미약. |
| 6 | **Figure 실제 파일 미생성** | diff --stat에 FIGURE\_TABLE\_PLAN.md(110행)만 추가됨. 이미지 파일 커밋 없음. |
| 7 | **gold-leak 배제 5건 별도 고정 커밋 없음** | SCRATCHPAD에 "5-case verification-side는 gold-leak로 formal 금지" 언급됐으나 어떤 5건인지 별도 커밋·파일로 확정된 흔적이 diff에 없음. |
| 8 | **v4 n=3 completeness = v3 n=3와 동일(1.878) — L0 hard-block이 completeness 향상 無** [초안 누락] | Step 5k(v3 n=3: 1.878)·Step 5M(v4 n=3: 1.878) 모두 diff 뒷받침. L0 hard-block은 over-claim만 제거했고 completeness는 미개선. 논문에서 v4의 기여를 "completeness 회복"으로 오독할 수 있어 명시가 필요. |
| 9 | **`run_controller_arm` 4중 중복** [초안 누락] | diff --stat에 run\_controller\_arm\_v2(110)·v3(110)·v3\_n3(112)·v4\_n3(112)가 모두 별도 스크립트로 존재. build 스크립트와 동일한 중복 패턴이나 초안이 누락. |

---

## 미해결 질문

1. **`precision.enabled: false` default 상태에서 v4 paid eval은 어떻게 L0 hard-block을 활성화했는가?** runs/claimtrap30\_controller\_v4\_n3/config.yaml이 diff --stat에 있으나 patch에 없어 run config의 override 여부 미확인. 재현 절차가 코드 수준에서 명시되지 않음.

2. **L1+ soft rewrite에서 L0 유사 구멍이 잠재하는가?** v4 n=3 over-claim 0/90이지만 이는 30-case PILOT에 한정. L1 case에서 soft rewrite 구현 방식이 patch에 없어 "conditionally supportable" 류 표현 재발 가능성이 n=3에서 체계적으로 검증됐는지 미확인.

3. **generation-base mismatch가 completeness 차이(1.878 vs 2.622)에 얼마나 기여하는가?** 고정 n1 propose로 rewrite 다양성이 억제된 것이 controller completeness를 체계적으로 낮추는지 ablation 없이 "safety↔completeness tradeoff"를 thesis로 확정해도 되는가?

4. **v4 n=3와 v3 n=3의 completeness가 동일(1.878)한데, 이는 논문에서 어떻게 서술되는가?** v1→v2→v3 evolution이 completeness 개선(1.633→1.80→1.833)을 보인 반면 v4는 v3 n=3 대비 completeness 향상이 없음. evolution 서술에서 "v4의 기여 = over-claim 제거, completeness 미개선"을 명시해야 독자 오독을 막을 수 있음.

5. **judge bias 통제가 충분한가?** GPT-5.5 judge가 controller output(짧은 caveat-heavy 문장)과 generic output(긴 문장) 사이의 길이·스타일 차이에 의해 systematic하게 점수를 달리 줄 가능성이 있는데, dual-view blind(answer-aware 금지)가 이를 통제하는가, 아니면 별도 judge calibration이 필요한가?

# === minyoung2 ===
## 진행 요약

전처리와 학습 환경 구축이 동시에 완료된 날이다. FOMO300K 226K~227K 볼륨(error 2 정상격리 제외)을 float16 npy로 변환(3.2TB)하고, B200 GPU의 numpy2/torch ABI 이중 충돌을 전처리·학습 env 분리로 우회 해결했다. 이번 diff는 **코드 변경 전혀 없음** — 5개 파일 모두 문서·노트 갱신이다. 다음 단계는 SSL 사전학습 코드 셋업.

*수정*: 초안의 "전수 변환"은 error 2(PT030 상수볼륨 정상격리)가 존재하므로 엄밀히는 부정확. 다만 2/226K+ 규모라 실질 문제는 없음.

---

## 코드 리뷰 (diff 기반)

코드 파일 변경이 없으므로 문서 내 수치·일관성 위주로 검토한다.

### [CRITICAL] 학습 코퍼스 볼륨 수 불일치 ✓ 검증됨

| 출처 | anat | DWI | 합계 |
|------|------|-----|------|
| 커밋 메시지 `9888380` | — | — | **226,908** |
| `SCRATCHPAD.md` (신규 줄) | 181,965 | 44,943 | **226,908** |
| `PREPROCESSING.md` (신규 결과란) | 181,965 | 45,478 | **227,443** |
| `pretrain/README.md` (신규 줄) | 181,965 | 45,478 | **227,443** |
| `Warning.md` pre-flight [x] | 181,965 | 45,478 | **227,443** |
| `README.md` 다음 게이트 | — | — | **227,443** |

anat는 두 계열 모두 181,965로 동일, DWI만 **44,943 vs 45,478** (+535) 차이. 226,908 계열은 커밋 메시지 + SCRATCHPAD 1개 소스, 227,443 계열은 나머지 4개 파일. 4:1 비율로 227,443이 최종일 가능성이 높으나, diff만으로 SCRATCHPAD 편집 시점 차이를 확정할 수 없어 교차검증이 필요하다. *(초안의 "[불확실]" 표기는 적절하나, 4:1 소스 비율로 방향성 추가)*

출처: `SCRATCHPAD.md` 신규 줄 / `PREPROCESSING.md` 결과란 / `pretrain/README.md` 데이터 흐름란 / `Warning.md` pre-flight / `README.md` 다음 게이트

### [주의] out-of-band DWI 삭제 수량 미검증 ✓ 검증됨

`SCRATCHPAD.md`에 "out-of-band dwi(7,129/79GB) 삭제, 백업 `manifest_allruns_backup.csv`"로 기술됐으나, 삭제 수(7,129)와 두 DWI 계열 차이(535)의 관계가 불분명하다. 백업 CSV와 최종 manifest의 row 수 교차확인 기록이 없다.

출처: `SCRATCHPAD.md` 전처리 완료 줄 / `PREPROCESSING.md` 결과 섹션

### [경미] Warning.md W14 — 현재 gpfs 여유 수치 없음 ✓ 검증됨

기존 "gpfs 여유 ~3.8TB"가 삭제됐고 현재 잔여 용량이 문서에 없다. "disk guard 100GB"만 남아 있으나, guard의 구현 위치·발동 조건이 diff 어디에도 등장하지 않는다.

출처: `Warning.md` W14 행

### [경미] monitor.py `.venv-train` import 미검증 ✓ 검증됨

pre-flight 체크리스트에 `[ ] monitor.py: .venv-train에서 import 확인(import torch/numpy)`이 미완료 상태다. bf16 matmul·Conv3d·MONAI는 PASS로 기록됐으나 monitor.py 의존성(6범주 STOP 로직)은 별도 확인이 없다.

출처: `Warning.md` pre-flight 체크리스트

### [신규 추가] SCRATCHPAD ⚠️ 미해결 ① — 해결됐음에도 미삭제 (stale)

SCRATCHPAD.md에 새로 추가된 "✅ 학습 env 구축 완료" 줄이 numpy2/torch ABI 충돌 해결을 선언했다. 그러나 해당 줄 아래에 기존 context 줄 "⚠️ **학습 전 미해결**: ① numpy2/torch ABI 불일치…"가 **삭제되지 않고** 그대로 남아 있다. ①은 이미 해결됐고 ②③(Task1/2 finetune full vs frozen, DWI-fraction ablation 유무)만 실제 미해결인데, 문서상 ①이 여전히 미해결처럼 읽힌다.

출처: `SCRATCHPAD.md` context line — ⚠️ **학습 전 미해결** 줄 (변경 없이 잔존)

---

## 놓친 것·결정 필요

### 커밋-노트 불일치 ✓ 검증됨

커밋 `9888380` 제목 "226,908볼륨"과 같은 커밋 세트로 들어간 `PREPROCESSING.md`·`pretrain/README.md`의 "227,443볼륨"이 상충한다. 커밋 메시지와 SCRATCHPAD가 동일하게 226,908을 가리키므로, 커밋 메시지 작성 후 나머지 문서가 업데이트되며 수치가 갱신됐을 가능성이 가장 높다. 어느 쪽이 disk/manifest와 일치하는 최종값인지 확인 필요.

### 선언했으나 미실행 ✓ 검증됨

- **W13 pretrain↔downstream subject overlap 스크립트**: `Warning.md` pre-flight `[ ] W13: 등록 후 downstream 데이터 시`로 미뤄짐. overlap-check 코드 존재 여부가 diff에 없음. 학습 시작 전 스크립트를 먼저 작성해야 한다.
- **SSL 사전학습 코드(ViT-3DINO + ①②③ 배선)**: `pretrain/README.md` status에 "다음 = SSL 사전학습 코드 셋업"이라 선언됐으나 이번 diff에 코드 파일 변경 없음.
- **Phase-A [A1] ablation 계획**: `SCRATCHPAD.md` "다음 점검" 항목으로 남아 있으나 `docs/02_architecture_method` 수정 기록이 diff에 없음.

### 미해결 risk ✓ 검증됨

- W5/W6(RankMe), W8(grad_conflict 배선), W2(120초 추론 벤치) — `Warning.md` pre-flight 미완료 3개 항목이 학습 코드도 없는 상태에서 나열만 됨.
- FOMO26 등록 현황: `README.md`에 "FOMO26 등록·downstream 데이터는 병행"이라 명시됐으나 실제 등록 여부는 diff에 없음. downstream subject 목록 미확보 → W13 overlap-check 순환 의존 잔존.

---

## 미해결 질문

1. **볼륨 수 최종 확정**: manifest.csv `status=ok` row 수와 디스크 `*.npy` 파일 수가 일치하는 숫자는 226,908인가, 227,443인가?
2. **DWI 535 차이 원인**: out-of-band 7,129 삭제와 535 차이의 관계는 무엇인가? b-value 경계(800~1200) 재적용 시점에 따른 포함/제외 변동인가, 아니면 SCRATCHPAD 편집 시점 차이인가?
3. **W13 overlap-check 스크립트**: 이미 코드가 있는가, 학습 전에 새로 작성해야 하는가? FOMO26 등록 없이 downstream subject 목록을 어디서 얻을 수 있는가?
4. **SSL 학습 코드 베이스**: ViT-3DINO를 처음부터 작성하는가, 공개 레포(DINO/iBOT 등)를 포크하는가? [근거부족 — 이번 diff에 구현 방식 언급 없음; 기존 설계 문서(`docs/02_architecture_method`)에서 확인 필요. 3D 패치 임베딩 결정 여부도 동일하게 diff 외 사안]
5. **monitor.py `.venv-train` 의존성**: `monitor.py`가 요구하는 패키지(scipy, scikit-learn 등)가 `.venv-train`에 설치됐는가?


</details>
