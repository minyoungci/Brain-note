# 일일 종합 — 2026-06-30 (KST)

> LLM 자동 종합(gen→critic→synth). **검토용 초안이며 미검증 주장이 있을 수 있음.**
> 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` synth=`claude-opus-4-8` · 활동 WS 1개

> Cross-workspace 종합(검증된 일일 분석 입력). **활동 WS 1개(minyoung2)** — 나머지 4개(minyoung3·minyoung4·plant·minyoungi)는 오늘도 검증된 분석 없음(가용 근거는 2026-06-07자 OPEN_QUESTIONS뿐, 3주 stale). 과장 회피, 근거 약한 곳 [불확실].

## 오늘 한 줄 요약

minyoung2가 **챌린지 제출 파이프라인을 처음으로 실물화**(`Challenge_Submission/`, predict.py 701줄, Apptainer SIF 5.2GB)하면서 어제 dark였던 "제출 컨테이너·strict 로드" 두 🔴가 최우선 트랙으로 복귀했지만, 그 대가로 **(a) 경로 문자열 기반 Task3/5 라우팅의 silent-wrong-output 리스크(CRITICAL)**, **(b) 공식 validator Phase 2·v2 SIF 빌드 미증명**, **(c) TC2 DECISION=GO의 잠정성**이 한꺼번에 노출됐고, 이 세 불확실성이 모두 **3회뿐인 validation attempt**라는 단일 희소자원으로 수렴하는데 이를 조율할 명시적 게이트가 없다 — 오늘 가장 큰 단일 리스크다.

## 워크스페이스별 핵심 (각 1~3줄)

| 워크스페이스 | 오늘 핵심 |
|---|---|
| **minyoung2** | ① 챌린지 제출 파이프라인 신설·SIF 빌드(검증 Phase 2 미증명), Task1 Synapse AUROC 0.658 확인 후 v2(frozen+5-seed ensemble)로 교체 결정. ② TC2 label-free 기준으로 α-ReQ 단독 선정(wg0.5 R²≈0.99), wg0.25/0.75 재학습 중·**GO 잠정**. ③ Flagship/문서 정렬(W17 신규 등록, Wave-I 과장 정정, Tutorial·inspect_data 코드 추가). |
| minyoung3 / minyoung4 / plant / minyoungi | **오늘 검증된 일일 분석 없음** — 어제와 동일하게 dark. 가용 근거는 3주 전(06-07) OPEN_QUESTIONS 트래커뿐으로 현재 상태 아님. |

## 주목·결정 필요 (우선순위순)

**1. validation attempt 3회 vs 다중 미증명 — 조율 게이트 부재 🔴**
Task3/5 라우팅 실환경 확인, 공식 validator Phase 2, v2 SIF 실빌드, TC2 checkpoint 최종 선택이 **모두 미완**인데 각각이 attempt를 소모할 수 있다. "v2 빌드 전엔 attempt 안 쓴다"는 선언만 있고 enforcement 없음. α-ReQ가 wg0.5 외 checkpoint를 고르면 SIF 전면 재빌드가 필요해 attempt 회계가 또 흔들린다.
→ *놓쳤을 가능성:* 일일 분석은 이 항목들을 개별 리스크로 나열했으나, **희소자원 1개로의 수렴**이라는 시스템 관점으로 묶지 않았다. 제출 전 "통과 선결조건 체크리스트 → 1 attempt 소비" 순서를 명문 게이트로 고정하지 않으면 개별 항목을 다 맞춰도 순서 실수로 attempt가 낭비된다.

**2. [CRITICAL] Task3/Task5 경로 문자열 라우팅 = silent wrong output 🔴**
`infer_t1_task()`가 전체 경로 문자열(`"task3"`/`"brainage"` 등)을 검사 — visible diff로 확정된 결함. `/input/case_001.nii.gz`처럼 경로에 task 식별자가 없으면 분기 실패 → Task3에 Task5 head 적용 시 **나이 scalar가 확률로(또는 반대로)** 출력되며 에러 없이 통과. Submission.md §3도 "Synapse 실환경 동일 여부 미확인 = 제출 리스크"로 자인.
→ *결정:* 주최측 문의(§3 액션) **발송·응답 여부**가 당일 커밋에 없음 — attempt 소진 전 반드시 선행. geometry fallback이 실제로 robust한지 별도 검증 필요.

**3. TC2 GO 잠정성 + 헤드라인 정정(fbd042a)의 미전파 🟠**
DECISION=GO가 α-ReQ R²≈0.99에 기대지만 이 fit이 **held-out인지 train-set self-fit인지 미확인** — self-fit이면 과적합 지표이고 Phase 1(wg0.25/0.75) 재현 실패 시 GO 근거가 무너진다. 더 중요한 건 fbd042a에서 "critic F1: rank↔transfer decoupling, **라벨-프리 selection 미확정**"으로 헤드라인을 정정했는데, 이 수정이 **AAAI draft·Flagship 문서에 반영됐는지 불명**.
→ *놓쳤을 가능성:* 코드/실험 정정은 잡혔으나 **정정의 문서 전파(propagation)** 가 추적되지 않았다. 논문이 정정 전 주장을 인용 중이면 paper-integrity 리스크 — 어제 "paper-ready 잠금"과 같은 모순 패턴.

**4. 어제 🔴 #1 `reverse_preprocessing`(seg metric-space validity) 여전히 dark 🟠**
오늘 R3 seg Wave-D~I 결과를 정리했으나 **metric이 원본 공간에서 산출되는지 재확인이 또 없다**. 무효 공간이면 Wave 결과 전체(및 AAAI draft 인용분)가 흔들린다. 어제 종합이 명시적으로 올린 최상위 항목이 **이틀 연속 미추적** — 해소가 아니라 공백이다. [불확실 — eval_seg 코드 미확인]

**5. W17(downstream 일반화 리스크) 등록되었으나 완화 액션 0 🟡**
cls(T1·T5) 과적합 risk가 CLAUDE.md W17로 문서화됐으나 cross-val scheme 변경·정규화 강화 등 **대응 코드가 당일 커밋에 없다**. 이는 Task1 LOOCV 0.942 → Synapse 0.658(42% 격차)와 직결되는 우려인데, v2 교체가 이 원인(site confound/label shift/전처리 불일치)을 통제했다는 **ablation이 없다**.

## 코드 리뷰 종합 (반복되는/심각한 결함)

- **반복 메타결함 — diff truncation으로 검증 미완(어제와 동일 패턴).** predict.py 701줄 중 diff가 ~240줄에서 잘려, 핵심 추론 블록(strict 로드 인자, brain-age clamp, T2*/SWI 처리, **실제 호출되는 checkpoint**)이 모두 **확인 불가 [불확실]**. 어제 "C4~C9 6건 전부 상한 미검토"가 오늘 [HIGH]×2 + [MEDIUM]×2로 재발 — "결함 확정"이 아니라 "검증 미완"이 누적되고 있다. **predict.py 전문 재검토가 단일 최고 ROI**.
- **확정된 심각결함(visible diff 근거):** Task3/5 경로 문자열 라우팅(CRITICAL). 라우팅 fragility는 task별로 반복 — Task1 vs Task2 구분이 `args.adc` **유무**에 의존(Submission.md §3 I/O 계약으로 확인)하는 등 dispatch가 전반적으로 brittle.
- **확정된 정리부채:** v1 `TASK1_CHECKPOINTS`(3-seed)가 v2 교체 결정 후에도 잔존(오적재 리스크) / Task1-v2 5-seed vs 타 task 3-seed 불일치 근거 미문서화 / Task2 미구현 SystemExit 메시지 모호 — 모두 visible diff 확정, 의도/버그 구분을 위해 주석·문서화 필요.
- **TC2 R²≈0.99 fit-range "4/4"** 의미(held-out vs self-fit)가 코드로 확인 안 됨 — 위 §3과 동일 뿌리.

## 미해결 질문 (연속 추적 — 어제 대비 신규/해소)

**진전·재활성(어제 dark → 오늘 표면화):**
- ✔️재활성 **제출 컨테이너(어제 S6)** → 오늘 `Challenge_Submission` 트랙으로 구축. 단 공식 validator Phase 2는 **여전히 미증명**(apptainer 부재, Permission denied).
- ✔️재활성 **strict 로드 footgun** → 오늘 [HIGH]로 재등장. 실제 `load_state_dict(strict=…)` 인자는 diff 밖이라 **미해결**.

**여전히 미추적(이틀 연속 dark):**
- ❌ **reverse_preprocessing / seg metric-space validity**(어제 🔴 #1) — 오늘도 미언급. AAAI seg 수치 신뢰성의 전제.

**신규(오늘 발생):**
1. Synapse 실환경 입력 경로에 task 식별자가 포함되는가, 중립 경로인가? (Task3/5 라우팅 운명을 결정 — 주최측 문의 발송/응답 여부 포함)
2. α-ReQ R²≈0.99 fit이 held-out인가 train self-fit인가? held-out이면 split 방식은?
3. 정상 Apptainer 노드는 확보됐는가, 언제 v2 SIF를 빌드하는가? (담당자·시점 불명)
4. Task1 LOOCV 0.942 vs Synapse 0.658 — 42% 격차의 주원인(site confound/label shift/전처리)과 v2의 통제 ablation 유무?
5. Task1-v2의 T2*/SWI 미사용이 FOMO26 system description **전면 공개 요건**에 위배되지 않는가?

**스테일 경고:** OPEN_QUESTIONS의 🔴 5건(KDRC 라벨 모순, OASIS 삼중값 재현 불가, IMG-020/022 결과 미생성, F10 통계, plant 통계력)은 **3주째 갱신 0** — 추적기 자체가 죽어 신규/해소 판정 불가. 살릴지 폐기할지 결정 필요.

## 워크스페이스 간 연결·시너지

**구조적 한계 명시:** 5개 중 1개만 활동 → 진정한 cross-workspace 비교 불가. 아래는 단일 WS 내 트랙 간 연결과 [불확실]한 잠재 시너지로 한정한다.

- **(WS 내, 확실) 제출 트랙 ↔ TC2 트랙의 충돌:** α-ReQ가 wg0.5 외 checkpoint를 선택하면 제출 SIF 재빌드가 강제됨 → 두 트랙이 **같은 validation attempt 예산**을 공유. 사실상 단일 의사결정으로 묶어야 한다(위 주목 #1).
- **(WS 내, 확실) W17 ↔ Task1 격차:** downstream 일반화 risk(W17)와 LOOCV→Synapse 42% 붕괴는 같은 현상의 문서/실측 두 얼굴. 통합 진단 필요.
- **(WS 간, [불확실]) TC2 라벨-프리 selection ↔ 치매 코호트 라인:** OPEN_QUESTIONS가 추적하는 KDRC/OASIS/NACC/ADNI 전이·ComBat 누수 문제와, minyoung2의 α-ReQ label-free checkpoint selection은 **개념상 "라벨 없이 전이 가능 모델 고르기"로 연결될 여지**가 있으나, 두 라인은 어제·오늘 모두 주제가 분리돼 보이며 3주 stale라 **근거 없음**. 시너지로 주장하지 않는다.
- **(공통 운영 리스크) 버전 안전망:** OPEN_QUESTIONS 기록상 minyoung3·plant는 `.git` 부재, minyoung4는 미커밋 — dark한 4개 WS가 살아있다면 동일 추적 공백을 안고 있을 것. minyoung2만 커밋 단위 검증이 가능한 현 상태가 **종합의 가시성을 1/5로 제약**하는 근본 원인이다.

---
<details><summary>워크스페이스별 검증 분석 (펼치기)</summary>

# === minyoung2 ===
## 진행 요약

오늘(2026-06-30) 작업은 세 트랙으로 진행됐다.

**① 챌린지 제출 파이프라인 신설** (eee76f0, 4f84359, df8a587, 563da7a; 445f104는 "Flagship AAAI 결과 테이블"로 이 버킷과 무관): `Challenge_Submission/` 전면 구축. `predict.py` 701줄 작성, Task1/3/4/5/6/7 라우팅 구현, Apptainer SIF 빌드 성공(5.2 GB, SHA256: 3e4d…). 다만 이 서버에서 `apptainer`가 없어 공식 validator Phase 2 통과는 미증명. Task1은 Synapse 제출 AUROC 0.658 확인 후 frozen foundation + 5-seed linear ensemble(v2)로 교체 결정, tar context 준비 완료(SHA256: 10da…).

**② TC2 label-free 기준 탐색** (95a4c9d → 2d6526c → b409121 → 0f1e8a6 → ddee58e, 최신 47c3cfc는 장기 학습 중 SSH 내성 watchdog + results 정리로 Phase 1 인프라 보강): α-ReQ를 유일 강후보로 선정(Phase 0), wg0.5 peak R²≈0.99 확인(Phase 0.5), wg0.25/0.75 재학습 착수(Phase 1). DECISION=GO는 **잠정** 상태. 별도로 fbd042a에서 TC2 헤드라인 정정(critic F1: rank↔transfer decoupling으로 정정, 라벨-프리 selection 미확정)이 이뤄졌으나 초안에서 누락됐다.

**③ Flagship/문서 정리** (나머지 커밋): v2_jepa probe 코드·Plan D/E/F 신설(diff --stat 확인), R3 seg Wave-D~I 결과 정리, W17(downstream 일반화 리스크) 신규 등록(CLAUDE.md W1~17 변경 확인), Wave-I 과장 정정, AAAI 포지셔닝 재정렬. **초안 누락 항목**: c873674에서 Tutorial/README.md(+42) + Tutorial/run_demo.py(+170)가 신설됐고, 98137d3에서 downstream/inspect_data.py(+219)가 추가됐다 — 둘 다 diff --stat에서 확인된 코드 추가임.

---

## 코드 리뷰

### [CRITICAL] Task3/Task5 라우팅 — 경로 문자열 기반 암묵적 의존
- `predict.py : infer_t1_task()` (diff 가시 구간, 대략 line 195 이후)
- 초안이 "파일 basename" 기반이라 서술했으나, 실제 코드는 `lower = str(p).lower()`로 **전체 경로 문자열**을 검사한다(`"task3"`, `"task_3"`, `"brainage"` 등). 개별 basename만 보는 게 아니므로 validator fixture 경로에 task 식별자가 포함돼 있다면 더 넓게 걸린다. 그러나 `/input/case_001.nii.gz`처럼 경로 어디에도 task 식별자가 없으면 여전히 분기 실패한다. Submission.md §3은 "Synapse 실환경도 같은지 확인 전까지는 제출 리스크"라 명시해 CRITICAL 판정을 뒷받침한다. Task3 케이스에 Task5 head가 적용되면 확률값이 나이 scalar로, 반대면 나이 예측이 확률로 출력되는 **silent wrong output** 발생 — 근거 충분.

### [HIGH] Task1-v2에서 T2*/SWI 입력 무시 — 경고 없음 [불확실]
- `predict.py : Task1-v2 추론 블록` (diff truncation으로 미포함)
- v2가 `--swi`/`--t2s`를 받지만 사용하지 않는다는 사실(의도된 ablation)은 Submission.md에서 확인됨. warn/log **부재** 여부는 diff가 ~240줄에서 잘려 실제 Task1-v2 추론 블록이 보이지 않아 증명 불가. 라우팅 fragility 주장(Task1 vs Task2 구분이 `args.adc` 유무에 의존)은 Submission.md §3 I/O 계약 테이블로 확인: Task1은 `--flair --dwi --adc`+ T2*, Task2는 `--flair --dwi`+ T2*(adc 없음)이므로 구조적 우려는 근거 있음. warn/log 절반은 [불확실].

### [HIGH] Checkpoint `strict=True` 강제 여부 미확인 [불확실 — diff 미포함]
- `predict.py` : 각 task 추론 블록 (diff truncation으로 미포함)
- Submission.md §2 "partial load 금지, missing/unexpected key면 실패해야 한다" 요건 확인됨. 실제 `load_state_dict` 호출 인자는 diff에 없어 확인 불가. 우려 자체는 타당하나 실제 결함인지는 불명.

### [MEDIUM] Task3 brain age 출력 범위 클램프 없음 [불확실 — diff 미포함]
- `predict.py` : Task3 추론 블록 (diff truncation으로 미포함)
- smoke 결과(71.26세, label 72)는 Submission.md에서 확인됨. clamp 로직 존재 여부는 diff에서 확인 불가. 우려는 타당하나 결함 확정 근거 없음.

### [MEDIUM] TASK1_CHECKPOINTS(v1, 3-seed) 잔존 — 초안 누락
- `predict.py` : `TASK1_CHECKPOINTS` (line ~74), `TASK1_V2_CHECKPOINTS` (line ~80)
- diff에서 직접 확인: v1 3-seed 체크포인트 목록과 v2 5-seed 체크포인트 목록이 **모두 정의돼 있다**. 실제 추론에서 어떤 것이 호출되는지는 diff truncation으로 확인 불가. v2 교체 결정(Submission.md) 이후에도 v1 CHECKPOINT 변수가 활성 코드에 남아 있다면 혼동·오적재 리스크가 있다.

### [MEDIUM] TC2 α-ReQ R²≈0.99 — fit-range 검증 기준 불명
- 커밋 `b409121`: "α-ReQ peak@wg0.5 fit-range robust(4/4, R²≈0.99)"
- "4/4"는 4개 fit range 구간 모두 통과를 의미하는 것으로 보이나, train-set self-fit인지 held-out validation인지 diff에서 확인 불가. self-fit이라면 R²≈0.99는 과적합 지표일 수 있고, TC2 Phase 1 재현 실패 시 DECISION=GO 잠정 근거가 흔들릴 수 있다.

### [MEDIUM] Task1-v2 5-seed vs 다른 task 3-seed — 설계 근거 미문서화
- `predict.py`: `TASK1_V2_CHECKPOINTS` 5개, `TASK3/4/5_CHECKPOINTS` 각 3개 — diff에서 직접 확인됨. 소규모 데이터셋에서 seed variance 관리 기준 불일치는 타당한 우려. 설계 결정이라면 주석 또는 Submission.md에 명시 필요.

### [LOW] Task2 route 부재 시 SystemExit 메시지 오해 유발
- `predict.py : infer_task()` raise 구문 (diff 가시 구간 확인)
- 에러 메시지에 Task2 언급 없음 — diff에서 직접 확인됨. validator log 분석 시 의도적 미구현인지 버그인지 구분 불가.

---

## 놓친 것·결정 필요

1. **Task3/Task5 실환경 basename/경로 확인 미완**: Submission.md §3에 "주최측 문의" 액션이 제시됐으나 당일 커밋에서 확인 또는 응답 흔적 없음. **validation attempt 소진 전에 반드시 선행**해야 한다.

2. **공식 validator Phase 2 통과 미증명**: Apptainer `instance start` Permission denied. 정상 노드 확보·일정이 커밋이나 SCRATCHPAD에 없음. "로컬 Linux 노드에서 재실행"이 선언만 되어 있다.

3. **Task1-v2 SIF 미빌드**: tar context 준비(SHA256: 10da…)만 됐고 실제 `.sif` 빌드는 로컬 노드 필요. 빌드 담당자·시점 불명. validation attempt를 v2 빌드 전에 쓰지 않겠다고 선언했으나 enforcement 방법 없음.

4. **TC2 GO 결정과 제출 타이밍 충돌 가능**: Phase 1이 wg0.25/0.75를 재학습 중인데, α-ReQ가 wg0.5 외 다른 checkpoint를 선택하면 현재 SIF를 전면 재빌드해야 한다. 이 의사결정이 validation attempt 3회 제한과 어떻게 조율될지 명시적 게이트 없음.

5. **W17 등록됐으나 완화 액션 없음**: cls(T1·T5) 과적합 risk가 W17로 문서화(CLAUDE.md 변경 확인)됐으나 cross-val scheme 변경, 정규화 강화 등 대응 코드 변경이 당일 커밋에 없음.

6. **Wave-I 과장 정정 내용 불명**: 커밋 `27b9054`가 diff --stat에는 포함됐으나 patch 미포함. 어떤 수치가 어떻게 바뀌었는지 확인 불가. AAAI draft에 Wave-I 결과가 인용돼 있다면 논문 업데이트 여부 확인 필요.

7. **v2_jepa Plan D/E/F — 실험 진행 여부 불명** [불확실]: `Plan_D/E/F` 파일과 `RUNNING_JEPA_PILOTS.md` 신설이 diff --stat에서 확인됨. 실제 실험 결과 파일은 diff에 없음. 계획 문서만 추가된 것인지, pilot이 실행 중인지 불명.

8. **초안 누락 — TC2 헤드라인 정정(fbd042a) 반영 여부**: "critic F1: rank↔transfer decoupling, 라벨-프리 selection 미확정"으로 정정됐는데, AAAI draft나 Flagship 문서에 이 수정이 반영됐는지 확인 필요.

9. **초안 누락 — Tutorial/run_demo.py(+170줄)·inspect_data.py(+219줄) 활용 계획 없음**: c873674(foundation fine-tuning tutorial)와 98137d3(downstream 데이터 확인 도구)는 diff --stat에서 명확히 확인되는 코드 추가다. 팀 내 사용 대상·온보딩 목적이 문서화되지 않았다.

---

## 미해결 질문

1. **Synapse 실환경에서 Task3/Task5 입력 파일 경로에 task 식별자가 포함되는가, 아니면 중립적 경로로 전달되는가?** `infer_t1_task()`는 전체 경로 문자열 검사 방식이므로, 경로에 "task3"/"task5"가 없으면 geometry fallback에 의존하게 된다. 주최측 문의를 발송했는가, 응답이 있었는가?

2. **TC2 Phase 1 (wg0.25/0.75) 결과는 언제 나오는가?** 결과 전에 validation attempt를 사용할 것인가, 아닌가? 결정 기준이 없으면 attempt가 낭비될 수 있다.

3. **α-ReQ R²≈0.99 fit은 held-out split 기준인가, train-set 기준인가?** held-out이라면 어떤 split 방식을 썼는가?

4. **공식 validator를 정상 실행 가능한 Apptainer 노드가 확보됐는가?** 확보됐다면 언제 사용할 수 있는가?

5. **Task1-v2의 T2*/SWI 미사용이 FOMO26 system description 제출 요건에 위배되지 않는가?** 일부 챌린지는 실제 사용 모달리티 전면 공개를 요구한다.

6. **Task1 내부 LOOCV AUROC 0.942 vs Synapse 실환경 0.658 — 42% 격차의 주요 원인이 site confound인가, label shift인가, 전처리 불일치인가?** v2(frozen foundation + mean fusion + 5-seed ensemble)가 이 원인을 어떻게 제어했는지 ablation이 있는가?


</details>
