# 일일 종합 — 2026-07-01 (KST)

> LLM 자동 종합(gen→critic→synth). **검토용 초안이며 미검증 주장이 있을 수 있음.**
> 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` synth=`claude-opus-4-8` · 활동 WS 2개

## 오늘 한 줄 요약

minyoung2가 하루 5,628줄의 고강도 외부검증 스프린트로 **cross-population 주장을 FDR 기준 "trend(유의 아님)"로 정직하게 하향 정정**(`5027173`)하고 Grad-CAM을 무효화(40–57% 뇌 바깥, `2773584`)하는 사이, minyoung4가 그 작업물을 큐레이션한 **AAAI 공식 확정 레포로 신설**(102파일 14,568줄)됐다 — 그러나 두 레포 모두 패치 상한(공식적 227줄 `common.py` 누락, minyoung4는 Python 코드 전체 누락)으로 **핵심 코드가 검증 불가 상태이며, minyoung2에서 확인된 결함(B1·B2)이 확정 레포로 전파됐는지가 오늘의 최대 사각지대**다. 어제 종합의 최상위 리스크였던 챌린지 제출·validation attempt·seg metric-space는 오늘 분석에서 **전면 소실**됐다.

## 워크스페이스별 핵심 (각 1~3줄)

| 워크스페이스 | 오늘 핵심 |
|---|---|
| **minyoung2** | ① BrainIAC 별도환경 unblock 후 다중모델 외부비교 완료, downstream 7-task에서 foundation > BrainIAC 유의 5/7. ② bootstrap CI + BH-FDR 적용 후 **cross-population을 "trend"로 정직 하향 정정**, Grad-CAM 무효·head-size confound 배제. ③ 논문 npj DM 방향, KDRC는 검정력 문제로 staged. |
| **minyoung4** | AAAI 제출용 **공식 확정 레포 신설** — minyoung2 작업물을 단일 init 커밋으로 큐레이션 이관. TC1(transfer Δ+0.134)·TC2 FINDING(rank-transfer decoupling)·TC2 NO-GO(라벨-프리 selector 실패) 확정, TC3(외부 multi-site)는 준비 중. **신규 코드 아님, 선별 이전**. |
| minyoung3 / plant / minyoungi | 오늘 검증된 일일 분석 없음 (어제와 동일 dark, 근거는 3주 stale OPEN_QUESTIONS뿐). |

## 주목·결정 필요 (우선순위순)

**1. minyoung2 결함(B1·B2·common.py 불투명)의 확정 레포 전파 여부 🔴**
minyoung4는 minyoung2 작업물의 **큐레이션 이전**이므로, minyoung2에서 CONFIRMED된 B1(`eval_frozen.py`의 `true_age_mean` 집계기준 불일치)·B2(`eval_fullft.py` 이중정규화 assertion)·D1(A4 종단 세션 불일치 오염)이 minyoung4 확정 자산에 그대로 담겼을 개연성이 높다. 그러나 minyoung4는 30KB 패치 상한으로 `pretrain/downstream/preprocessing/eval_harness.py` 전체가 검증 불가.
→ *놓쳤을 가능성:* 두 일일 분석은 각자 자기 레포만 검토했다. **"확정 레포에 결함이 curation 필터를 통과했는가"는 어느 쪽도 교차 확인하지 않았다.** AAAI 제출물의 무결성 관점에서 이것이 단일 최고 ROI — minyoung2 B1·B2 fix가 minyoung4로 반영되는 명시적 동기화 게이트가 필요하다.

**2. cross-population 정정(`5027173`)·Grad-CAM 무효(`2773584`)의 문서·확정레포 미전파 🔴**
minyoung2가 cross-population을 "trend"로 정직 하향하고 Grad-CAM을 무효화했으나, draft 파일(`04_4_tc3_external.md` 등)과 minyoung4 STATUS.md 확정 테이블에 **반영됐는지 양쪽 diff 모두에 없어 확인 불가** [근거부족]. minyoung4 확정 자산이 정정 **이전** 표현을 인용 중이면 paper-integrity 리스크다.
→ *놓쳤을 가능성:* 어제 종합도 지적한 **"정정의 문서 전파 미추적" 패턴이 이틀 연속 반복**. 코드/통계 정정은 잡히는데 그 정정이 논문·확정레포로 흘렀는지는 계속 공백.

**3. minyoung4 문서 내부 모순 — Table 1 정의 vs 배제 결정 🟠**
`docs/02`가 infarct(n=21) "완전 배제"를 선언했는데 같은 파일 Table 1 목록에 infarct 열이 잔존, 5점 그리드 근거(α-ReQ peak@wg0.25)인데 Group B 테이블에 wg0.25·wg0.75 누락. polymicro AUROC는 CI 없이 점추정만.
→ *결정:* Table 1을 `{brain age r(CI), polymicro AUROC, rankme}`로 확정할지, polymicro를 CI 미비로 보조 텍스트 강등할지. 배제 결정과 표 정의의 직접 모순은 리뷰어가 즉시 포착한다.

**4. TC2 FINDING의 inverted-U가 내부 단일 코호트(n=494)에만 의존 🟠**
minyoung4 `docs/01`이 "C3 외부서 inverted-U 미재현이면 경고 자체 붕괴 — 완료형 금지"를 자인. 외부 6코호트 재현 실패 시 TC2 FINDING이 "wg0.5 >> wg0" 사실로만 축소된다. minyoung2의 외부검증 harness가 바로 이 재현의 도구인데 TC3는 아직 준비 중.
→ 어제 "TC2 GO 잠정" 우려는 오늘 **NO-GO(라벨-프리 selector 실패)로 명확히 해소**됐으나, FINDING 자체의 외부 재현 리스크는 미해소로 승계.

**5. 어제 최상위 리스크(챌린지 제출·validation attempt·seg metric-space)의 전면 소실 🟡**
어제 종합의 CRITICAL이던 Task3/5 경로라우팅 silent-wrong-output, 3회 validation attempt 조율, reverse_preprocessing seg metric-space가 **오늘 두 분석 어디에도 언급 없음**. 초점이 챌린지 제출 → AAAI 외부검증 논문으로 이동한 것으로 보이나, 챌린지가 제출·마감됐는지 폐기됐는지 **근거 없음** [불확실]. 미해결 CRITICAL의 조용한 소멸인지 우선순위 전환인지 확인 필요.

## 코드 리뷰 종합 (반복되는/심각한 결함)

- **[반복 메타결함] 패치 truncation으로 핵심 코드 검증 미완 — 이틀+ 연속.** minyoung2는 `common.py` 227줄(`age_adjust`, `kfold_oof`, `extract_features`, `_gap_groups`) 전체 누락으로 B1·D1·R1·R2의 실제 영향도 확정 불가, minyoung4는 30KB 상한으로 Python 코드 전체가 문서 기반 추론뿐. **"결함 확정"이 아니라 "검증 미완"이 누적**되는 어제와 동일 패턴. common.py 전문 재검토가 minyoung2 단일 최고 ROI.
- **[반복] bootstrap CI 누락 — 두 레포 공통.** minyoung2 R4(AIBL→AJU/A4 cross-cohort pearson·MAE에 CI 없음), minyoung4(polymicro AUROC 점추정만). 논문이 이 수치를 직접 인용하면 신뢰구간 부재가 리뷰 지적 대상. within-cohort는 CI가 있는데 cross-cohort/보조지표만 빠지는 비대칭이 반복된다.
- **[확정, visible diff] 집계 모집단 불일치·데이터 오염.** minyoung2 B1(`startswith(g)`로 "NC"·"HC"·"DEMENTIA" 누락 → `true_age_mean`과 `gap_mean` 모집단 상이), D1(A4 종단 세션 불일치 age-ROI 오염, 케이스 수 미기록), D2(head-size threshold 0.05 하드코딩). B1은 AIBL에 실제 "NC"/"DEMENTIA" 레이블 subject 존재 여부에 영향도가 좌우됨 → 미해결 질문 5로 연결.
- **[확정] A4 all-CN 처리 가정 논문 미명시.** `has_dx` False 시 전원 CN 취급하나 A4는 "preclinical amyloid-positive" — 독자 오해 소지. 두 레포 공통 관련(minyoung4 외부 코호트 배선).

## 미해결 질문 (연속 추적 — 어제 대비)

**신규 (오늘 등장):**
- `common.py`의 `age_adjust`/`kfold_oof`/`extract_features` 구현 — B1·R1·R2·D1의 실제 영향도가 전부 여기 종속 (minyoung2 Q1–3).
- downstream 7-task "유의 5/7"의 BH-FDR 보정 패밀리 범위·q-value 임계값 (minyoung2 Q4).
- AIBL에 실제 "NC"/"DEMENTIA" 레이블 subject 존재 여부 (B1 영향도, minyoung2 Q5).
- Grad-CAM 무효화 후 논문 XAI 섹션 처리(삭제/limitation 이동/개선) (minyoung2 Q6).
- inverted-U의 C3 외부 6코호트 재현 가능성 + fallback 서사 (minyoung4 Q1).
- BrainIAC 획득 여부 vs BrainJEPA 단일 baseline 확정 (AAAI 데드라인 대비, minyoung4 Q4).

**해소:**
- ☑ **TC2 label-free selection GO 잠정성** → minyoung4에서 **NO-GO(라벨-프리 selector 실패)로 확정**. 어제 "GO 잠정, self-fit 여부 불명" 우려가 명확히 정리됨.
- ☑ **cross-population 주장 강도** → FDR 기준 "trend(유의 아님)"으로 정직 하향(`5027173`). (단, 문서 전파는 미확인 — 주목 #2로 승계)

**소실 (해소 아님, 추적 공백):**
- ☐→? 챌린지 Task3/5 경로라우팅 silent-wrong-output, 3회 validation attempt 게이트, reverse_preprocessing seg metric-space — 오늘 언급 전무. 제출 완료/폐기/보류 중 무엇인지 불명 [불확실].
- ☐ 3주 stale OPEN_QUESTIONS(KDRC 라벨 권위 모순, OASIS 진단 삼중값, minyoung3·plant git 부재)는 여전히 미갱신.

## 워크스페이스 간 연결·시너지

- **minyoung4 = minyoung2의 큐레이션 산출물 (강결합).** 오늘 두 레포는 독립 연구가 아니라 **동일 작업물의 원본↔확정본 관계**다. 따라서 (a) minyoung2 코드 결함의 확정레포 전파 여부(주목 #1), (b) minyoung2 통계 정정의 확정레포 반영 여부(주목 #2)가 자동으로 최우선 교차 확인 항목이 된다. **minyoung2에서 fix가 발생하면 minyoung4로 재-큐레이션하는 단방향 동기화 규율**이 없으면 확정레포가 stale 결함/구표현을 고정한다.
- **외부검증 harness 공유 계보.** minyoung2의 `eval_frozen.py`/`eval_cls.py`/`brainage_3cohort.py` + `common.py` 공유코어가 minyoung4의 `eval_harness.py`(432줄)로 이어지는 것으로 추정 [불확실 — 패치에 코드 없음]. 두 레포에서 발견된 CI 누락·A4 all-CN 가정은 이 공유 계보에서 비롯된 **동일 뿌리의 반복 결함**일 가능성이 높다 — 한쪽 수정이 양쪽에 이득.
- **BrainIAC의 이중 역할.** minyoung2는 BrainIAC를 외부 비교 baseline으로 이미 unblock·비교 완료(`3fa8200`)했으나, minyoung4 STATUS.md는 BrainIAC 획득을 여전히 "web/HF acquisition, else JEPA suffices"로 열어둠. **minyoung2의 완료 상태가 minyoung4 계획에 반영 안 된 정보 비대칭** — minyoung4 Q4(BrainIAC 투자 여부)는 minyoung2 결과를 참조하면 일부 해소될 수 있다.

---
*입력: 검증된 활동 WS 2개(minyoung2·minyoung4). 나머지 3개 dark. 두 레포 모두 핵심 코드가 패치 상한으로 누락되어 코드 관련 판단 상당수가 [불확실]·문서 기반 추론임을 명시한다.*

---
<details><summary>워크스페이스별 검증 분석 (펼치기)</summary>

# === minyoung2 ===
## 진행 요약

하루 만에 50개 파일 5,628줄이 추가된 고강도 평가 스프린트 (diff --stat 직접 확인). 핵심 흐름: ① BrainIAC 별도 환경(monai 1.3.2)으로 unblock → 다중모델 외부비교 완료 (커밋 `3fa8200`), ② 외부검증 harness(AIBL/AJU/A4/ADNI) 코호트별 모듈화 + `common.py` 227줄 공유코어 추출 (`cde75bf`, `0a01d95`), ③ downstream 7-task 벤치마크에서 foundation > BrainIAC 유의 5/7 달성 (`daa6ba5`), ④ 다중비교 bootstrap CI + BH-FDR 적용 (`8d9295f`), ⑤ cross-population 주장을 FDR 기준 "trend(유의 아님)"으로 정직 정정 (`5027173`), ⑥ Grad-CAM 40–57% 뇌 바깥 무효 확인(`2773584`) · head-size confound 배제(`81da0cb`). 논문 방향 npj DM (`a25100c`), KDRC 검정력 문제로 staged 처리 (`a25100c`, `5027173`).

---

## 코드 리뷰 (diff 기반)

### 🔴 버그

**B1. `eval_frozen.py` — `true_age_mean` 집계 기준이 `_gap_groups`와 불일치 [CONFIRMED]**

```python
for g in res["aibl_gap_raw"]:
    res["aibl_gap_raw"][g]["true_age_mean"] = float(
        ya[np.array([str(d).upper().startswith(g) for d in dxa])].mean()
    )
```

`AIBL_GRP = [("CN", ("CN", "NC", "HC")), ("MCI", ("MCI",)), ("AD", ("AD", "DEMENTIA"))]` 기준으로 `_gap_groups`가 집계하므로 "NC"·"HC"·"DEMENTIA" 레이블 피험자가 gap 통계에는 포함된다. 그러나 이 루프는 `startswith(g)` 만으로 비교하므로 `g="CN"` → "NC"·"HC" 누락, `g="AD"` → "DEMENTIA" 누락. AIBL 코호트에 해당 레이블이 실제로 존재하면 AD·CN 그룹의 `true_age_mean`이 `gap_mean`과 집계 모집단이 달라진다. (diff 내 `eval_frozen.py` 코드에서 직접 확인)

**B2. `eval_fullft.py` — assertion이 double-normalization 후 실패할 가능성이 매우 높음 [CONFIRMED, 단 "항상 실패" 표현은 과장]**

```python
assert 0.0 - 1e-4 <= a.min() and a.max() <= 1.0 + 1e-4, ...
```

`_pp(..., normalization_operation=["volume_wise_znorm"])` 는 z-score를 적용하므로 출력 범위가 [0, 1]로 보장되지 않는다. 이미 정규화된 yucca4 npy에 재적용하면 분포가 다시 이동하여 `a.max() > 1.0 + 1e-4`가 될 가능성이 매우 높다. 코드 자체도 `⚠️ W3` 주석으로 이중정규화 문제를 인지하고 있으며, "부활 시 no_norm으로 고칠 것"이라 명시한다. `_pp` 내부의 rescale 단계 여부가 diff에 없으므로 "항상 실패" 단언은 [과장]; 그러나 skull-strip 후 재활성화 시 첫 subject에서 크래시 위험이 높다.

---

### 🟡 데이터 오염·수치 안정성

**D1. `brainage_3cohort.py` — A4 종단 세션 불일치 시 age-ROI 쌍 오염, 개수 미기록 [CONFIRMED]**

```python
cand += [os.path.join(base, s, "t1w", "roi_masks") for s in sorted(os.listdir(base))]
```

정확 세션 ROI 부재 시 다른 시점의 ROI를 사용하나 `r["age"]`는 원래 세션 값 그대로. ROI 부분상관 분석이 종단상 다른 시점 데이터로 오염된다. 결과 JSON에는 `n_with_roi`만 기록되고 세션 불일치 케이스 수는 기록되지 않는다. (diff 내 코드 직접 확인)

**D2. `head_size_confound.py` — head-size threshold 0.05 하드코딩, 코호트 간 정규화 범위 검증 없음 [CONFIRMED]**

```python
hs = np.array([float((np.load(r["npy"]) > 0.05).sum()) for r in rows], np.float32)
```

yucca4 전처리의 정규화 범위가 코호트별로 다를 경우 동일 0.05 임계값이 다른 voxel 범위를 선택한다. 검증 코드나 범위 확인 assert 없음.

---

### 🟡 재현성·검증 누락

**R1. `eval_frozen.py` — 동일 random-feature OOF를 두 지표에 독립 2회 계산 [PLAUSIBLE, 불확실]**

```python
rand_pear = [_pear(ya[cn_a], kfold_oof(Xr[cn_a], ya[cn_a], args.lam)) for Xr in Xa_rs]
rand_mae  = [_mae (ya[cn_a], kfold_oof(Xr[cn_a], ya[cn_a], args.lam)) for Xr in Xa_rs]
```

`kfold_oof`의 내부 fold seed가 고정되지 않으면 두 지표가 서로 다른 OOF에서 계산될 수 있다. `common.py` 내용이 diff에 없어 확정 불가 [불확실]. OOF를 한 번 계산해 재사용하는 것이 더 안전하다.

**R2. `brainage_3cohort.py` — `extract_features` 중복 호출, `random` 파라미터 기본값 불일치 위험 [PLAUSIBLE, 불확실]**

`analyze()` 내부에서 `random=False`로 호출하고, `main()`에서 동일 tag로 `random` 파라미터 없이 재호출한다. `common.py`의 `extract_features` 기본값이 `random=False`가 아니면 캐시 미스 + 다른 feature 추출. `common.py` 내용이 diff에 없어 확정 불가 [불확실].

**R3. `eval_cls.py` — diff가 `_cn_ad_subset` 정의에서 잘려 `main()` 로직 전체 검토 불가 [CONFIRMED]**

cross-cohort AUROC 계산, `_zscore_own` 사용 여부, BH-FDR 적용 방식이 diff에서 누락되어 검증 불가.

**R4. `brainage_3cohort.py` — cross-cohort 결과에 bootstrap CI 없음 [CONFIRMED, 초안 누락]**

```python
res["crosscohort_from_AIBL_CN"][c] = {"pearson": _pear(yc, pc)[0], "mae": _mae(yc, pc), ...}
```

within-cohort(`accuracy.pearson_ci`, `accuracy.mae_ci`)와 달리 AIBL→AJU/A4 cross-cohort 전이 pearson·MAE에는 bootstrap CI가 없다. 논문에서 cross-cohort 수치를 직접 인용할 경우 신뢰구간 부재가 문제가 된다.

---

## 놓친 것·결정 필요

1. **`eval_fullft.py`(E2) 재활성화 조건 미결 [CONFIRMED].** 코드 docstring에 "AIBL fresh BET 필요"라 명시되나 실행 계획이 없다.

2. **A4 코호트를 all-CN으로 처리하는 가정이 논문에 명시 안 됨 [CONFIRMED].** `brainage_3cohort.py` · `head_size_confound.py` 모두 `has_dx`/`any(r["dx"])`가 False이면 `np.ones(len(rows), bool)`로 전원 CN 취급. A4는 "preclinical amyloid-positive"이므로 CN과 동일하다고 독자가 오해할 수 있다.

3. **downstream 벤치마크 "유의 5/7"의 BH-FDR 보정 패밀리 범위 불명확 [불확실].** 커밋 `8d9295f`·`daa6ba5`만으로는 7-task 내 보정인지 cross-population 비교까지 합산한 패밀리인지 구분 안 됨.

4. **Grad-CAM 무효화 이후 논문 XAI 섹션 처리 방향 [근거부족].** 커밋 `2773584`로 Grad-CAM 무효는 확인되나, draft 파일(`04_4_tc3_external.md` 등) 내용이 diff에 없어 draft 반영 여부 검증 불가.

5. **cross-population 정정(`5027173`)이 draft 섹션에 반영됐는지 [근거부족].** `04_4_tc3_external.md`(+73줄), `04_5_model_comparison.md`(+49줄)이 diff --stat에 있으나 내용이 patch에 없어 구 표현 잔존 여부 확인 불가.

6. **KDRC staged 계획의 구체 수치 없음 [불확실].** `STATE.md`(+55줄 변경)가 diff --stat에 있으나 patch에 없어 power analysis 수치·재개 시점 기재 여부 확인 불가. 커밋 메시지 수준에서는 "검정력 필요" 선언만 있다.

7. **`common.py` 227줄 전체가 diff patch에 없음 [초안 누락].** `age_adjust`, `_gap_groups`, `kfold_oof`, `extract_features` 등 핵심 함수의 구현이 불투명하여 B1·R1·R2·D1 관련 실제 영향도와 미해결 질문 1·2가 근본적으로 검증 불가 상태임을 명시해야 한다.

---

## 미해결 질문

1. `common.py`의 `age_adjust(gap, y, cn_mask)` 가 정확히 어떤 회귀를 사용하는가? linear residual인가, site offset + age slope 동시 제거인가? cross-cohort gap 해석의 핵심.
2. `kfold_oof`는 seed 인수를 받는가? 같은 입력에서 두 번 호출 시 동일 fold 분할·OOF를 보장하는가? (R1 영향도 결정)
3. `extract_features`의 `random` 파라미터 기본값은? `False`가 아닐 경우 R2 버그 실재.
4. downstream 7-task BH-FDR 보정에서 "유의 5/7"의 q-value 임계값과 보정 패밀리 범위는?
5. AIBL에서 "NC" 또는 "DEMENTIA"로 레이블된 실제 subject가 있는가? (B1의 실제 영향도 판단)
6. Grad-CAM 결과(40–57% 뇌 바깥)를 논문에서 어떻게 처리할 것인가 — 삭제·limitation 이동·방법론 개선 중 선택?

# === minyoung4 ===
## 진행 요약

`minyoung4`가 AAAI 제출용 **공식 확정 레포**로 신설됐다. 단일 이관(init) 커밋으로 `minyoung2` 작업물을 큐레이션해 102파일 14,568줄이 일괄 추가됐다(diff stat 직접 확인). 확정 자산으로 명시된 항목은 TC1(protocol-adaptive transfer Δ+0.134, gap +0.101), TC2 FINDING(rank-transfer decoupling, regret 0.194), TC2 METHOD NO-GO(라벨-프리 selector 실패), 외부 라벨 테이블(brain-age 2,093, dx, morphometry)이며(STATUS.md 확정 테이블 기준), TC3(외부 multi-site 검증)는 전처리·실험 준비 중 상태다. 오늘 코드가 새로 작성된 것이 아니라 **기존 작업물의 선별 이전**이다. ~~이 레포의 코드 품질은 `minyoung2` 시점 기준이다~~ — [근거부족: diff는 minyoung2 코드 품질을 언급하지 않음. 커밋 메시지는 "확정본 큐레이션"이라고만 기술.]

---

## 코드 리뷰 (diff 기반)

> **⚠️ 패치 상한(30,000 B) 초과로 Python 코드 파일 전체(`pretrain/`, `downstream/`, `preprocessing/`, `baseline-codebase/`)가 패치에 포함되지 않음. 코드 관련 항목은 문서 기반 추론이며 [불확실] 처리.**

### 문서 내 수치 불일치

- **`docs/02` Group B 테이블 vs. 5점 그리드 불일치**
  `STATUS.md`와 `docs/01`은 TC2 NO-GO 근거로 "5점 검정(`phase1_5point.json`)"을 명시하고, α-ReQ의 argmax가 5점 격자에서 **wg0.25**임을 명시한다. 그러나 Group B 테이블(`docs/02`)에는 `wg0, wg0.5, wg1`(+random) 4행만 있고 **wg0.25·wg0.75가 누락**됐다. 논문 Table 1이 이 4행 테이블을 그대로 쓰면 5점 검정의 핵심 근거(α-ReQ peak@wg0.25)를 독자가 확인할 수 없다.
  `docs/02_ablation_study_plan.md:Group B 결과표`

- **Paper-ready Table 1에 "infarct" 열이 잔존**
  `docs/02` Group B에서 infarct(n=21)를 "완전 배제(~~컷~~)"한다고 선언했는데, 같은 파일 하단 `Table 1` 목록에는 여전히 `{brain age r, polymicro, infarct, rankme}` 4열이 기재돼 있다. 배제 결정과 표 정의가 직접 모순된다.
  `docs/02_ablation_study_plan.md:Paper-Ready Tables 섹션`

- **polymicro AUROC CI 미기재**
  brain age는 CI가 명시([0.762,0.819] 등)되지만, polymicro AUROC는 점추정만(0.793→0.957→0.984) 있고 bootstrap CI가 없다. `docs/02` 자체가 "random baseline CI [0.438,0.767]도 chance 포함 → 절대값 신뢰 불가"라고 지적했음에도 polymicro 점추정만으로 보조 근거를 삼는 것은 수치 불안정성을 은폐할 수 있다.
  `docs/02_ablation_study_plan.md:Group B 결과표`

### 재현성 위험 [불확실]

- **vit_ibot collapse 참조 run의 통제 부재** [불확실]
  Group R 테이블에 `vit_ibot`이 `collapse_risk` 레이블로 포함돼 있으나, 문서 자체에 "직접 ResEnc-DINO matched run 없음 → 과대해석 금지"라고 명시돼 있다. 코드 파일이 패치에 없어 어떤 스크립트로 이 결과를 재현하는지 확인 불가. backbone 비교표(Group E)에 이 수치를 인용할 경우 독자 오독 위험.
  `docs/02_ablation_study_plan.md:Group R`

- **`low-LR scratch ≈ frozen-scratch` 동일 가정** [불확실]
  `docs/02`는 "low-LR mode는 pretrained decoder 사용 → matched low-LR scratch는 frozen-scratch와 사실상 동일, 별도 +0.142 주장 안 함"으로 처리해 별도 실험 없이 재사용한다. decoder 초기화 방식의 차이가 이 동치를 깨는 엣지 케이스가 있을 수 있으며, `downstream/` 코드에서 해당 분기 구현이 패치에 없어 확인 불가.

- **외부 코호트 전처리 동일성 검증 미완료** [불확실]
  `docs/03`이 "동일 Yucca 4-step"을 요구하지만 A4는 "raw 재전처리 진행 중"으로 명시돼 있다. diff stat에 `preprocess_external_yucca4.py`(680줄)·`preprocess_fomo300k.py`(525줄) 모두 존재하나, 패치 상한으로 내용 확인 불가. 두 스크립트 간 파라미터 drift가 있으면 C3 전이 격차의 일부가 전처리 artifact일 수 있다.

---

## 놓친 것·결정 필요

1. **TC2 5점 그리드 데이터 미노출**
   `phase1_5point.json`이 `results/tc2_labelfree_selection/`에 있다고 STATUS.md에 선언됐으나 이번 diff에는 포함되지 않았다. 공식 레포가 결과 파일을 담지 않는다면 reproducibility claim이 약해진다. 결과 파일 포함 여부 또는 별도 릴리즈 관리 결정 필요.

2. **`docs/03` 평가 프로토콜 섹션이 패치에서 잘림**
   Low-LR/full fine-tune 평가 방법론 텍스트가 30,000 B 한도로 끊겼다. 해당 섹션에 C1 프로토콜의 구현 세부사항이 있을 경우 검토 누락이다.

3. **Baseline 코드 상태 선언 없음**
   STATUS.md 로드맵 §2에 "BrainJEPA(local, external-eval cache exists)"가 baseline으로 언급됐지만, 이번 diff에 BrainJEPA 관련 코드나 결과 파일이 보이지 않는다. `minyoung2`에 잔류 중인지, 미완인지 불분명.

4. **TC3 외부 brain age·dx 실험 코드 연결 미확인**
   `docs/03`이 `eval_harness.py`(432줄, diff stat 확인)의 `--ckpt/--baseline/--tasks`로 외부 코호트 배선을 "예정"한다고 기술하나, 현재 어떤 task 이름으로 6코호트가 연결됐는지 패치 내용이 없어 확인 불가.

5. **`docs/07` shortcut-control 문서 미포함**
   STATUS.md §3에 "shortcut-통제 평가(`docs/07`)"가 언급됐지만 이번 diff에 `docs/07`이 없다. 확정 레포 포함 목록 정리 필요.

6. **OASIS-3 제외 결정 문서화 부족** *(초안 누락)*
   `docs/03`은 OASIS-3를 "OASIS-1/2(566 subj)가 사전학습에 포함되고 ID 재익명화로 disjoint 증명 불가"라는 이유로 sensitivity check 외 사용 금지로 명시했다. 이 결정이 STATUS.md 확정 항목에는 나타나지 않으므로, 논문 데이터 섹션에서 별도 명시 및 독자 검증 경로 필요.

7. **M2 외부 RankMe argmax 확인 항목 미기재** *(초안 누락)*
   STATUS.md 확정 항목에 "M2 external RankMe argmax = wg0 (already confirmed: 255.6 max) — write up"이 포함돼 있다. 이 수치가 TC2 FINDING의 외부 재현 근거이므로 논문 기술 범위 및 보고 위치(본문/보충) 결정 필요.

---

## 미해결 질문

1. **inverted-U의 C3 재현 가능성**: 내부 brain age n=494 단 하나의 inverted-U를 외부 6코호트에서 재현하지 못하면 TC2 FINDING이 "wg0.5 >> wg0"이라는 사실로만 축소되고 inverted-U 주장이 붕괴한다. `docs/01`도 "C3 외부서 inverted-U 미재현이면 경고 자체 붕괴 — 완료형 금지"를 직접 명시했다. C3 실험 ETA와 fallback 서사를 미리 준비해뒀는가?

2. **논문 Table 1의 최종 열 구성**: infarct 배제 결정이 내려진 이상, Table 1을 `{brain age r, polymicro AUROC(CI 추가), rankme}` 3열로 확정할 것인가, 아니면 polymicro도 CI 미비로 보조 텍스트로만 처리할 것인가?

3. **5점 그리드 wg0.25·wg0.75 결과 공개 범위**: 논문 본문에 5점 전체를 표로 노출할 것인가, 아니면 α-ReQ 아티팩트 설명에만 인용하고 wg0/0.5/1 3점 테이블을 메인 테이블로 쓸 것인가? 두 선택이 서로 다른 재현 증거 요건을 요구한다.

4. **BrainIAC 획득 여부**: STATUS.md에 "web/HF acquisition, else JEPA suffices"로 열려 있다. AAAI 데드라인 대비 BrainIAC 시도에 투자할 시간이 있는가, 아니면 BrainJEPA 단일 외부 baseline으로 확정할 것인가?

5. **`docs/10` FastSurfer ROI dense-seg pre-registration 상태**: STATUS.md에 "provisional/BLOCKED on disk"로 표시됐다. 이 블록이 TC2 구원 경로(off-center optimum 재주장)의 critical path인가, 아니면 AAAI 제출 범위 밖으로 완전히 deferred됐는가?


</details>
