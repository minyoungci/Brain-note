# 일일 종합 — 2026-06-28 (KST)

> LLM 자동 종합(gen→critic→synth). **검토용 초안이며 미검증 주장이 있을 수 있음.**
> 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` synth=`claude-opus-4-8` · 활동 WS 1개

## 오늘 한 줄 요약

minyoung2가 R3 segmentation을 마무리(trigeminal Dice 0.445/NSD 0.804, meningioma 0.159)하고 7-task downstream 파이프라인을 **순수 신규 2,295줄**로 대량 구축했으나, 핵심 구현 파일 다수가 diff 상한으로 미검토 상태이며 제출 컨테이너(S6)는 미착수다. **오늘 검증된 분석은 minyoung2 단일 워크스페이스뿐** — 따라서 본 문서의 "cross-workspace 종합"은 구조상 제한적이며, 아래 비교/시너지 섹션은 그 한계를 명시한다.

---

## 워크스페이스별 핵심

| 워크스페이스 | 오늘 핵심 |
|---|---|
| **minyoung2** | R3 seg 개선 완료(trigeminal `clDice+EMA`로 R1 대비 +0.032/+0.018, meningioma `Tversky β0.8+EMA`로 +25%). Hausdorff 경계 실험은 18h 초과로 중단. 7-task downstream 스캐폴드·v2(JEPA) 스캐폴드·foundation↔downstream 불일치 문서 신규 커밋. |
| minyoung4 / minyoung3 / plant / minyoungi | **오늘 검증된 일일 분석 없음.** 이 워크스페이스들에 대해 내가 가진 것은 2026-06-07자(3주 전) `OPEN_QUESTIONS.md` 트래커 항목뿐이며, 이는 오늘 상태가 아니다. 새로운 종합 근거 없음. |

> [불확실] OPEN_QUESTIONS.md가 추적하는 연구(치매 코호트 KDRC/OASIS/NACC/AIBL/ADNI, ComBat, ROI 정량)와 오늘 minyoung2 작업(seg 챌린지 제출, NSD/Dice, B200, 리더보드)은 **주제가 서로 다른 라인으로 보인다.** 트래커의 "minyoung2 3D CNN(IMG-020/022)" 항목과 오늘의 seg 작업이 동일 워크스페이스의 다른 시기 작업인지, 트래커가 단순 stale인지 확인 필요.

---

## 주목·결정 필요 (우선순위순)

**1. `reverse_preprocessing` 미구현 상태로 eval_seg 운용 — 헤드라인 수치 자체의 타당성 위협** 🔴
`docs/07` 불일치 6번이 "다음 구현에서 추가해야 한다"고 명시. 만약 metric이 원본 공간이 아닌 전처리 공간에서 계산되고 있다면, 오늘의 자랑인 **R1~R3 비교 수치(0.445 등)가 잘못된 공간에서 산출됐을 수 있다.**
→ *당신(일일 분석)이 놓쳤을 가능성:* 이 항목을 결정 테이블 **6번**으로 낮게 뒀는데, 이건 정밀화 이슈가 아니라 **"R3 개선" 서사 전체를 무효화할 수 있는 validity 위협**이다. 우선순위를 최상위로 올려야 한다. ([불확실] — 실제 eval_seg가 어느 공간에서 metric을 내는지 코드 확인 필요)

**2. S6 제출 컨테이너 미착수 + B200 GPU 미테스트** 🔴
SCRATCHPAD "다음=S6 제출컨테이너"이나 이번 커밋에 컨테이너 파일 없음. 외부 데드라인이 걸린 유일한 항목이며, 베이스 이미지·B200 미검증·7-task 추론 통합이 모두 잔여. **잔여 작업량 대비 데드라인 여유가 종합 문서 어디에도 정량화돼 있지 않다** — 가장 먼저 일정 추정이 필요.

**3. meningioma 0.159가 제출 최종값인지 결정** 🟠
seg 가중 50% 가정 시 리더보드 영향 큼. "멀티모달 learned-fusion 필요"로 한계는 인정됐으나 R4 실행 계획이 없다. **포기/추가실험을 명시적으로 결정**해야 한다(매달려 있는 상태가 가장 나쁨).

**4. `strict=False` + `assert` 로드 footgun (C1)** 🟠
`assert`는 `python -O`/pytest 기본 실행에서 비활성 → partial load가 무음 통과 → 평가가 잘못된 가중치로 돌 수 있다. 문서·SCRATCHPAD가 strict 정책을 상충 기술. `if missing: raise`로 교체 필요.

**5. 순수 신규 2,295줄, 테스트 실행 흔적 없음** 🟠
삭제 0줄 / 신규 26파일은 **검증 안 된 표면이 한 번에 크게 늘었다**는 뜻이다.
→ *당신이 놓쳤을 가능성:* 일일 분석은 개별 파일 미검토(C4~C9)는 짚었으나, **새 7-task 파이프라인에 대해 단위/스모크 테스트가 돌았는지 자체를 묻지 않았다.** 큰 신규 코드의 첫 질문은 "돌아가긴 하는가"여야 한다.

**6. WaveB/Hausdorff 추적 단절 (C3+C7)** 🟡
`run_r3_waveB.sh +32`는 커밋됐으나 `R3_WAVEB_SUMMARY.md` 부재, COMPARISON.md에 중단 기록 여부 불명. WaveB=중단된 boundary 실험으로 강하게 추정됨. 미기록 시 후속자가 "boundary 미시도"로 오해.

**7. Task4 multiclass(`{0,1,2}`) 처리 미완 (C5)** 🟡
불일치 7번 문서·코드 양쪽이 잘려 softmax 3ch vs sigmoid 2ch, 제출 라벨 포맷이 validator 요건과 맞는지 확인 불가.

---

## 코드 리뷰 종합

- **심각(1건) — C1 strict 로드 footgun.** `strict=False`+`assert not missing` 조합 + 문서/SCRATCHPAD 정책 상충. 이것 하나가 평가 무결성을 조용히 깨뜨릴 수 있는 유일한 "심각" 후보. 단 `core.py` 패치가 diff에 없어 [불확실] — 실제 구현이 `strict=True`면 해소.

- **반복 패턴 — 리뷰 커버리지 자체의 공백(메타 결함).** C4·C5·C6·C7·C8·C9 **6건이 모두 "30000B diff 상한으로 미검토"** 다. 즉 신규 핵심 파일(`seg_v3.py +234`, `models.py +18`, `foundation_model_design.md +243`, COMPARISON.md +43, bakeoff eval +52)의 큰 비중이 리뷰되지 않았다. 오늘 리뷰는 결함을 찾았다기보다 **"리뷰가 끝나지 않았다"**는 신호 — 파일을 직접 열어 보는 후속 패스가 반드시 필요.

- **재현성/추적성 위생.** C1(정책 상충)·C3·C7(WaveB·Hausdorff 결과 미기록)·C5(문서 잘림)이 한 묶음 — 실험은 돌았으나 **기록의 연속성이 끊긴** 패턴이 반복된다.

- **긍정(인정할 점).** C2와 Q6에서 EMA 오염 주장을 "근거부족"으로 자기 철회한 것은 적절하다. R1/R2는 EMA 미사용 코드(`seg_v2.py`, `eval_finetune.py`) 기반이므로 철회가 맞다 — **이 결정을 다시 들추지 않는다.**

---

## 미해결 질문 (연속 추적)

**전제:** 어제 종합이 없으므로(`없음`) 엄밀한 day-over-day delta는 불가. 비교 가능한 유일한 기준선은 `OPEN_QUESTIONS.md`(2026-06-07, **3주 전 stale**)뿐이다.

**신규(오늘 minyoung2에서 발생, 트래커에 없던 것):**
1. eval_seg가 `reverse_preprocessing` 없이 **원본 공간 정합을 보장하는가?** → R1~R3 수치 타당성 직결.
2. WaveB = Hausdorff 18h 중단 건과 동일한가? COMPARISON.md에 중단 사유가 기록됐는가?
3. `downstream/core.py`의 실제 로드는 `strict=True`인가 `strict=False+assert`인가? (문서 상충)
4. Task4 multiclass 출력을 seg_v3가 어떻게 처리하는가(채널 수/argmax/제출 포맷)?
5. S6 제출 컨테이너 착수 일정·잔여 작업량은? (외부 데드라인 유일)
6. meningioma learned-fusion은 R4로 가는가, 포기하는가?

**해소된 항목:** 없음. 오늘 작업은 새 코드를 추가했을 뿐 기존 트래커 항목(F10 통계, ComBat 누수, NACC 결측코드 등)을 해소하지 않았다.

**stale 경고:** 트래커 🟠 1번 "minyoung3·plant git 부재 / minyoung4 미커밋"은 2026-06-07 기준. minyoung2는 오늘 정상 커밋 중이나, **나머지 워크스페이스의 git 안전망 상태는 3주째 미확인** — 오늘 검증 범위 밖이라 갱신 불가.

---

## 워크스페이스 간 연결·시너지

**단일 워크스페이스(minyoung2)만 오늘 분석이 있으므로 진정한 cross-workspace 시너지는 도출 불가.** 다음 두 가지만 [불확실]로 기록한다:

1. **트래커와 오늘 작업의 단절.** `OPEN_QUESTIONS.md`는 5개 워크스페이스 + 치매 코호트 연구를 추적하지만, 오늘 minyoung2의 seg 챌린지 제출 작업과 그 트래커 항목 사이에 직접 연결이 보이지 않는다. **두 연구 라인이 별개이거나 트래커가 stale** — 종합의 의미가 살려면 이 관계를 먼저 정리해야 한다.

2. **재사용 가능 자산 후보.** 오늘 구축된 7-task downstream 파이프라인(`core.py`, `eval_*.py`)과 foundation↔downstream 불일치 문서는, 만약 다른 워크스페이스가 동일 foundation 모델을 쓴다면 공유 자산이 될 수 있다. 다만 다른 워크스페이스의 오늘 상태를 모르므로 **시너지 주장은 보류** — 내일 다른 워크스페이스 분석이 들어오면 가장 먼저 확인할 연결점.

---

> **종합가 메모:** 오늘은 "여러 워크스페이스 종합"을 요청받았으나 실제 입력은 minyoung2 하나였다. 이 불일치 자체가 가장 먼저 확인할 사항 — 나머지 워크스페이스 분석이 누락된 것인지, 오늘 minyoung2만 활동한 것인지 확인되면 다음 종합의 정확도가 크게 올라간다.

---
<details><summary>워크스페이스별 검증 분석 (펼치기)</summary>

# === minyoung2 ===
## 진행 요약

**R3 seg 개선 완료**: trigeminal `clDice+EMA` → Dice 0.445/NSD 0.804 (R1 대비 +0.032/+0.018), meningioma `Tversky β0.8+EMA` → 0.159 (R1 대비 +25%). Boundary(Hausdorff CPU) 실험은 18h 초과로 중단. 동시에 7-task downstream 파이프라인 신규 구축(`core.py`, `eval_*.py`, `seg_v2/v3.py`, 오버나잇 쉘 스크립트 다수)과 foundation↔downstream 불일치 정리 문서(`docs/07_…`), v2(JEPA) 스캐폴드 커밋이 이루어짐. 총 26파일·2295줄 신규 삽입, 삭제 라인 0줄(순수 신규).

---

## 코드 리뷰 (diff 기반)

### 🔴 C1 — `strict=False` + Python `assert` 조합의 footgun, 그리고 문서·SCRATCHPAD 불일치
**`docs/07_downstream_integration_mismatch.md`** (diff 내 코드 블록)

```python
model.load_state_dict(sd["student"], strict=False)
assert not missing
assert not unexpected
```

문서는 이를 "현재 구현 원칙"으로 제시한다. 그런데 같은 커밋의 SCRATCHPAD는 `downstream/core.py`에 대해 **"wg0.5 strict 로드(부분로드 footgun 차단)"** 으로 기술한다. 두 설명이 상충한다. `strict=False`를 쓰면서 `assert`로 막는 패턴은 `python -O` 또는 pytest 기본 실행 시 assert가 비활성화되어 partial load가 무음 통과될 수 있다. `if missing: raise RuntimeError(...)` 형태가 필요하다. `core.py` 패치가 diff에 포함되지 않아 실제 구현 확인은 불가. **[불확실 — `core.py` 구현 확인 필요, 단 문서·SCRATCHPAD 불일치 자체는 diff에서 확인됨]**

### 🟡 C2 — EMA bias correction이 R1/R2 Δ 수치에 미치는 영향 없음 [근거부족]

~~"수정 이전에 완료된 R1·R2 Δ 수치(예: trigeminal Δ+0.249, infarct Δ+0.346)가 부풀려진 값일 수 있다"~~

**[근거부족]** — EMA는 `seg_v3.py`에 신규 추가된 기능이다. SCRATCHPAD의 EMA zero-init 버그 적발·수정 항목은 명시적으로 R3 code audit 맥락(June 26, `seg_v3.py` 개발 중)에 위치하며, 수정은 R3 실험 착수 전에 완료됐다. R1/R2 결과(trigeminal Δ+0.249, infarct Δ+0.346)는 `seg_v2.py`와 `eval_finetune.py`에서 도출된 것으로, 두 스크립트에는 EMA 기능 자체가 존재하지 않는다. 따라서 R1/R2 Δ 수치의 EMA 오염 가능성은 diff·SCRATCHPAD 어느 쪽으로도 뒷받침되지 않는다.

### 🟡 C3 — WaveB 요약 파일 부재, Hausdorff 중단과의 연관성
**diff stat**: `run_r3_waveB.sh +32`, `R3_WAVEA_SUMMARY.md +5`, `R3_WAVEC_SUMMARY.md +5` — `R3_WAVEB_SUMMARY.md` 없음.

WaveB는 SCRATCHPAD에서 "clean clDice 핸디캡제거 + **clDice+boundary NSD**" 실험으로 계획됐고, R3 완료 엔트리는 "boundary(Hausdorff CPU 18h 미완)→중단"을 기록한다. WaveB = Hausdorff 경계 실험 중단 건일 가능성이 높다. 스크립트는 커밋됐으나 결과 요약이 없고, 연속성이 끊겨 있다.

### 🟡 C4 — `downstream/seg_v3.py` 내용 미검토
**`downstream/seg_v3.py:1-234`** (전체 신규, +234줄)

diff 패치 30000B 상한으로 이 파일에 도달하지 못했다. `_ClDiceWrap`(sigmoid+2채널 one-hot+순서 수정), TTA 채널 순서, EMA param/buffer zero-init, LLRD 구현이 실제 포함되어 있는지 확인 불가.

### 🟡 C5 — `docs/07_downstream_integration_mismatch.md` 불일치 7번 미완성
**`docs/07_downstream_integration_mismatch.md` 마지막 줄**

```
## 7. 불일치 7: Task4는 binary가 아니라 multiclass
```

diff가 제목에서 잘렸다. Task4 `{0,1,2}` 멀티클래스 처리(출력 채널 수, softmax 3ch vs sigmoid 2ch, argmax 논리, 제출 라벨 포맷)가 문서·코드 양쪽에서 완성됐는지 확인 불가.

### 🟡 C6 — `pretrain/models.py` +18줄 내용 불명
**diff stat**: `pretrain/models.py | 18 +` — 패치 범위 밖. 커밋 메시지(`[MODEL]`)가 이 파일을 포함하나 `forward_multimodal`, `forward_masked`, 또는 다른 경로 중 어느 변경인지 특정 불가.

### 🔵 C7 — Hausdorff 중단이 COMPARISON.md에 명시 여부 불명
**`experiments/phase_b/downstream_runs/COMPARISON.md` +43줄** — 내용이 패치에 없다. "boundary 시도→18h 미완→중단" 이 명시적으로 기록됐는지 알 수 없다. 미기록 시, 나중에 보는 사람이 "boundary 미시도"로 오해할 수 있다. **[불확실]**

### 🔵 C8 (신규) — `pretrain/run_bakeoff_eval.sh` +52줄 목적 불명
**diff stat**: `pretrain/run_bakeoff_eval.sh | 52 +` — 체크포인트 선정(3-variant bakeoff) eval 스크립트로 추정되나 이번 커밋의 SCRATCHPAD 엔트리에서 명시적 언급이 없다. 이미 완료된 실험의 재현 스크립트인지, 아직 쓰이지 않은 스크립트인지 확인 불가.

### 🔵 C9 (신규) — `docs/foundation_model_design.md` +243줄 미검토
**diff stat**: `docs/foundation_model_design.md | 243 +` — SCRATCHPAD에서 "blog 문서 `docs/foundation_model_design.md`"로만 언급된다. 243줄 신규 문서이나 초안 리뷰에서 전혀 다뤄지지 않았다. 내용·정확성 검토 없음.

---

## 놓친 것·결정 필요

| # | 항목 | 근거 |
|---|------|------|
| 1 | **WaveB 결과 부재 (Hausdorff 중단일 가능성)** | `run_r3_waveB.sh +32` 커밋됐으나 `R3_WAVEB_SUMMARY.md` 없음. SCRATCHPAD WaveB 계획이 "clDice+boundary NSD"이고 R3 완료 엔트리가 "boundary 18h 미완→중단"을 기록 — WaveB = 중단된 boundary 실험으로 강하게 추정됨. 명시 필요 |
| 2 | **meningioma 0.159가 제출 최종값인지 결정 미완** | SCRATCHPAD "R3 종료" 선언. scratch 0.107 대비 Δ+0.052. "멀티모달 learned-fusion 필요"로 한계 인정됐으나 실행 계획 없음. seg 가중치 50% 고려 시 리더보드 영향 큼 |
| 3 | **S6 제출 컨테이너 미착수** | SCRATCHPAD "다음=S6 제출컨테이너". 이번 커밋에 컨테이너 관련 파일 없음. 베이스 이미지(`pytorch:2.11.0-cuda12.6`)·B200 GPU 미테스트 이슈 인지된 상태 |
| 4 | **v2(JEPA) 스캐폴드 착수 시점·우선순위 미결정** | `pretrain_v2/README.md +45줄`, 커밋 `d265b4e`. v1 보존 원칙 명시됐으나 v2 착수 시점·GPU 할당 결정 없음 |
| 5 | **Task4 multiclass 처리 미완성** | 불일치 7번 문서·코드 양쪽 잘림. `{0,1,2}` 라벨을 seg_v3가 어떻게 처리하는지(softmax 3ch vs sigmoid 2ch) 확인 불가 |
| 6 | **`reverse_preprocessing` 미구현 상태로 eval_seg 운용 중** | `docs/07` 불일치 6번이 "다음 구현에서 추가해야 한다"고 명시. 현재 eval_seg 결과가 원본 공간 불일치 채로 metric을 내는지 확인 필요 |
| 7 | **`pretrain/run_bakeoff_eval.sh` 목적 불명** | diff stat에만 존재(+52줄). 이번 커밋 SCRATCHPAD 엔트리에서 미언급. 재현용인지 미사용인지 불명 |

---

## 미해결 질문

1. **WaveB는 Hausdorff 경계 실험(18h 중단)과 동일한 건인가?** 스크립트가 커밋됐고 계획에 "clDice+boundary NSD"가 포함됐다면, WaveB가 중단 사유와 함께 COMPARISON.md에 기록됐는가?

2. **Task4 multiclass(`{0,1,2}`) 출력을 seg_v3가 어떻게 처리하는가?** 채널 수, argmax 논리, 제출 라벨 포맷이 validator 요건과 맞는가?

3. **`downstream/core.py`의 실제 load 구현은 `strict=True`인가, `strict=False+assert`인가?** 문서와 SCRATCHPAD가 상충하므로 코드 직접 확인 필요.

4. **`reverse_preprocessing` 없이 돌고 있는 eval_seg 결과는 원본 공간 정합이 보장되는가?** 현재 COMPARISON.md에 기록된 R1~R3 수치가 올바른 공간에서 계산됐는지 확인 필요.

5. **S6 제출 컨테이너 착수 목표 일정은?** 베이스 이미지 결정, B200 GPU 미테스트 이슈, 7-task 추론 코드 통합까지 잔여 작업량과 데드라인 여유가 어느 정도인가?

6. **meningioma 개선을 위한 "멀티모달 learned-fusion"은 R4로 가는가, 포기하는가?** 0.159 절대값과 seg 50% 가중치를 고려하면 리더보드 영향이 크다. ~~EMA bias correction 전 R1 Δ 수치 재측정 질문은 삭제 — R1은 EMA 미사용 코드(`seg_v2.py`, `eval_finetune.py`) 기반이므로 해당 없음.~~


</details>
