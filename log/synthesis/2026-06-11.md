# 일일 종합 — 2026-06-11 (KST)

> LLM 자동 종합(gen→critic→synth). **검토용 초안이며 미검증 주장이 있을 수 있음.**
> 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` synth=`claude-opus-4-8` · 활동 WS 3개

## 오늘 한 줄 요약

세 워크스페이스가 같은 날(06-11) 동시에 주제를 갈아엎었는데 — minyoung4는 "구조 T1w로 AD/aging은 method-novelty 공간이 없다"며 **사망 선고**(de-confounding 3회 실패, morphometry ceiling+site=population)를 closure로 영구화한 반면, minyoung2·plant는 바로 그 **구조 T1w 표현학습으로 신규 베팅**을 걸었다 — 즉 한 워크스페이스가 닫은 문으로 두 워크스페이스가 들어갔고, 그 두 베팅의 핵심 수치는 모두 **leakage 의심 split** 위에 서 있다.

## 워크스페이스별 핵심

- **minyoung2** — 구 debiasing/adapter phase 청산 후 "ROI-token SSL for Structural Brain MRI"로 전환, R0–R4 게이트 당일 전부 통과 선언 + 13K SSL 확장 착수(98fffe5). 핵심 주장 "ROI-token이 whole-volume SSL·hand-crafted 둘 다 이김(CDR-SB +0.10~0.15)"이나 **whole-volume 기준 수치 부재 + 3180 pretrain/eval split 미명기**.
- **minyoung4** — 전량 문서 커밋(closure 2개). 실험 코드·체크포인트(22+15GB) reset 삭제, 수치만 보존. PET-distill·GRL de-siting·BrainIAC frozen-probe 4방향 kill 게이트 확정. de-confounding 실패를 minyoung2까지 포함한 **cross-workspace 패턴(F5)**으로 기록.
- **plant** — `microbrain`(bias-robust T1w micro-표현)으로 재정의. P0 audit A0–A5 완료("bias 실재+분리가능"), P2 Stage-0 smoke 실행(GPU5, 1 seed, held-OASIS 0.583 < morph bar 0.72, "파이프라인 검증, 결정 아님"). 주제는 **morphometry 약한 regime(MCI/amyloid/preclinical)**에서의 micro-표현 기여로 수렴.

## 주목·결정 필요 (우선순위순)

**1. 🔴 구조 T1w 역설 — minyoung4의 사망 선고가 minyoung2의 신규 베팅을 이미 반증했는가?**
이것이 오늘의 최고 레버리지 결정이다. minyoung4는 "구조 T1w로 AD를 푸는 질문 자체가 공간 없음"을 4방향 측정으로 확정했는데, minyoung2는 같은 날 그 공간으로 들어가 CDR-SB(=dementia severity, **morphometry가 가장 강한 영역**)를 타깃으로 "ROI-token이 이긴다"고 주장한다. minyoung2의 thesis("micro가 morpho를 넘는다")가 가장 약해지는 지점이 정확히 CDR-SB다. 반면 plant는 morphometry가 **약한** regime을 명시적으로 겨냥하므로 minyoung4 findings를 (의식했든 아니든) 회피하는 더 방어 가능한 베팅이다. → **minyoung2가 minyoung4 closure를 인지하고 설계했는지, CDR-SB 타깃이 morpho-ceiling을 어떻게 우회하는지 명시 필요.**
*내가 놓쳤을 가능성*: 세 pivot이 한 연구자(Min)의 조율된 자원 재배치인지 독립 에이전트의 우연인지 확인 불가 [불확실]. 조율됐다면 minyoung4의 죽음→minyoung2/plant 베팅이 의도된 분업일 수 있고, 그렇다면 "역설"이 아니라 "minyoung4 findings를 입력으로 받은 정제"일 수 있다. 그러나 인계 문서가 informal(F5 한 줄)뿐이라 입력 전파가 확인되지 않는다.

**2. 🔴 leakage가 세 워크스페이스 공통 단일 실패 모드 — "SSL이 이긴다"류 주장 전부 보류.**
minyoung2(3180 pretrain=downstream 라벨 집합 동일, test 피험자 representation이 SSL에 노출 가능), minyoung4(BrainIAC가 ADNI/OASIS 사전학습 포함 추정), plant(A4 잔차화 train/test fit 일관성·held-OASIS 조기종료 사용 여부 patch 절단으로 미확인) — 세 곳 모두 핵심 수치가 leakage 의심 위에 있다. 어제 OPEN_QUESTIONS의 "ComBat/within-cohort 누수"가 이제 **워크스페이스 횡단 보편 주제**로 승격됐다. → split 구조를 SPEC/문서에 명기하기 전까지 minyoung2 R3/R4 수치, plant 0.583, minyoung4 BrainIAC 비교를 확정 인용 금지.

**3. 🔴 plant GPU 승인 게이트 위반 가능성 — 거버넌스 사안.**
Stage-0 smoke가 GPU5에서 실행됐는데(SCRATCHPAD 명시) CLAUDE.md Confirmation Gates는 "GPU 스크립트=Min 승인 후"를 규정하고 DECISION_LOG·커밋 어디에도 승인 기록이 없다. 수치 신뢰도가 아니라 **프로세스 규율** 문제이며, 같은 패턴(문서 vs 실행 불일치)이 P0_bias_audit_plan.md "설계안·승인 전 금지" 텍스트가 audit 완료 후에도 미갱신된 채 남은 것과 겹친다. → 사후 승인 기록이라도 남길지 결정 필요.

**4. 🟠 minyoung4 closure 수치가 영구 인용원인데 재현 불가 + 내부 수치 불일치.**
22+15GB 코드·체크포인트가 reset 삭제돼 두 closure 문서가 유일 기록인데, ① 서버 백업 존재 여부 미언급 [불확실], ② CN/AD AUC가 문서 간 0.881(LOCO 6코호트 평균) vs 0.911(KDRC-CV)로 불일치하며 §1.4 수렴 테이블에서 "morpho 0.91"로 혼재. negative/audit 논문으로 인용될 경우 reviewer 첫 공격 포인트. → 백업 확인 + 두 수치의 프로토콜 차이를 문서에 명시.
*내가 놓쳤을 가능성*: closure가 "죽은 방향"이라 검토 우선순위가 낮아 보이지만, minyoung4가 F5에서 minyoung2의 de-confounding 실패까지 기록한 이상 이 수치들은 **minyoung2/plant 신규 베팅의 baseline 근거**로 역수입될 수 있다 — 재현 불가 상태면 신규 베팅의 토대도 같이 불안정해진다.

## 코드 리뷰 종합 (반복·심각 결함)

- **🔴 leakage 미증명 (3/3 워크스페이스)** — pretrain=eval 집합 동일(minyoung2), foundation model 사전학습 코호트 오염(minyoung4 BrainIAC), 잔차화·held-out 규율 미확인(plant). 단일 가장 심각한 횡단 결함.
- **🟡 baseline 정의·수치 부재로 delta 주장 검증 불가 (2/3)** — minyoung2 whole-volume SSL의 method·절대 수치 없이 "+0.10~0.15"만, minyoung4 BrainIAC 비교의 leakage-clean 경계(AJU+KDRC only vs 7코호트) 불명. 비교 대상이 안 보이면 우위 주장은 검증 불능.
- **🟡 검증·재현 인프라 결손 (3/3)** — plant 전체 repo 테스트 0건(P1 게이트 위반인 채 P2 실행), minyoung2 multi-seed 선언했으나 수치는 단일 점추정, minyoung4 코드 삭제로 재현 원천 소실.
- **🟡 문서 vs 실행 불일치 (3/3)** — minyoung2 SPEC "R0-R4 완료" vs 13K 미완, plant "설계안·승인 전 금지" 문서가 audit 완료 후 미갱신, minyoung4 closure 간 상호참조 링크 깨짐(FAILED_3D_CNAD 출처 불명).
- **🟡 주장 범위 과잉 (2/3)** — minyoung2 "전 코호트"(실제 3코호트, OASIS 누락), plant "D5 폐기"(실제 AD 분류 한정 반증, MCI/amyloid 미검증). 둘 다 문서 내부 자기모순.

## 미해결 질문 (어제 대비 신규/해소)

**🆕 신규 (오늘 발생)**
1. minyoung2 R2–R4 pretrain/test split 분리 여부 — 3180 전체 pretrain인가, test 제외인가? (leakage 확정 키)
2. minyoung2 whole-volume SSL baseline method·코호트별 절대 수치 — apples-to-apples인가?
3. minyoung2 13K pretrain 결과 평가 시점 — 현 3180 수치를 대체/추가? "3180-pretrain 잠정" 표기 필요.
4. minyoung4 `clin_dx_label` subject-고정 버그가 LOCO **단면** CN/AD 분류(0.88–0.91)에도 영향을 주는가, longitudinal만 무효화하는가?
5. minyoung4 삭제 37GB의 서버 백업 존재 여부.
6. plant Stage-0 smoke의 실제 target(CDR≥0.5 / CN/AD / amyloid) — DECISION_LOG 미기록.
7. plant GPU5 실행 사전 승인 여부.

**✅ 해소·무효화 (pivot으로 orphan됨 — OPEN_QUESTIONS.md 정리 필요)**
- minyoung2 3D CNN(IMG-020/022) 결과, P1 null "privileged 死 vs adapter 학습 실패" 분리 — **주제 전환으로 무효**.
- plant converter 통계력(~270 converter), Cox feature 컬럼 충돌(APOE/sex) — **microbrain 재정의로 longitudinal 폐기, 무효**.
- minyoung4 G0 feature coverage(1,857 subj ROI mask) — **closure로 무효**.
> ⚠️ 어제 추적하던 5개 🔴/🟠 항목 중 최소 3개가 오늘 pivot으로 orphan됐다. OPEN_QUESTIONS.md를 갱신하지 않으면 죽은 질문을 계속 추적하게 된다.

**🔁 연속 (여전히 open, 오늘 중요도 상승)**
- **OASIS 진단 삼중값 재현 불가** — 오늘 이중으로 관련: minyoung2가 OASIS를 downstream에서 누락(이유 미기재), minyoung4 BrainIAC가 OASIS leakage 의심. 데이터 신뢰의 공통 분모.
- **ComBat/LOCO 누수** → 위 주목 #2로 승격.
- **KDRC 라벨 권위 모순** — minyoung2 CDR-SB 채택, plant target 미확정 모두 이 라벨 출처에 의존하나 미해소.

## 워크스페이스 간 연결·시너지

- **minyoung4 → minyoung2/plant 입력 전파 누락.** minyoung4는 whole-T1w 계열 SSL/probe의 실패를 측정으로 확정했고, minyoung2는 지금 whole-volume SSL을 baseline으로 비교하고 있다 — minyoung2가 비교하는 whole-volume이 minyoung4가 이미 죽인 것과 같은 계열일 가능성이 높다. minyoung4 closure 수치를 minyoung2의 whole-volume baseline 정의에 직접 인용하면 중복 실험을 절약하고 baseline 공백(주목 #4)을 메울 수 있다.
- **plant가 minyoung4 findings의 유일한 정합 후계.** plant만 "morphometry 약한 regime"을 명시적으로 겨냥 — minyoung4의 morpho-ceiling 결론을 우회 설계로 흡수한 형태. plant의 P0 audit(voxel→site 0.475, 잔차화 후 disease drop −0.05)은 minyoung4의 site=population 결론과 같은 현상을 독립 측정한 것으로, **두 측정의 교차 검증**으로 묶을 가치가 있다.
- **leakage 규율을 공통 게이트로 표준화.** 세 곳이 같은 함정에 빠져 있으므로, "pretrain/eval split 명기 + LOCO + train-fit 잔차화"를 워크스페이스 횡단 invariant 단위테스트로 만들면 minyoung2·plant 양쪽의 핵심 주장을 동시에 방어할 수 있다(plant의 P1 게이트가 이미 이를 요구하나 미충족).
- **어제의 병목(minyoungi DICOM 변환·minyoung3 F04)이 오늘 분석에서 사라짐.** 두 데이터·인프라 워크스페이스가 오늘 비활동(또는 미분석)인데, minyoung2의 13K 확장·plant의 npy 캐시는 결국 그 데이터 파이프라인에 의존한다 — 신규 베팅 3개가 모두 같은 미확인 데이터 토대 위에 있을 위험이 어제와 동일하게 잠재 [불확실, minyoungi/3 오늘 상태 미확인].

---
<details><summary>워크스페이스별 검증 분석 (펼치기)</summary>

# === minyoung2 ===
## 진행 요약

오늘(2026-06-11) minyoung2는 이전 phase(debiasing/adapter, 실험 파일 8개 삭제 −1380줄)를 완전히 청산하고 **신규 주제 "95 DKT ROI-token SSL for Structural Brain MRI"로 전환**하였다. SPEC.md 전면 교체(−128줄 순감)를 포함해 R0–R4 게이트를 당일 전부 통과 선언하고, 마지막 커밋(98fffe5)에서 SSL을 13K로 확장 및 multi-seed 추가를 착수했다. 핵심 주장: **ROI-token SSL이 whole-volume SSL·hand-crafted ROI 둘 다 이김(CDR-SB, +0.10~0.15)** — 단, whole-volume baseline의 실제 수치는 SPEC에 미기재.

| 게이트 | 상태 | 비고 |
|---|:---:|---|
| R0 ROI 캐시 | ✅ | 3180 정렬 검증 포함; ROI feature 190d |
| R1 amyloid 폐기 / R1b CDR-SB 채택 | ✅ | CN-stratified 역전 확인; `r1_feasibility.py` / `r1b_task.py` 신규 추가 확인 |
| R2 roitoken.py SSL pretrain | ✅ | 3180 기반, L1 roi 0.467 / whole 0.716 |
| R3 downstream Ridge | ✅ | ROI-token > hand-crafted; **AJU/KDRC/ADNI 3개 코호트만** — OASIS 결과 없음 |
| R4 핵심 ablation | ✅(reviewer 평가: 잠정) | ROI-token > whole-volume +0.10~0.15; whole-volume 기준값 미기재 |
| 13K SSL 확장 | 🔄 | 98fffe5에서 `cache_img13k.py` + `roitoken --data 13k` 착수, 결과 미기재 |

---

## 코드 리뷰 (diff 기반)

### 🔴 데이터 누수 위험 — `SPEC.md §5 R2`

R2 SSL pretrain이 **downstream 라벨 집합과 동일한 3180 subjects 전체**에서 수행됨("pretrain(3180)"). R3/R4 Ridge 평가가 이 3180의 분할로 이루어진다면, test split 피험자의 representation이 SSL pretraining에서 이미 노출됨 → downstream 수치 과대추정 가능. **현재 SPEC diff에서 pretrain/eval split 분리 여부를 확인할 수 없음.** 13K 확장(98fffe5)이 구조적으로 해소할 수 있으나 현재 보고된 R3/R4 핵심 수치(AJU 0.478/KDRC 0.415/ADNI 0.533)는 여전히 3180-pretrain 기반이며 13K 결과는 없다. R2–R4 수치를 확정 인용하기 전에 split 구조를 SPEC에 명기해야 한다.

### 🟡 whole-volume SSL baseline 정의 및 수치 불명확 — `SPEC.md §5 R4`

"ROI-token > whole-volume 전 코호트(+0.10~0.15)"에서 ① whole-volume SSL의 구체적 method(Models Genesis? SimMIM? 자체 voxel-MAE?)가 미기재이고, ② whole-volume의 **실제 절대 수치**도 없다. delta(+0.10~0.15)만으로는 whole-volume이 hand-crafted(0.433/0.394/0.482)보다 높은지 낮은지조차 확인 불가. 동일 pretrain 예산·데이터에서 학습된 apples-to-apples 비교인지 불투명하다.

### 🟡 recon L1 수치 맥락 부재 — `SPEC.md §5 R2`

"recon L1 roi 0.467 / whole 0.716" — 스케일 기준(초기값·chance·normalized 여부)이 없어 "정상 학습" 판단 근거 불충분. whole이 roi보다 높은 것이 feature 차원 수 차이인지 학습 난이도 차이인지 불명.

### 🟡 중복 캐시 스크립트 — `cache_img.py (+57줄)` vs `cache_img13k.py (+65줄)`

두 파일이 별도로 추가됨은 diff stat으로 확인된다. 그러나 98fffe5 커밋 메시지가 `cache_img13k`를 "self-contained"라 칭하므로 양자의 차이가 단순히 manifest 경로·출력 크기뿐이라는 초안의 주장은 **[추정 — diff 패치에 두 파일 내용 없음]**. 향후 전처리 로직 수정 시 양쪽에 반영해야 하는 유지보수 리스크는 diff stat으로 확인 가능한 사실이다.

### 🟡 multi-seed CI 미기재 — `SPEC.md §5 R3/R4`

98fffe5 커밋 메시지에 "multi-seed downstream 추가"가 선언됐으나 SPEC §5 R3/R4 수치는 단일 점추정값(AJU 0.478, KDRC 0.415 등). 재현성 주장이 수치에 반영되지 않은 상태다.

### 🟠 CN-stratified CDR-SB 평가 누락 — `SPEC.md §7 risk 3`

SPEC §7에서 "CN-stratified·amyloid-balanced 평가 필수"를 직접 명시했으나 R3/R4 결과에 stratified 수치 없음. CDR-SB 신호는 severe 케이스에 집중되므로 전체 corr이 좋아도 CN 서브셋 성능이 논문 신뢰도를 좌우한다.

### 🟡 OASIS 코호트 downstream 결과 누락 — `SPEC.md §3, §5 R3/R4`

SPEC §3 Feasibility 표에는 OASIS row가 있고 "imaging이 covariate 압도(+0.20, corr 0.284)"가 기재됐다. 그러나 R3/R4 결과는 AJU/KDRC/ADNI 세 코호트만 제시한다. R4의 "전 코호트" 표현이 사실상 3개 코호트임에도 "전 코호트"로 기술된 점은 SPEC 내부 불일치다.

---

## 놓친 것·결정 필요

**1. SPEC "R0-R4 완료(✅)" 선언과 13K pretrain 미완료의 불일치**
커밋 순서상 aa8d594(SPEC R0-R4 완료 기입) → 98fffe5(13K 착수). SPEC에 완료로 표시된 R3/R4 핵심 수치는 13K pretrain 전 상태다. SSOT 원칙("생성·검증 분리")에 따르면 13K 결과가 달라질 경우 현 수치는 미확정으로 표기했어야 한다. **R3/R4 수치 옆에 "3180-pretrain 잠정" 표기 필요.**

**2. DAMT(ACCV2024) baseline 미실행**
SPEC §6 must-beat 목록에 DAMT가 명시됐으나 어느 커밋에도 DAMT 관련 실험 코드나 결과가 없다. R4 ablation을 "핵심 기여 입증"으로 선언하기 전 DAMT와의 직접 비교 없이는 ACCV 리뷰어의 핵심 공격 포인트가 된다.

**3. masked vs contrastive 항 ablation 미실행**
SPEC §5 "남은 것: ⑤ masked/contrastive 항 ablation". SPEC §4.2가 두 pretext task를 정의하고 roitoken.py가 구현을 담당하나(+200줄, 실제 코드는 패치 미수록), 각 항 기여를 분리하는 실험 결과가 없다. 설계 기여 방어에 필수다.

**4. fine-tune 결과 부재**
R3/R4 전체가 frozen embed→Ridge(linear-probe). SPEC §5 "남은 것: ④ fine-tune". "ROI-token SSL이 이김"이라는 주장의 범위가 linear-probe에만 국한되는지, fine-tune에서도 유지되는지 미검증이다.

**5. SPEC §8 마지막 문장 잘림**
diff 패치 끝 "skull-strip shortcut(P" — 30000B 제한으로 원문이 절단됐다. risk 내용이 불완전하게 남아 있는 상태다.

**6. R4 "전 코호트" 주장과 OASIS 결과 누락 불일치**
R3 결과에 OASIS가 없음에도(AJU/KDRC/ADNI만) R4가 "전 코호트"라 표현한다. SPEC §2 downstream 설명 역시 "AJU 1000 / KDRC 534"만 명시한다. ADNI를 downstream으로 포함한 근거와 OASIS 제외 이유가 SPEC에 기재되지 않았다. "전 코호트" 표현을 실제 범위에 맞게 수정해야 한다.

---

## 미해결 질문

1. **R2–R4 SSL pretrain의 train/test split 구조는?** — 3180 전체를 pretrain에 쓰고 그 subset을 test로 사용했는가, 아니면 test를 pretrain에서 제외했는가? (데이터 누수 여부 결정)

2. **R4 whole-volume SSL baseline은 어떤 method이며 절대 수치는?** — 동일 pretrain 예산·데이터에서 비교한 apples-to-apples인가? whole-volume baseline의 코호트별 corr 수치 없이는 delta(+0.10~0.15) 주장 검증 불가.

3. **13K pretrain 결과는 언제 평가할 예정인가?** — 현 R3/R4 수치(3180 기반)를 대체하는가, 추가 R5 게이트인가? SPEC §5 업데이트 시점과 "3180-pretrain 잠정" 표기 필요.

4. **masked-region modeling에서 복원 target은 feature인가 voxel인가?** — SPEC §4.2가 "feature 또는 voxel"로 미결 상태를 그대로 기재. 두 선택이 method 서사("ROI-as-unit SSL")와 재현성에 어떻게 영향을 주는지 SPEC에 확정 기재 필요.

5. **DAMT 재구현·비교 계획이 있는가?** — 없다면 ACCV 제출 전 이 baseline 없이 리뷰를 통과할 전략이 있는가?

6. **OASIS 코호트를 R3/R4 downstream에서 제외한 이유는?** — SPEC §3에 OASIS feasibility 수치(imaging +0.20)가 있으며 downstream 라벨도 완비(§2 구 SPEC 3180 중 443명). 제외 근거가 없다면 "전 코호트" 주장 범위 수정 또는 OASIS 결과 추가가 필요하다.

# === minyoung4 ===
## 진행 요약

오늘 minyoung4 워크스페이스의 작업은 **전량 문서 커밋**이었다. 코드·실험 결과물은 이미 reset으로 삭제된 상태이며, 두 개의 closure 문서를 통해 당일 탐색 종료 근거(측정값 포함)를 영구 보존하고 워크스페이스를 clean slate로 정리했다.

- **`FAILED_DECONFOUNDING_EXPLORATION_CLOSURE_20260611_KO.md`**: PET-distill de-confounding(Dir A)·GRL adversarial de-siting(Dir B)·cross-site dementia 벤치마크(P1)의 kill gate 수치 보존.
- **`FAILED_FOUNDATION_AUDIT_CLOSURE_20260611_KO.md`**: BrainIAC frozen-probe · few-shot · harmonization · longitudinal conversion 라벨 조사 결과 보존.
- 핵심 결론: "AD/aging을 구조 T1w로 풀겠다"는 질문 자체가 morphometry ceiling + site=population으로 인해 method-novelty 공간이 없음을 4방향 측정으로 확정.
- **[추가]** 탐색 실패는 단일 워크스페이스 범위가 아님: `FAILED_DECONFOUNDING §핵심findings F5`에서 "de-confounding 3회 실패(A·B·minyoung2 §9)"를 명기 — minyoung2까지 포함한 cross-workspace 패턴이 기록됨.

---

## 코드 리뷰

diff에 실행 코드는 없고 분석 문서 2개만 추가되었으므로, **기록된 측정 방법론·수치 일관성·재현성 위험**을 검토한다.

### 1. BrainIAC 평가 — leakage 경계 불명확
`FAILED_FOUNDATION §1.2`

BrainIAC가 ADNI/OASIS를 사전학습에 포함했을 가능성을 "추정"으로만 표기(`~35 데이터셋, ADNI/OASIS 포함 추정 → 누수`)했고, §1.2 비교 테이블의 수치(site-probe 0.842, CN/AD 0.735)가 leakage-clean(AJU/KDRC only) 기준인지 전체 7코호트 기준인지 명시되지 않았다. `§1.1` morphometry BAR는 "leakage-clean AJU+KDRC"를 명기한 반면 EXP-F2 헤더는 "우리 v2 T1w; 전처리 공정 검증됨"으로만 기재 — 동일 조건 여부가 독자에게 불투명하다. 논문 작성 시 reviewer의 첫 번째 공격 포인트가 된다.

### 2. CN/AD AUC 수치 두 문서 간 불일치
- `FAILED_DECONFOUNDING §2.3`: ROI-morphometry LOCO 평균 **0.881**
- `FAILED_FOUNDATION §1.1`: morphometry BAR CN/AD AUC **0.911** (KDRC-CV, cross 0.87)

두 수치는 평가 프로토콜이 다르다(LOCO 6코호트 평균 vs KDRC hold-out CV). 이 차이에 대한 명시적 설명이 없어 `§1.4` 3-way 수렴 테이블의 "morpho 0.91" 참조와 혼재된다. 독자가 같은 수치로 읽을 위험이 있다.

### 3. 삭제된 실험 — 수치 단독 보존, 재현 불가
`FAILED_DECONFOUNDING §5`, `FAILED_FOUNDATION §4`

`experiments/`(desiting_v1 22GB 캐시·ckpt 포함), `foundation_audit/`(BrainIAC 7GB 가중치·clone·env 포함 15GB)이 reset으로 **영구 삭제**되었고, 수치는 이 두 문서가 유일한 기록이다. 코드·체크포인트 부재로 closure 수치가 향후 논문에서 인용될 경우 독립 재현이 불가능하며, BrainIAC 가중치(CC BY-NC) 재취득 시 재다운로드 + 환경 재구성이 필요하다.

### 4. Longitudinal 라벨 버그 — 기존 LOCO 결과 영향 범위 불명확
`FAILED_FOUNDATION §1.6`

`clin_dx_label`이 `subject_firstnonnull`으로 고정되어 ADNI/NACC/AIBL/A4 전 코호트에서 dx 변동 0%임을 확인했다. 이 버그가 longitudinal conversion 탐색만 무효화하는지, 아니면 LOCO 평가에 사용된 단면 dementia label에도 영향을 주는지 — 범위 분리 문장이 문서에 없다. CN/AD 단면 분류("최종 진단" 사용)에 영향이 없다고 묵시적으로 전제하고 있으나 확인 문장이 없다.

### 5. amyloid stratum 해석 — 대안 가설 미배제
`FAILED_DECONFOUNDING §2.1`

"음성 stratum cohort-AUC 0.855 / 양성 0.734 → 교란은 tracer/scanner"라는 추론은 논리적으로 타당하나, amyloid 상태 자체가 피험자 모집 전략(site별 enrichment 차이)과 confounded될 경우 동일 패턴이 나타날 수 있다. 두 stratum 간 AUC 차이에 대한 통계 검정 없이 kill gate가 발화됐다. [불확실]

### 6. EXP-2 λ-sweep 비단조성 해석
`FAILED_DECONFOUNDING §2.2`

λ=0.3에서 LOCO 0.886으로 테이블 내 최고값을 기록하고 cohort-AUC 0.835는 λ=1.0의 0.843보다 낮다. "비단조=노이즈"로 처리했으나, LOCO 변동 폭 전체가 0.880–0.886(6코호트 평균 기준 ≈0.006)으로 측정 오차 범위 내임을 감안하면 노이즈 해석은 방어 가능하다. 다만 λ=0.3 LOCO peak에 대한 명시적 분석 없이 무시된 점은 지적될 수 있다. [불확실]

---

## 놓친 것·결정 필요

### 1. MUST-1 미완, 논문 타임라인 없음
`FAILED_FOUNDATION §1.5`에서 harmonization 논문(NeuroImage: Clinical)의 필수 조건으로 "positive-unmask control(MUST-1) 미완"을 명기했으나, 두 커밋 어디에도 담당자·완료 기한·다음 액션이 없다. 선언된 viable 옵션(audit 논문 2개)이 미완 상태로 closure에 기록된 채 방치되었다.

### 2. BrainIAC reality-check 논문 가능성과 CC BY-NC 라이선스 충돌 미검토
`FAILED_FOUNDATION §3`에서 BrainIAC 결과를 근거로 한 negative 논문을 viable로 분류했으나, `§1.2`에서 CC BY-NC 라이선스를 명기했다. 비상업 학술 발표에 이 라이선스가 어떤 제약을 주는지 검토 기록이 없다.

### 3. `minyoung4/data/` 정리 미완
`FAILED_DECONFOUNDING §4` 말미에 "legacy `preprocessed_mm` 스냅샷은 별도 정리 대상"이라 명기되었으나 이번 커밋에 포함되지 않았다. 디스크 점유 규모와 삭제 여부가 결정되지 않은 채 남아 있다.

### 4. 두 closure 문서 간 수치 참조 링크 없음
`FAILED_FOUNDATION §1.4` 3-way 수렴 테이블 ①항이 "FAILED_3D_CNAD"를 언급하나, 해당 문서가 minyoung4인지 다른 워크스페이스인지, 현재 접근 가능한지 명시되지 않았다. 수치 역추적 불가.

### 5. [추가] minyoungi cross-workspace 보존 항목 — 인계 명시 없음
`FAILED_DECONFOUNDING §4`에서 `minyoungi preprocessing/{paths,executor,pet_suvr}.py + run_pet_adni_oasis.py` 및 `minyoungi Track-01 EXPERIMENTS.md EXP-003`이 보존 대상으로 열거되었다. 그러나 이 항목들이 minyoungi 워크스페이스의 어느 경로에 있고, 해당 워크스페이스 담당자가 이 변경을 인지하는지에 대한 인계 문서나 커밋이 없다.

### 6. [추가] F4 orthogonality 주장 — 형식 검정 없이 선언
`FAILED_DECONFOUNDING §2.4 F4`: "site-decodability ⟂ disease-transfer 분리"는 λ-sweep에서 LOCO(disease)가 cohort-AUC(site) 변화와 독립적으로 움직였다는 관찰에서 추론되었다. 그러나 두 지표의 통계적 독립성을 검정한 기록이 없고, 표본이 λ 4개 점에 불과하다. 논문에서 이 분리를 주요 근거로 사용하면 reviewer가 formal test를 요구할 가능성이 있다.

---

## 미해결 질문

1. **MUST-1(positive-unmask control) 없이 harmonization 논문이 peer-review를 통과할 수 있는가?** `FAILED_FOUNDATION §1.5`에서 "deflate-not-unmask" 주장의 필수 조건으로 명기된 MUST-1이 미완인 상태에서, 리뷰어가 반증 조건을 요구할 경우 대응 전략이 문서에 없다.

2. **BrainIAC 비교의 leakage-clean 경계가 어디인가?** `§1.1` morphometry BAR는 "AJU+KDRC only"로 명시되었으나, `§1.2` BrainIAC 결과(site-probe 0.842, CN/AD 0.735)가 동일 clean subset 기준인지 전체 7코호트 기준인지 diff에서 확인되지 않는다.

3. **`clin_dx_label` subject-고정 버그가 기존 LOCO 단면 분류(CN vs AD) 결과에도 영향을 주는가?** longitudinal conversion만 무효화한다면 LOCO 0.88–0.91 수치는 유효하지만, diff 어디에도 "단면 분류는 영향 없음"을 명시적으로 선언한 문장이 없다.

4. **삭제된 22+15 GB 실험 데이터의 서버 백업 존재 여부는?** `/home/vlm/data/` 공유 데이터는 보존됐으나 `experiments/` 체크포인트는 reset으로 삭제됐고, 서버 수준 백업 여부가 두 문서 모두에서 언급되지 않았다. [불확실 — diff에 근거 없음]

5. **[추가] amyloid PET 2,896 subj 전처리 데이터(`/home/vlm/data/preprocessed_official/v2`)의 재사용 가능성 조건은?** `FAILED_DECONFOUNDING §4`에서 "defect-free, 재사용 가능"으로 보존 선언했으나, 이 데이터가 Dir A kill 사유(tracer/scanner confound)에 의해 오염된 상태임에도 재사용 가능하다고 판단한 근거(어떤 task에 재사용 가능한가)가 명시되지 않았다.

# === plant ===
## 진행 요약

**핵심 전환 (diff 지지):** 워크스페이스 `plant`를 `microbrain`(bias-robust T1w micro-표현학습)으로 전면 재정의. 삭제 항목: `build_longitudinal_cases.py`(308줄), `test_build_longitudinal_cases.py`(90줄), `note/SCRATCHPAD.md`(97줄), `note/daily/2026-06-01.md`(40줄), 종단 사전등록 문서(96줄) 등 총 635줄+. `RESEARCH_BRIEF.md`·`CLAUDE.md`·`DECISION_LOG`·`REPO_STRUCTURE`·`docs/ledgers/`·`docs/investigations/` 신설. (commit 3541826, diff-stat 전체 지지)

**실험 진행 (SCRATCHPAD.md 직접 지지):**
- **P0 audit A0~A5 완료:** voxel→site AUROC 0.475(3.3×chance), N4 효과 −0.006(N4≠harmonization), 잔차화 후 disease 0.722(drop −0.05) → "bias 실재+분리가능" 판정, P2 진입 근거 확보.
- **P2 Stage-0 smoke:** ResNet10, 2mm, **GPU5**, OASIS held-out, 1 seed, 10 epoch. held-OASIS AUROC last-3 0.583 — morph bar 0.72 미달. "파이프라인 검증 완료, 결정 아님"으로 명시. overfit/no-transport 징후 및 preload IO 병목(13분) 기록.
- **Amyloid CPU:** morph+age 0.755, 이미지 texture 기여 없음, APOE4 주 레버(+0.023) → **amyloid 바 상향 morph+APOE ≈ 0.78**. 혈액바이오마커(D5) AD 분류 한정 반증(dementia +0.005, MCI +0.000, amyloid +0.007) → DECISION_LOG에 폐기 기록(단, MCI/amyloid는 미검증으로 SCRATCHPAD에 별도 주석).
- **주제 수렴:** morphometry가 약한 regime(MCI/amyloid/preclinical)에서 LOCO-transportable micro-표현이 기여하는가. (DECISION_LOG + RESEARCH_PROPOSAL.md 지지)

---

## 코드 리뷰

### 1. `experiments/P2/stage0_smoke.py` (77줄)

> 초안에서 "내용 미공개"로 표현했으나, diff-stat에 77줄 추가가 명확히 기록됨. patch 30000B 상한으로 내용이 절단된 것이며 파일 부재가 아님. 이하 우려는 여전히 유효하나 전제 수정.

- **validation-lock 불확실 [불확실]:** SCRATCHPAD "held-OASIS AUROC last-3 0.583" — last-3 평균은 체크포인트 선택이 아닌 reporting 방식이나, 실제 코드에서 held-out을 조기종료 기준으로 썼는지 patch 절단으로 확인 불가.
- **multi-seed=1:** SCRATCHPAD "1 seed, 10ep" 명시. 커밋 제목 "Stage-0 smoke PASS"가 결정으로 오독될 여지 있음.
- **GPU 승인 기록 부재 [초안 누락]:** SCRATCHPAD에 "GPU5"에서 실행됐다고 명시됨. CLAUDE.md Confirmation Gates는 "GPU 스크립트(학습·대형 추출 배치) 실행 = Min 승인 후" 를 규정하는데, DECISION_LOG와 diff 어디에도 승인 기록 없음.

### 2. `src/microbrain/audit.py` (213줄, patch 절단)

- **전체 repo 테스트 0건 [초안 과소 기술]:** 초안은 "audit.py 대응 테스트 없음"으로만 기술했으나, 기존 유일 테스트 파일(`tests/test_build_longitudinal_cases.py` 90줄)이 삭제됐고 diff-stat 전체에 신규 테스트 파일 없음. **전체 테스트 커버리지 0건** 상태.
- **P1 게이트 위반:** RESEARCH_BRIEF.md P1 섹션 "split/leakage 단위테스트가 통과해야 P2 진입" 명시(diff 직접 지지). 현재 테스트 0건인 채로 P2 Stage-0가 실행됨.
- **A4 잔차화 누수 위험 [불확실]:** audit.py 코드 미공개(patch 절단)로 train/test 일관 fit 여부 확인 불가. 유효한 우려.
- **A2 voxel→site 0.475: smoke vs full [불확실]:** P0_bias_audit_plan.md "A2/A3만 텐서 1-pass는 smoke→full 승인 필요" 명시(diff 지지). SCRATCHPAD가 이 수치가 smoke-subset인지 전수인지 명시하지 않음.

### 3. `notebooks/_build_notebooks.py` (492줄, patch 절단)

- **재현성 종속성 [불확실]:** P0 audit 수치(A0~A5 figures/CSV)가 이 빌더에 종속되는지 코드 미공개로 확인 불가. 492줄 빌더에 단위테스트 없음(diff-stat 신규 테스트 파일 전무).
- ~~"results/P0/ 하위를 별도 커밋했는지 diff-stat에 없음 [불확실]"~~ **[근거부족, 제거]:** diff-stat 주석에 "코드/문서, 데이터 제외"가 명시돼 있어, 결과·데이터 파일의 부재는 필터링의 결과이며 의심 근거 아님.

### 4. `docs/P0_bias_audit_plan.md` (166줄, 부분 공개)

- **문서 vs 실행 불일치 (patch 직접 지지):** 문서 헤더 "상태: 설계안(코드 아님). Min 승인 전 GPU·대형 추출 금지" — 이 텍스트가 patch에 그대로 있음. SCRATCHPAD에는 P0 audit이 완료됐다고 기록. 승인 시점·승인자 미기록 상태로, 다음 에이전트가 문서를 읽을 경우 혼란 원인.

### 5. amyloid n~750 코호트 구성

- **선정 기준 미문서화 [불확실]:** SCRATCHPAD "biomarker (amyloid, n~750)"에서 voxelwise_qc_candidate 12,978로부터 ~750으로의 필터링 기준이 diff에 없음. LOCO fold별 class balance 미검증.

---

## 놓친 것·결정 필요

| 항목 | 설명 | diff 근거 |
|---|---|---|
| **GPU 승인 게이트 미확인** | Stage-0 smoke가 GPU5 실행(SCRATCHPAD 명시). CLAUDE.md Confirmation Gates "GPU 스크립트 = Min 승인 후" 규정. DECISION_LOG·커밋 어디에도 승인 기록 없음. | CLAUDE.md §Confirmation Gates + SCRATCHPAD |
| **P1 phase 누락** | RESEARCH_BRIEF.md P0→P1→P2 순서·조건 명시. P1 deliverable(split/누수 단위테스트 PASS + G1/G2 수치 고정)에 해당하는 커밋 없음. P0 A5(LOCO morph 0.766)가 baseline 일부 대체 가능하나, "단위테스트 통과 후 P2 진입" 조건은 미충족 기록. | RESEARCH_BRIEF.md §4 P1 |
| **Stage-1 kill-criteria 미확인** | SCRATCHPAD "Stage-1 결정 run … kill-criteria" 예고. P2_plan.md(51줄 추가)가 patch에 없어 NO-GO 숫자 확인 불가. | diff-stat P2_plan.md 존재, patch 미포함 |
| **target 정의 확정 전 smoke 실행** | DECISION_LOG "target 정의(CDR≥0.5 vs CN/AD) — Min 결정 대기"가 open 상태인데 Stage-0 smoke 실행. smoke의 실제 target이 결정 로그에 없음. | DECISION_LOG open item |
| **`P0_bias_audit_plan.md` 상태 미갱신** | 문서가 여전히 "설계안·승인 전 금지" 텍스트 그대로(patch 지지). 승인 시점·승인자 미기록. | P0_bias_audit_plan.md vs SCRATCHPAD |
| **IO 병목 해소 미실행** | SCRATCHPAD "preload 1000=13분 → Stage-1 npy 캐시 필요". diff-stat에 캐시 빌드 스크립트 없음. | SCRATCHPAD + diff-stat |
| **혈액바이오마커 반증 범위 과잉** | DECISION_LOG "D5 폐기"로 기록됐으나 SCRATCHPAD 자체에 "단 MCI/amyloid는 미검증"이라 주석됨. AD 분류 한정 반증을 전체 arm 폐기로 기록하는 것은 SCRATCHPAD 내 자기모순. ledger 범위를 "AD 분류 한정 반증"으로 수정 필요. | SCRATCHPAD 주석 vs DECISION_LOG |
| **longitudinal 사전등록 문서 삭제** | `docs/ledgers/2026-06-01-longitudinal-incremental-transport-prereg.md` 96줄 삭제(diff-stat). 이 사전등록 폐기에 대한 전용 음성 ledger 기재 여부 불명. DECISION_LOG에는 "longitudinal→microbrain 재정의" 항목만 있고 사전등록 문서 폐기 근거는 별도 기록 없음. | diff-stat |

---

## 미해결 질문

1. **GPU 승인:** Stage-0 smoke 실행 전 Min 승인이 이루어졌는가? DECISION_LOG에 기록이 없다면 CLAUDE.md Confirmation Gate 위반이다. 사후 기록이라도 남길 것인가?

2. **P1을 별도 phase로 수행할 것인가?** P0 A5 LOCO morph 0.766이 P1 baseline을 대체한다면, RESEARCH_BRIEF.md P1이 요구하는 "split/leakage 단위테스트 PASS"는 어떻게 충족할 계획인가?

3. **Stage-1 kill-criteria 구체 숫자는?** G1(site classifier balanced-acc ≤ 1/K + ε, ε=?)와 G2(morphometry incremental: 부트스트랩 CI lower > 0? Δ > 0.02?) 숫자 명세. P2_plan.md(patch 미포함)에 이미 있는가?

4. **실험 target 확정:** CDR≥0.5 binary인가, CN vs AD인가, amyloid binary인가? Stage-0 smoke가 어느 target으로 실행됐는지 DECISION_LOG에 없음. amyloid로 추정되나 미기록.

5. **A2/A3 텐서 전수 pass 승인 여부:** SCRATCHPAD의 voxel→site 0.475가 smoke-subset 결과라면 full-pass를 추가 승인받아야 하는가? P0_bias_audit_plan.md는 이 게이트를 명시하나 현재 실행 범위(smoke/full)가 불명.

6. **`src/microbrain/audit.py` 단위테스트 계획:** RESEARCH_BRIEF.md P1 게이트 "split/leakage 단위테스트 통과" 조건 충족을 위해 언제 작성할 것인가? (현재 전체 repo 테스트 0건)

7. **npy 캐시 빌드:** Stage-1 전 `data/derived/` 하위 npy 캐시 생성 스크립트의 승인 절차와 예상 디스크 사용량은? (Stage-0 IO 13분 병목의 전제 조건)


</details>
