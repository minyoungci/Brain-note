# 일일 종합 — 2026-06-11 (KST)

> LLM 자동 종합(gen→critic→synth). **검토용 초안이며 미검증 주장이 있을 수 있음.**
> 모델: gen=`claude-sonnet-4-6` critic=`claude-sonnet-4-6` synth=`claude-opus-4-8` · 활동 WS 3개

## 오늘 한 줄 요약

오늘 활동한 세 워크스페이스(minyoung2·minyoung4·plant)가 **사실상 같은 경험적 발견 — "이 다기관 데이터에서 deep/learned 표현이 hand-crafted morphometry를 이기지 못하고, site-bias는 실재하나 임상 신호를 죽이지는 않는다" — 에 독립적으로 도달**했으나, 여기서 **세 가지 양립 불가능한 전략적 결론**을 내렸다: minyoung2는 이를 논문의 PRIMARY 주장("site-invariance ⇏ clinical-transferability" 반증)으로 격상했고, minyoung4는 같은 사실을 근거로 방향을 폐기·reset 했으며, plant는 바로 그 전제에 **반대 베팅**하는 새 프로그램(microbrain)을 오늘 착수했다. 즉 한 사실에서 "기여/사망/재도전"이 동시에 갈렸고, 이 중 최소 둘은 동시에 옳을 수 없다.

> *날짜 주의*: 오늘(06-11) 검증 분석은 3개 WS만 제공됨. minyoungi·minyoung3는 어제 종합·OPEN_QUESTIONS에 등장하나 오늘 활동/분석 없음 — 아래 "해소" 판정은 활동 부재로 인한 미추적과 구분 필요.

## 워크스페이스별 핵심

- **minyoung2** — GAP(G1–G8) 전체 + code-auditor 감사 + research-critic REFRAME을 하루에 완결. 핵심: ROI-vol(0.420)이 deep(0.276~0.292)보다 cross-cohort CDR 전이 우월(site-bias가 더 높은데도), anat-pretrain(G5) T1-only 최고 0.349, combo 0.440. PRIMARY 주장을 "dissociation"에서 **"site-invariance가 clinical-transferability를 보장하지 않는다"의 반증**으로 REFRAME 확정. 단 강한 SSL(SimCLR)은 amyloid에서만 chance(0.545)로 실패 보고, **CDR LOCO 판정은 미완**.

- **minyoung4** — de-confounding/cross-site dementia 4(?)개 방향을 측정 근거로 배제하고 실험 산출물을 reset 삭제. 오늘 커밋은 아카이브 문서 1건(+70줄)뿐 — 삭제 자체는 이 diff로 검증 불가. 결론: "임상 라벨(dx/amyloid/cdr)엔 deep-method headroom 없음". PET 전처리(2,896 subj)·manifest 인프라는 보존. 문서 내부 "4개 vs 3개 방향" 카운트 불일치.

- **plant** — longitudinal에서 **microbrain(site/scanner bias-robust 표현학습)으로 완전 재정의**. P0 bias audit(A0~A5) 실행: "bias 실재(voxel→site 3.3×chance) + disease 분리가능(잔차 후 AUROC 0.722, drop −0.05)" 판정으로 P2 진입 근거 확보. 단 "부피 너머 신호 천장" 가설이 여전히 dominant prior이고, P2 설계·Min 승인 미결.

## 주목·결정 필요 (우선순위순)

**1. 🔴 [포트폴리오] 같은 사실에서 갈린 세 결론을 먼저 조정하라 — 특히 plant vs minyoung4 직접 충돌.**
minyoung4 §2.2(λ=0.3 de-siting ΔLOCO≈0)와 §2.3(morpho-distill 3D encoder = ROI logistic, ΔAUC≈0)은 **plant가 P2에서 검증하려는 가설("bias-robust 표현이 부피 너머 신호를 잡는다")의 선행 반증(prior-art falsification)**에 가깝다. plant가 minyoung4 결과를 읽지 않고 P2에 GPU를 투입하면 **이미 null로 나온 실험을 재실행**할 위험이 크다. *내가 놓쳤을 가능성*: minyoung4의 ΔAUC≈0/ΔLOCO≈0은 모두 **CI·per-cohort 분산이 없는 점추정**이라(아래 4·5번), "null"이 아니라 "검정력 부족"일 수 있다. 그렇다면 plant의 재도전이 정당할 수도 있으나 — 그 경우 두 WS는 *같은 MDE 질문*을 공유하므로, 충돌이 아니라 **공동 검정력 분석**으로 묶여야 한다. 어느 쪽이든 따로 진행하는 건 비효율.

**2. 🔴 [minyoung2] REFRAME된 PRIMARY 주장이 미완성 판정 위에 서 있다.**
SPEC §10.7은 "강한 SSL이 ROI를 따라잡으면 dissociation 붕괴"를 kill gate로 명시했는데, 보고된 SSL 실패는 **amyloid(0.545≈chance)뿐이고 CDR LOCO 수치가 없다**. 그런데 REFRAME은 "확정"으로 처리됐다. amyloid에서의 SimCLR representation 실패는 "aug가 너무 쉬움"이라는 별개 실패일 수 있고, 정작 헤드라인(ROI 0.420 vs SSL on CDR) 비교가 빠져 있다. *내가 놓쳤을 가능성*: research-critic이 amyloid 실패만으로 REFRAME을 승인했다면, critic 자체가 kill gate를 잘못 적용한 것일 수 있다 — 즉 검증 단계가 통과시킨 게 아니라 **질문을 바꿔 회피**한 것일 수 있다. reviewer가 가장 먼저 칠 지점.

**3. 🔴 [minyoung2 / 데이터 인프라] canonical orientation 누락이 dissociation의 근거 데이터를 오염시킬 수 있다.**
`cache_t1.py`·`cache_ext.py`·`cache_parc.py`에 `as_closest_canonical()` 호출이 없다 — 이는 프로젝트 CLAUDE.md가 명시한 gotcha("nibabel resample LIA→RAS 실패")의 정면 위반. 코호트별 MRI orientation이 다르면 **해부 방향이 뒤섞인 채 캐시**되고, 하필 그 데이터가 **cross-cohort 전이(G8, 외부 코호트)** — dissociation 주장의 바로 그 근거 — 에 쓰였다면 결과 재현이 의문이다. *내가 놓쳤을 가능성*: 기존 `s2.py:load_resized`가 이미 방향 보정을 한다면 새 캐시만 불일치일 수 있는데, 그러면 오히려 **G5(in-domain, 기존 캐시) vs G8(외부, 신규 캐시) 비교 자체가 orientation 처리 불일치로 오염**된다 — "누락"보다 "혼용"이 더 위험.

**4. 🔴 [plant] P0 실행이 Confirmation Gate를 우회했을 가능성.**
`P0_bias_audit_plan.md`는 "Min 승인 전 학습·대형 추출·GPU 금지" + "smoke(350)→승인→full" 2단계 게이트를 설계했으나, SCRATCHPAD는 smoke 결과·Min 명시 승인 기록 없이 "P0 audit 실행 완료"로 점프했다. A2/A3 텐서 전수 1-pass가 승인 없이 돌았다면 **CLAUDE.md Confirmation Gate 위반**. *내가 놓쳤을 가능성*: 이건 plant 단독 이슈가 아니라 어제 종합이 짚은 **"Min 승인 장시간 전처리 클러스터"**(minyoungi DICOM, minyoung4 G0, minyoung2 G8 training)의 한 사례다 — 개별 사후 승인보다 **클러스터 일괄 정책**이 필요.

**5. 🟠 [minyoung4·plant 공통] 통계적 동등성을 점추정만으로 단정.**
minyoung4(ΔAUC≈0, λ ΔLOCO +0.4pp)와 plant(A5 LOCO mean 0.766, drop −0.05)가 모두 **CI·per-cohort 분산·prevalence baseline 없이** "≈0/transport/separable"을 선언한다. 이는 OPEN_QUESTIONS의 "F10 +0.018: 음성 vs 검정력 부족 미분리"와 **같은 방법론적 결함의 재발**이다. 특히 plant는 AJU 97.7% impaired라는 극단 base-rate 코호트를 포함해 LOCO mean 하나로 "transport"를 판정할 수 없다. equivalence test(TOST)/random-effects 없이는 1·2번의 전략 충돌을 풀 수 없다.

## 코드 리뷰 종합 (반복·심각)

**반복 패턴(워크스페이스 교차):**
- **전처리 orientation 일관성** — minyoung2 캐시 3종 `as_closest_canonical()` 누락. plant도 `_build_notebooks.py`가 동일 `preprocessed_official` manifest 절대경로 의존 → 셋 다 **공유 데이터 레이어의 방향/경로 일관성**이라는 한 뿌리.
- **점추정→동등성 단정** — minyoung4 ΔAUC≈0·λ +0.4pp, plant A5 −0.05 모두 CI 없음. (위 5번)
- **테스트·시드 부재** — plant: 기존 테스트 90줄 전면 삭제 후 `audit.py`(213줄)·`_build_notebooks.py`(492줄)에 신규 테스트 0줄. minyoung2: `g1.py` seed 없음(`g2.py`엔 `manual_seed(42)` 있음). 재현성·누수가드가 모두 비어 있다.
- **diff 절단 너머의 미검증 "PASS"** — minyoung2 g5/g7/g8(patch 상한 초과)·plant `_build_notebooks.py`(492줄) 본문 미공개. 어제 종합 #5(검증 공백)가 **그대로 지속**. train-only PCA fix가 G5/G7/G8에 전파됐는지, 노트북 6종이 seed 고정인지 둘 다 확인 불가.

**심각(개별):**
- minyoung2 `g2.py:remap` 배열 크기 — 원본 atlas 큰 label(예: 2035)이 clip으로 조용히 background(0)에 매핑, `F.one_hot(K+1)` 초과 시 CUDA silent OOB/crash 가능.
- minyoung2 `cache_ext.py:13` 모듈 최상단 `LABELS = np.load(...)` — 파일 부재 환경(첫 실행/CI/smoke)에서 import만으로 crash.
- plant SCRATCHPAD 단위 혼용 — `A2 0.475`가 balanced-acc(7-class chance 0.143×3.3)인데 같은 줄 `A0 AUROC 0.71`과 섞여, AUROC 0.475(≈chance)로 오독 시 해석 역전.

## 미해결 질문 (어제 대비)

**해소·무효화:**
- ☑(잠정) 어제 #1 *minyoung2 방향 결정* → GAP 완결 + REFRAME으로 **사실상 해소**. 단 위 2번(SSL CDR 판정 미완)으로 "잠정 해소"에 머묾.
- ☑(무효) 어제 #3 *plant Cox feature 컬럼 충돌* → longitudinal 방향 자체가 microbrain으로 피벗되어 **무효화**. OPEN_QUESTIONS의 *plant converter 통계력*도 같은 이유로 무효.

**신규:**
- ☐ minyoung2 **SSL CDR LOCO 결과는 어디 있나** — amyloid만으로 dissociation 생사 판정 완료라 볼 수 있나? (2번)
- ☐ minyoung2 캐시 `as_closest_canonical()` **의도적 생략인가**, 기존 캐시와 방향 처리 일치하는가? (3번)
- ☐ plant P0 **smoke 결과·Min 승인**은 어느 커밋/노트에? (4번)
- ☐ plant **A5 per-cohort 수치**(AJU hold-out CDR LOCO, prevalence baseline 대비 delta·CI)?
- ☐ minyoung4 **"4개 방향"의 실재** — minyoung2 §9가 실제로 de-confounding 실패를 기록하는가(교차참조 무결성)?

**지속(미해소):**
- ☐ 어제 #2 *minyoungi DICOM 대량 변환 승인* — 오늘 minyoungi 활동 없어 **미추적**(해소 아님). raw 저커버리지(T1 39.4%) 병목 여전.
- ☐ 어제 #4 *minyoung3 규범 plan 단일화* — 오늘 활동 없음.
- ☐ 어제 #5 / OPEN_Q *diff 절단 검증 공백* — minyoung2 g5/g7/g8·plant 노트북 본문 여전히 미열람.
- ☐ OPEN_Q *minyoung2 3D CNN(IMG-020/022) 결과 미생성* → 오늘 **G8-resnet18 "🔄 학습중"**으로 부분 진행했으나 미완, strong-deep baseline 결론 여전히 보류.

## 워크스페이스 간 연결·시너지

1. **3-way 수렴 = 포트폴리오 수준의 (잠정) 음성 결론.** minyoung2·minyoung4·plant가 독립적으로 "morphometry ceiling / deep no-headroom"에 도달했다. 이것이 portfolio가 **"이 데이터·이 라벨에선 deep이 안 된다"로 수렴 중**임을 시사한다 — 중요하지만 부정적 결론. minyoung2가 이를 *반증 논문*으로 전환한 것은 영리한 활용이나, 위 2·3번이 사실이면 minyoung2도 음성 진영으로 합류해 **세 WS 전부 null**이 된다.

2. **plant ⟵ minyoung4 (직접, 행동 필요).** plant P2 착수 **전에** minyoung4 §2.2/§2.3을 prior art로 반드시 검토할 것. 같은 데이터·같은 가설을 다른 WS가 이미 (점추정상) null로 봤다. 충돌이면 plant는 재실행을 멈춰야 하고, 검정력 문제면 둘은 **공동 MDE/equivalence 분석**으로 합쳐야 한다.

3. **minyoung4 F5 ⟷ minyoung2 §9 (무결성).** minyoung4가 "de-confounding 3회 실패"의 세 번째로 minyoung2 §9를 인용하나 검증 불가이고, 오늘 minyoung2 분석엔 §9 언급이 없다. 워크스페이스 간 인용이 **검증 없이 결론 카운트를 부풀리는** 패턴 — 1번의 전략 조정 전에 이 교차참조부터 확정해야 한다.

4. **공유 데이터 레이어가 공유 리스크.** minyoung4가 보존 선언한 PET 전처리·manifest, plant가 의존하는 `preprocessed_official`, minyoung2가 새로 만든 캐시 — 셋 다 같은 레이어 위에 있고, **canonical orientation·커버리지·경로 무효화**가 셋의 공통 약점이다. minyoung2의 캐시 버그를 고치는 김에 **레이어 차원의 orientation/경로 검증 유틸**을 만들면 3개 WS가 동시 수혜.

5. **Min 승인 장시간 전처리 클러스터(어제→오늘 지속).** plant P0(사후?), minyoung2 G8 training, minyoungi DICOM, minyoung4 G0 — 개별 승인이 아니라 **일괄 우선순위화**가 합리적. 단, 1번 수렴 결론이 맞다면 이 GPU 투자 다수가 *null 재확인*에 쓰일 위험이 있어, 승인 전에 "무엇을 새로 알 수 있는가"를 각 건에 요구해야 한다.

---
*[방법론 메모] 본 종합의 신뢰도는 여전히 diff 절단 너머 코드(minyoung2 g5/g7/g8, plant 노트북 본문) 미열람에 걸려 있다. 위 1·2·5의 전략 판단은 그 코드와 minyoung4의 CI/per-cohort 분산이 확인되기 전까지 [불확실].*

---
<details><summary>워크스페이스별 검증 분석 (펼치기)</summary>

# === minyoung2 ===
## 진행 요약

오늘 minyoung2 워크스페이스에서 **GAP 프로그램(G1–G8) 전체 + code-auditor 감사 + research-critic REFRAME** 이 하루에 완결됐다. 핵심 발견은 세 가지: (1) hand-crafted anatomy(ROI-vol 0.420)가 learned deep(0.276~0.292)보다 cross-cohort CDR 전이가 우월 — site-bias도 더 높은데도 (dissociation); (2) anatomy-prediction pretraining(G5)이 T1-only 최고치 0.349; (3) combo(anat-pretrain feat + ROI-vol)=0.440으로 ROI 단독 초과. code-auditor가 G3 transductive PCA→train-only PCA 수정(C2)과 캐시 정렬 assert(C1)를 처방해 leakage-free 통과. research-critic 평결을 받아 PRIMARY 주장을 "dissociation"에서 **"site-invariance ⇏ clinical-transferability" 반증**으로 REFRAME 확정. 강한 SSL(SimCLR) baseline은 amyloid 0.545≈chance로 실패("aug 너무 쉬움") — CDR LOCO 판정은 아직 확인 필요.

---

## 코드 리뷰 (diff 기반)

### 버그

**`cache_t1.py:worker` — canonical 방향 확인 없음 (고위험)**
`cache_t1.py:11–16` 에서 NIfTI 로드 후 바로 interpolate한다. 프로젝트 CLAUDE.md에 "nibabel resample_from_to LIA→RAS 실패 → `as_closest_canonical()` 선행 필수"가 명시되어 있음에도 이 파일에는 호출이 없다. 다른 캐시 스크립트(`cache_ext.py`, `cache_parc.py`)도 동일하게 누락. 코호트(NACC/A4/AIBL 등)마다 MRI orientation이 다르면 **전혀 다른 해부 방향으로 캐시**되어 pretrain 왜곡 — G8 결과가 이 데이터로 나왔다면 재현 의문.

**`g2.py:remap` 배열 크기 문제 (중위험)**
`g2.py:128–130` `remap = np.zeros(int(labels.max()) + 1, ...)` 후 `remap[np.clip(parc, 0, int(labels.max()))]`로 인덱싱. `cache_parc.py`에서 `labels`는 lowres 다운샘플 후 unique label이지만, 원본 atlas에 2035 같은 큰 label이 있을 경우 remap 배열이 작아 clip이 잘못된 label(0번=background)로 조용히 매핑된다. `g2.py:165` `F.one_hot(..., self.K+1)` 범위를 초과하는 값이 남아있으면 CUDA에서 silent OOB 또는 crash.

**`cache_ext.py:LABELS` 전역 로드**
`cache_ext.py:13` `LABELS = np.load(...)` 가 모듈 최상단에서 실행된다. `roi_feats.npz`가 없는 환경(예: 처음 실행, CI)에서 import 시점에 crash — smoke test나 import만 해도 터짐.

### 데이터 누수

- G3 transductive PCA → train-only PCA 수정(커밋 5003562, code-auditor C2)은 **이미 반영**. 나머지 실험(G5, G7, G8)이 이 fix 이후 동일 train-only 로직을 쓰는지 diff에서 확인 불가 — g5.py·g7.py·g8.py는 patch 상한 초과로 미제공 [불확실].
- `cache_ext.py:47–49` `ok = [r is not None for r in res]` → `df["consortium"].values[ok]`는 numpy boolean indexing으로 맞지만, `Pool.map` 순서가 input 순서와 동일함을 명시한 주석이 없다(표준 동작이긴 함). assert 없음.

### 재현성

**`g1.py` — `torch.manual_seed` 없음**
`g1.py` 전체에 seed 고정이 없다. `g2.py:109`에는 `torch.manual_seed(42)`가 있으나 g1에는 없어 multi-run 비교 시 다른 초기화로 결과 변동 가능. augment(`g1.py:46–50`, `g2.py:92–96`)도 run마다 다름 — 이건 의도적일 수 있으나 폴드별 seed 고정 없이 mean corr을 보고하면 재현성 주장 약화.

### 수치 불안정

`cache_ext.py:32–34` `vol = nv[LABELS]; vol = vol / (vol.sum() + 1e-6)` — 분모 ε처리는 되어 있으나, LABELS 인덱스가 `np.bincount(..., minlength=2036)` 배열의 범위 내인지 보장이 없다. LABELS가 95 라벨 기준이면 max~200 범위지만 외부 코호트 parcellation이 다른 atlas이면 OOB [불확실].

---

## 놓친 것·결정 필요

**1. SSL 실패 = dissociation 생사 판정 미완성**
SPEC §10.7은 "강한 SSL(SimCLR)이 ROI 따라잡으면 dissociation 붕괴"라고 판정 기준을 설정했다. 커밋 5067d06는 "SSL 실패(amy 0.545≈chance)"를 보고하지만, **CDR LOCO 결과가 없다**. amyloid chance 성능은 SimCLR representation 자체의 failure이고, CDR LOCO가 ROI(0.420)를 따라잡았는지/못 했는지 — 논문의 핵심 판정 — 이 diff에서 확인되지 않는다. "aug 너무 쉬움"이 실패 원인이라면 더 강한 aug로 재시도 없이 판정을 "통과"로 처리하면 reviewer 공격 대상.

**2. G8-resnet18 결과 없음**
RESULTS.md §5 표에 "🔄 학습중"으로만 표기. compact CNN 대비 ResNet18이 T1-only 0.420 도달하면 fix 주장이 강해지는 핵심 데이터인데 커밋에 결과 없음.

**3. `⬜` 항목 3개 전부 미착수**
SPEC §10.7 체크리스트 중 ⬜ DAMT 공개코드 재현, ⬜ Lu/Currin 인용·반박, ⬜ amyloid 2nd axis — 하나도 커밋에 없다. DAMT는 "출판 필수(must-cite + method overlap)" 수준으로 명기했으나 진척 없음. 제출 전 이 세 개가 없으면 reviewer F1·F2 reject 그대로.

**4. canonical 방향 미적용 vs 기존 캐시 일관성**
G5/G6/G7 결과에 사용된 기존 T1 캐시(`t1_96.npz`, `s2.py load_resized`)가 `as_closest_canonical()`을 적용했는지 diff에 없다. 새로 만든 `cache_t1.py`·`cache_ext.py`에 없으므로, **기존 캐시와 불일치**가 생길 수 있다 — G8(외부 코호트)과 G5(in-domain) 비교가 오염될 수 있음.

**5. CDR dissociation ADNI 예외 처리 결정 없음**
stats_hard 결과 "CDR 3/4(ADNI 예외: ROI 0.318 < CNN 0.334, CI 겹침)" — 논문에서 이를 limitation으로 명시할지, ADNI 특이성(DX 분포 차이)으로 설명할지 결정이 없다.

---

## 미해결 질문

1. **SSL(SimCLR) CDR LOCO 결과는 어디에 있나?** amyloid만 0.545로 실패 보고됐는데, ROI(0.420)와 비교할 CDR 수치 없이 "dissociation 생사 판정"을 완료했다고 볼 수 있나?

2. **cache_t1.py·cache_ext.py에 `as_closest_canonical()` 의도적 생략인가?** 기존 `load_resized`(s2.py)가 이미 방향 보정을 한다면 중복 필요 없지만, 새 캐시 스크립트는 s2.py를 쓰지 않으므로 명시적 확인 필요.

3. **G8-resnet18 학습이 언제 완료되나, 그리고 T1-only 0.420 도달 실패 시 플랜 B는?** Design-Y(~12K)를 다음 단계로 예정했는데 데이터 준비(canonical 방향 포함) 리드타임 있나?

4. **DAMT baseline 없이 제출 가능한 가장 가까운 데드라인이 있나?** ACCV vs MICCAI 중 어느 쪽을 타겟으로 하는지에 따라 DAMT 재현의 우선순위가 결정된다. (※critic 실패, 미검증)

# === minyoung4 ===
## 진행 요약

minyoung4 워크스페이스에서 진행하던 de-confounding / cross-site dementia 탐색 방향들을 측정 근거로 배제하고, 실험 산출물(`experiments/`, `audits/`, `SPEC.md`, `CONTRIBUTION.md`)을 reset으로 삭제했다. 오늘 커밋은 **문서 1건 추가(+70줄)만** — 삭제·reset 자체는 이 커밋 이전에 수행된 것으로, 이 diff로는 확인 불가. 핵심 결론은 "이 데이터의 임상 라벨(dx/amyloid/cdr)엔 deep-method 헤드룸이 없음"이며(§0 직접 인용), 공유 인프라(PET 전처리 2,896 subj, manifest 토대)는 보존 선언. — "LOCO 평가 코드 보존"은 §4 명시 항목이 아니라 §6에서 "재사용 가능했음"으로만 언급됨 [근거부족: 보존 선언으로 표현 불가].

§0에서 "4개 독립 방향 배제"를 주장하나, §1이 명시하는 방향은 minyoung4 내 3개(Direction A, B, P1)이며, F5에서 4번째로 minyoung2 §9를 소급 산입한 것으로 보임 — 이 차이는 문서 내부 불일치이자 외부 컨텍스트 의존.

---

## 코드 리뷰

> 이번 diff는 문서 1파일 추가뿐이므로 코드 버그·데이터 누수 점검 대상 없음. 문서 내 수치·주장을 대조 검증.

**[수치 일관성]**
- `§2.1` — PET SUVR cohort macro-AUC 0.829 → scale 정규화 후 0.776. diff에 수치 자체는 존재하나 정규화 방법(z-score? min-max? per-cohort?) 미기재 — 재현 불가. "shape에도 cohort 신호"라는 해석은 수치 하나로 확정 불가 [불확실].
- `§2.1` — amyloid 통제 후 음성 stratum 0.855 / 양성 0.734가 kill gate 발화 근거로 제시됨. 두 수치 모두 diff에 명기되어 있으나, stratum별 샘플 수·신뢰구간 없음. "tracer/scanner 교란" 해석은 수치만으로 단정 불가 [불확실].
- `§2.1` — "구조→amyloid ΔAUC≈0" 수치 diff에 명기됨. 단, 기준 AUC(clinical-only ~0.78의 `~` 표기)가 근사값 — 정확도 불명확.
- `§2.2` — λ=0.3 LOCO 0.886이 baseline(0.882)보다 +0.4pp. diff 표에서 확인됨. "ΔLOCO≈0" 결론에 표준편차·신뢰구간 없어 노이즈 여부 판단 불가 [불확실].
- `§2.3` — "morpho-distill 3D encoder = ROI logistic, ΔAUC≈0" 결론의 근거가 평균값 0.881 vs 0.882뿐. LOCO 6코호트 개별 분산 미제시 — 통계적 동등성 주장 약함.

**[외부 참조 검증 불가]**
- `§2.4 F5` — "de-confounding 3회 실패(A·B·minyoung2 §9)" — A·B는 이 diff 내에서 확인되나 `minyoung2 §9`는 이 diff 외부 문서. 해당 실패가 실제 기록됐는지 이 diff로 검증 불가.
- `§6` — "minyoung3 Q-ROUTE가 이미지 추론형 영역 선점" — 전략적 주장이나 minyoung3 현황은 이 diff 외부. 사실 여부 검증 불가.

**[경로 참조]**
- `§4` — 보존 경로 `` `@/home/vlm/data/preprocessed_official/v2` `` — `@` prefix가 파일 참조 컨벤션인지 오기인지 diff만으로 불명확. 실제 경로 유효성 미검증.

**[표기 오류]**
- `§4` — `minyoungi`(i 접미)가 diff 내 **2회** 등장(`minyoungi 파이프라인 패치`, `minyoungi Track-01`). 초안이 "3회"로 기술한 것은 오류 — diff 원문 재확인 결과 2회. `minyoung1`(숫자 1)의 오기로 추정되나 실제 워크스페이스명 미확인 [불확실].

**[클래스 불균형 미기재 — 초안 누락]**
- `§3` — manifest 구성 "CN 7580 vs AD+Dem 969" diff에 명기. 약 8:1 불균형. AUC 보고는 불균형에 robust하나, "morphometry ceiling" 판단이 이 불균형 하에서 이루어진 것임을 문서가 명시하지 않음. 향후 재사용자에게 오해 소지.

---

## 놓친 것·결정 필요

1. **reset 삭제 미검증**: 이 diff에는 파일 추가(+70)만 있다. `experiments/`(22GB 캐시+ckpt), `audits/`, `SPEC.md`, `CONTRIBUTION.md` 삭제가 실제 완료됐는지 이 diff로 확인 불가. reset이 이전 세션에서 수행됐다면 해당 삭제 커밋이 없는 이유가 별도로 설명돼야 함.

2. **"4개 방향" vs 문서 내 3개 방향 불일치**: §0은 "4개 독립 방향 배제"를 선언하지만 §1은 minyoung4 내 3개(A, B, P1)만 기술. F5에서 minyoung2 §9를 소급 포함한 것으로 추정되나, 이 카운팅이 명시적이지 않아 문서 내부 불일치.

3. **F5 minyoung2 §9 교차 검증 불가**: "de-confounding 3회 실패"의 세 번째 사례(minyoung2 §9)가 실제로 존재하고 동일 결론을 지지하는지 이 diff로 검증 불가.

4. **`data/` 정리 미결**: §4에서 `minyoung4/data/`(legacy `preprocessed_mm` 스냅샷)를 "별도 정리 대상"으로 선언했으나 이번 커밋에 정리 없음. 디스크 점유 규모와 정리 일정 불명확.

5. **minyoungi 표기 오류 수정 필요**: 아카이브 문서가 다른 워크스페이스를 오기로 참조하면 추후 탐색자에게 혼란. `minyoung1`이 맞는지 확인 후 수정 필요.

6. **EXP-003 교차 참조 미확인**: §4에서 `minyoungi EXPERIMENTS.md EXP-003`을 "정당 기여"로 선언하나 해당 파일에 이 탐색 결과가 실제 반영됐는지 이 diff로 확인 불가.

7. **다음 방향 미선언**: §6에서 longitudinal MCI→AD를 "고위험"으로 표시하며 보류. minyoung4 reset 후 다음 탐색 방향이 없는 상태.

---

## 미해결 질문

1. λ=0.3에서 LOCO +0.4pp가 노이즈인지, de-siting 소폭 유효 신호인지 — LOCO 6코호트 개별 AUC 분산 없어 판단 불가.
2. PET SUVR scale 정규화 방법은 무엇인가? 0.829→0.776 drop이 방법에 따라 달라질 수 있음.
3. `minyoungi` 표기가 `minyoung1`(숫자)의 오기인가, 별도 식별자인가?
4. F5 근거로 인용된 minyoung2 §9의 de-confounding 실패 기록이 실제로 존재하는가?
5. CN 7580 vs AD+Dem 969 불균형(~8:1) 하에서도 "morphometry ceiling" 판단이 동일하게 성립하는가 — stratified 분석 또는 balanced-subset 재확인이 있었는가?
6. `preprocessed_mm` 스냅샷 정리 일정과 담당자가 누구인가?

# === plant ===
## 진행 요약

plant 워크스페이스를 longitudinal에서 **microbrain(site/scanner bias-robust 표현학습)** 으로 완전 재정의했다. 당일 두 커밋으로 크게 두 단계를 완료했다: ① 설계 문서 일체(RESEARCH_BRIEF, CLAUDE.md 확장, P0 계획, DECISION_LOG, 다기관 RL 전략) + audit 라이브러리(`src/microbrain/audit.py`) 작성, ② P0 bias audit 노트북 6종(A0~A5) 실행·판정. 핵심 판정은 **"bias 실재(voxel→site 3.3×chance) + disease 분리가능(잔차 후 AUROC 0.722, drop −0.05)"** 이며 P2 진입 근거는 확보했으나, (b) 부피 너머 신호 천장 가설이 여전히 dominant prior다. 다음 단계(P2 설계 + Min 승인)는 미결 상태.

---

## 코드 리뷰

**1. 수치 표기 혼용 — `SCRATCHPAD.md:7–8`**
`A2 voxel→site **0.475(3.3×)**`에서 0.475가 balanced-acc인지 AUROC인지 명시 없음. 7-class chance balanced-acc = 1/7 ≈ 0.143 → 3.3× = 0.47(balanced-acc)로 계산은 맞지만, 같은 줄의 `A0 site-only AUROC 0.71`과 단위가 섞여 표기된다. AUROC 0.475(= chance 근처)로 오독되면 해석이 역전된다. 리포트·노트북에서 단위 명시 필수.

**2. A4 feature 기술 오류 + "separable" 해석 과장 — `SCRATCHPAD.md:8`, `docs/P0_bias_audit_plan.md` 표 A4 행**
초안은 "morphometry feature → site 회귀 잔차화"라고 기술했으나, P0 plan 측정 표의 A4 입력 컬럼은 **"A1/A2 feature"**(형태계측 + 복셀 강도 통계 혼합)로 명시돼 있다. "fs_vol_*만 사용"이라는 기술은 diff가 뒷받침하지 않으므로 수정이 필요하다. 핵심 우려(잔차화에 쓴 feature와 CDR 예측 feature의 동일성·순환성)는 [불확실] 태그가 적절하다. 아울러 A5 bar(0.766) 대비 −0.05 하락을 "분리가능" 근거로 단독 인용하는 것은 "site와 선형 공유된 형태계측 성분이 제거된 당연한 손실"로도 읽힌다 — SCRATCHPAD의 "=SEPARABLE" 단언은 약간 낙관적이다.

**3. confound V=0.42 환경에서 LOCO disease 해석 — `SCRATCHPAD.md:9`, `docs/P0_bias_audit_plan.md:§0.1`**
AJU 97.7% impaired rate 환경에서 LOCO hold-out AJU는 base-rate만으로도 CDR 예측이 쉬워지고, AJU를 train에서 빼면 impaired sample이 급감한다. "A5 LOCO mean 0.766, transport 됨"이라고 기록됐으나, P0 plan §3-A5가 명시한 per-cohort AUROC·bAcc·prevalence baseline이 SCRATCHPAD에 없다. mean 하나만으로 "transport"를 판정하기엔 근거가 부족하다. [불확실]

**4. 테스트 전면 삭제, 재추가 없음 — diff stat**
`tests/test_build_longitudinal_cases.py` −90줄 삭제 후, `src/microbrain/audit.py`(213줄)·`notebooks/_build_notebooks.py`(492줄)에 대응하는 새 테스트가 0줄 추가됐다. diff 상한으로 audit.py 내부 확인은 불가하다. P2 진입 조건인 "split/leakage 단위테스트 통과" 요건은 **`RESEARCH_BRIEF.md` §4 P1**에 명시돼 있다(초안이 "P0 plan §0"으로 귀속시킨 것은 출처 오류).

**5. `notebooks/_build_notebooks.py` 재현성 — diff 범위 초과**
492줄짜리 노트북 생성 스크립트 내부가 diff 상한으로 미공개다. [불확실] 매니페스트 절대경로(`/home/vlm/data/preprocessed_official/...`)에 의존할 경우 경로·파일 변경 시 재생성이 깨진다. seed 고정 여부 역시 확인 불가.

**6. `docs/DECISION_LOG.md` P0 항목 — 롤백 지점 미특정**
첫 항목은 `27b1665`로 해시를 특정했으나, P0 audit 완료 항목에는 "되돌아갈 지점: 직전 commit"만 기록됐다. "직전 commit"은 조회 시점에 따라 다른 커밋을 가리켜 롤백 근거로 쓸 수 없다.

**7. A1 vs A2 site 예측 강도 차이 미분석 — `SCRATCHPAD.md:7–8` (diff 추가 항목)**
SCRATCHPAD는 `A1 morph→site 0.27(2×)`와 `A2 voxel→site 0.475(3.3×)`를 나란히 기록한다. 형태계측보다 raw voxel이 site 정보를 1.65배 더 많이 담는다는 이 차이는 P2 설계(어떤 표현이 site 누수에 더 취약한가)에 직접 관련되지만, 초안 어느 항목에서도 분석되지 않았다.

**8. A3 결과 "4%" 의미 미분석 — `SCRATCHPAD.md:8` (diff 추가 항목)**
SCRATCHPAD는 `A3 atlas η²>0.1 = 4%`를 기록한다. atlas region 중 4%만 site bias η²>0.1을 보인다는 뜻으로, bias가 뇌 전체에 분산되지 않고 **국소 집중**됨을 시사한다. 이는 P2 patch-based/region-masked 아키텍처 선택에 관련되는 설계 입력이나, 분석에 반영되지 않았다.

---

## 놓친 것 · 결정 필요

**1. P0 실행 승인 불일치 (가장 중요)**
`docs/P0_bias_audit_plan.md` 상단: "Min 승인 전 학습·대형 추출·GPU 금지". SCRATCHPAD 시간순: "P0 설계 완료, 승인 대기" → "P0 audit 실행 완료". A2/A3 텐서 전수 1-pass는 plan에서 "smoke(350-subj) → Min 승인 → full"의 2단계 게이트로 설계됐으나, smoke 실행 결과 기록과 Min 명시 승인이 어느 커밋·노트에도 없다. **CLAUDE.md Confirmation Gate 위반 가능.**

**2. per-cohort breakdown 미기록**
SCRATCHPAD·DECISION_LOG 모두 A5 LOCO를 "mean 0.766"만 기록. P0 plan §3-A5는 per-cohort AUROC/bAcc + prevalence baseline + bootstrap CI를 산출물로 명시했다. 판정 근거가 mean 하나면 (특히 AJU처럼 base-rate가 극단적인 코호트를 포함할 때) 재검토가 어렵다.

**3. P2 진입 조건 미충족 — 단위테스트**
`RESEARCH_BRIEF.md` §4 P1: "split/leakage 단위테스트가 통과해야 P2 진입". 현재 test suite는 0이다. P2 설계 논의가 이미 SCRATCHPAD에 올라와 있어 조건 충족 없이 논의가 선행됐다.

**4. `docs/multisite_RL_strategy.md` 지위 모호 — `SCRATCHPAD.md` 두 번째 항목**
"가설·pre-P0"로 표시됐으나, P0 결과(A4/ADNI V<0.06 확인)를 근거로 "측정으로 뒷받침되는 유일 진입로"라는 전략을 확정처럼 기술했다. P0 판정이 "bias 실재+분리가능"으로 완료된 지금 전략 문서 상태가 pre/post 혼용이다.

**5. 미결 목록 3개 — `SCRATCHPAD.md` 미해결 섹션**
top-level 승격 여부, OBSERVATORY 6번째 등록 여부, P2 arm 우선순위 — 세 항목이 당일 커밋에서 결정되지 않았다. P2 arm 우선순위는 P2 설계서 작성 전에 확정돼야 한다.

**6. `docs/REPO_STRUCTURE.md` 생성 미추적 (diff 추가 항목)**
diff stat은 `docs/REPO_STRUCTURE.md` +46줄 신규 생성을 보여주지만, SCRATCHPAD에서 한 줄 언급("규약=docs/REPO_STRUCTURE.md")에 그치고 DECISION_LOG에 구조 결정으로 기록되지 않았다. 규약 문서 자체가 어떤 결정의 결과인지 추적 불가다.

**7. 구 longitudinal 산출물 삭제 미기록 (diff 추가 항목)**
diff stat은 `scripts/build_longitudinal_cases.py` −308줄, `note/SCRATCHPAD.md` −97줄, `note/daily/2026-06-01.md` −40줄, `…longitudinal-incremental-transport-prereg.md` −96줄 삭제를 보여준다. DECISION_LOG는 라인 재정의를 기록했으나 삭제된 파일의 구체적 목록·이유가 없어 되돌리기가 필요한 경우 어느 파일이 사라졌는지 로그만으로 파악할 수 없다.

---

## 미해결 질문

1. **A2 smoke → full 게이트**: smoke(350-subj) 실행 결과 파일이 어느 노트북/리포트에 있는가? full pass에 대한 Min의 명시적 승인은 언제 어디서 이루어졌는가?

2. **A4 feature 구성**: 실제 실행 시 잔차화에 사용된 feature가 plan대로 A1+A2(형태계측+복셀) 혼합인가, 아니면 형태계측(A1)만인가? 어느 쪽이든 "잔차화 input ≠ CDR 예측 input"임을 SCRATCHPAD가 명시하지 않아 재현이 어렵다.

3. **A5 per-cohort 수치**: AJU(97.7% impaired) hold-out 시 CDR LOCO AUROC는 얼마인가? prevalence baseline 대비 delta와 bootstrap CI는?

4. **"유일 진입로" 근거 강도**: A4/ADNI vendor⊥dx V<0.06이 "측정으로 뒷받침되는 유일 진입로"라는 결론이 다른 코호트 배제 논리인가, "clean 환경에서 먼저 검증 후 확장"인가? `docs/multisite_RL_strategy.md` 내에 이 구분이 명확히 기재돼 있는가(diff 범위 밖).

5. **`_build_notebooks.py` 재현성**: 노트북 6종을 다른 날·다른 manifest 버전으로 재실행하면 동일 수치가 재현되는가? seed 고정 여부가 스크립트 내에 있는가?


</details>
